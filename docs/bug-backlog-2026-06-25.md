# Bug-hunt backlog — cycle 1 (2026-06-25)

Adversarial multi-agent hunt (8 finder lenses x up to 5 rounds, skeptic-verified). 102 confirmed: 3 critical, 36 high, 40 medium, 23 low. Single-skeptic verify is permissive — **re-verify each before fixing** (some proposed fixes would break legit behavior, e.g. the device-token 'hijack' overwrite is intentional for login/logout hand-off).

✅ = fixed this session.  Counts/titles are the finders' own; file:line best-effort.

## CRITICAL (3)

- ✅ **Public /auth/register accepts attacker-chosen role → self-register as admin (full privilege escalation)**
  - `backend/internal/handlers/auth.go:102-114 (Register); route in cmd/api/main.go:171`
  - Register reads the account role straight from the request body (`role := req.Role`) with the only normalization being empty→consumer. There is NO allowlist restricting the value to consumer/seller. The DB column explicitly permits admin: migration 003_courier.sql defines `CHECK (role IN ('consumer','seller','admin','courier'))`, so `INSERT ... role='admin'` succeeds, and generateTokens then mints …
  - _Fix:_ Validate/clamp the requested role to the set of self-serviceable roles before insert. In Register (and mirror in SocialLogin), replace the `role := req.Role; if role == "" { role = models.RoleConsumer }` block with an explicit allowlist, e.g.:    role := req.Role   switch role {   case "":       rol…
- ✅ **Social-login and phone-OTP signup honor an attacker-chosen role (admin self-registration)**
  - `backend/internal/handlers/social_auth.go:115-160 (and backend/internal/handlers/phone_auth.go:251-264)`
  - Both public account-creation paths take the role straight from the request body and INSERT a new user with it, with no allowlist. In SocialLogin (social_auth.go:115-118, 154-160) `role := req.Role` flows directly into `INSERT INTO users (... role ...) VALUES (... $5 ...)`. createPhoneUser (phone_auth.go:251-264), reached from VerifyPhoneLogin, does the same. `models.UserRole` is a bare string with…
  - _Fix:_ Add a single shared role-allowlist helper (e.g. `func sanitizeSignupRole(r models.UserRole) (models.UserRole, bool)` returning false for anything other than consumer/seller/courier) and call it at every account-creation entry point. In SocialLogin and createPhoneUser, after defaulting empty role to …
- ✅ **Scheduled orders auto-rejected & refunded the instant they promote to pending**
  - `backend/internal/scheduler/dispatcher.go:354-395 (sweepScheduled) and 603-612 (sweepStaleRejection)`
  - A scheduled order is created with status='scheduled' and created_at=now (when the customer placed it, possibly hours/days before the delivery window). sweepScheduled flips it to 'pending' 30 minutes before the window with `UPDATE orders SET status='pending'` and intentionally does NOT touch created_at. In the SAME runAll tick, sweepScheduled (line 254) runs immediately before sweepStaleRejection (…
  - _Fix:_ Make the stale-pending SLA clock start at promotion time, not order-creation time, for scheduled orders. Cleanest: in sweepScheduled set a promotion timestamp (e.g. `UPDATE orders SET status='pending', updated_at=NOW()` is already there — but stale-rejection keys off created_at). Either (a) change s…

## HIGH (36)

- **Menu item sheet never pre-selects default modifiers (iOS does)**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/screens/restaurant/MenuItemSheet.kt:69 (selections init); 102-108 (requiredGroupsValid)`
  - Android initializes modifier selections as an empty map (`val selections = remember(menuItem.id) { mutableStateMapOf<String, Set<String>>() }`) and never applies the seller-configured default options. The Modifier model carries `is_default` (Models.kt line 300), and iOS MenuItemView.swift explicitly pre-selects each group's defaults onAppear (lines 221-230: `group.modifiers.filter(\.isDefault)`) p…
  - _Fix:_ Seed `selections` from each group's default modifiers on first composition, mirroring iOS. e.g. change the remember to compute initial defaults: `val selections = remember(menuItem.id) { mutableStateMapOf<String, Set<String>>().apply { menuItem.modifierGroups.forEach { g -> val defs = g.modifiers.fi…
- **Cart addItem silently drops new special instructions when merging a no-modifier item into an existing line**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/viewmodels/CartViewModel.kt:189-202`
  - addItem() merges a newly added item into an existing cart line whenever the item has no modifiers and matches by menuItem.id only (`it.menuItem.id == menuItem.id && it.selectedModifiers.isEmpty()`), ignoring specialInstructions. On the merge branch it does `existing.copy(quantity = minOf(existing.quantity + quantity, 99))`, which keeps the OLD note and discards the freshly-supplied `specialInstruc…
  - _Fix:_ Include the (normalized) special instructions in the merge-match predicate so a differing note creates a new line instead of overwriting, matching iOS. Compute the normalized note once and use it in both the match and the new CartItem:  val normalizedNotes = specialInstructions?.trim()?.take(500)?.t…
- **Default percent tip silently dropped to $0 on process-death checkout recovery (iOS re-refresh missing)**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/viewmodels/CheckoutViewModel.kt:452-497 (refreshBundle), 464`
  - On the process-death / Stripe-3DS-activity-recreation recovery path, bootstrap() is re-entered with an empty localCart, so `_localSubtotalCents` stays 0. In refreshBundle(), `tipBase = _uiState.value.bundle?.subtotal ?: _localSubtotalCents` is 0 (bundle is null on the first call), so currentTipCents(0) computes the default 18% Percent tip as `(0 * 1800) / 10_000 = 0`. The backend only honors an ab…
  - _Fix:_ Mirror iOS: in refreshBundle(), before nulling the bundle, capture `val neededReRefresh = state.tipChoice is TipChoice.Percent && tipBase == 0`. After the successful response sets the bundle, if `neededReRefresh && resp.body()?.tip == 0 && (resp.body()?.subtotal ?: 0) > 0 && currentTipCents(resp.bod…
- **Android available-delivery card shows payout WITHOUT the tip (lower than actual and inconsistent with iOS)**
  - `android/courier/app/src/main/java/com/koshereats/courier/ui/screens/dashboard/DashboardScreen.kt:177`
  - The Android courier 'available delivery' card headline shows only `d.deliveryFee.formatPrice()`. The backend ListAvailableDeliveries returns courier_tip in the payload, and iOS shows deliveryFee + courierTip as the headline payout (ios/courier/KosherEatsCourier/Views/Dashboard/DashboardView.swift:308). The Android AvailableDelivery model (android/.../data/models/Models.kt:158-170) does not even de…
  - _Fix:_ Add `@SerializedName("courier_tip") val courierTip: Int = 0` to the AvailableDelivery data class in android/courier/.../data/models/Models.kt, and change DashboardScreen.kt line 177 to show the combined amount, e.g. `Text((d.deliveryFee + d.courierTip).formatPrice(), ...)`, optionally adding an 'inc…
- **external_provider='dispatching' claim sentinel can leak permanently, stranding the order with no delivery path**
  - `backend/internal/dispatch/external.go:72-98 (claim CAS + release), no reaper anywhere`
  - Dispatch() claims an order for external dispatch by CAS-setting external_provider='dispatching', then makes blocking provider quote/CreateDelivery calls before either persisting the real delivery id or calling release() to NULL the sentinel. If the process dies (OOM kill / deploy / panic) between the claim CAS (line 72) and the persist (line 207) or release (line 91), the order is left with extern…
  - _Fix:_ Add a reaper to the scheduler (mirroring the payout 'processing' reaper) that resets stale claims, e.g. in runAll: `UPDATE orders SET external_provider=NULL, updated_at=NOW() WHERE external_provider='dispatching' AND external_delivery_id IS NULL AND updated_at < NOW() - interval '5 minutes'`. This r…
- ✅ **UpdateCartItem silently discards the notes sent in the request body**
  - `backend/internal/handlers/cart.go:277-280`
  - UpdateCartItemRequest carries both `quantity` and `notes` (decoded at line 262), but the UPDATE only sets `quantity`: `UPDATE cart_items SET quantity = $1 WHERE id = $2 ...`. Any `notes` the client edits via this endpoint are accepted (no error) yet never persisted, so the special-instruction the customer typed (e.g. "no onions", allergy note) is dropped and the stale/empty note is what flows into…
  - _Fix:_ Persist notes in the same UPDATE: `UPDATE cart_items SET quantity = $1, notes = $2 WHERE id = $3 AND cart_id IN (SELECT id FROM carts WHERE user_id = $4)` passing req.Quantity, req.Notes, itemID, user["user_id"]. Optionally enforce the same len<=500 cap used in AddToCart.
- **Courier payout pays out the full delivery_fee (incl. provider-cost portion) plus a 2.5% bump funded by a zero service fee**
  - `backend/internal/handlers/courier_orders.go:318-322`
  - DeliverOrder computes `platformBump := subtotal * 25 / 1000` and `payout := deliveryFee + tip + platformBump` (courier_orders.go:321-322). The inline comment (lines 318-320) justifies this as '2.5% ... paid out of our service fee' combined with 'the consumer's 2.5% (already inside delivery_fee)'. Both premises are false against the current fee model: (1) serviceFee is hard-coded to 0 everywhere (o…
  - _Fix:_ Recompute payout against the current fee model and the platform's actual margin. The courier should receive the labor portion of the delivery fee, not the provider-cost passthrough plus the platform's markup. Capture the markup (and providerFee) at order time (e.g. persist provider_fee_cents on the …
- **Approved courier with no Stripe Connect account is never paid (payouts silently dropped)**
  - `backend/internal/handlers/courier_orders.go:346-363`
  - A courier is allowed to go online, claim, and deliver as soon as onboarding_status='approved' (SetCourierOnline only checks 'approved'; ClaimOrder/PickupOrder/DeliverOrder never check Stripe Connect). Approval does NOT require a stripe_connect_id: the Checkr stub auto-approve sets payout_ready=true but leaves stripe_connect_id NULL (backend/internal/background/checkr.go:80-84), and AdminApproveCou…
  - _Fix:_ Do not silently drop the payout when connectID is empty. Either (a) block going online/claiming until stripe_connect_id is set (gate SetCourierOnline and ClaimOrder on stripe_connect_id IS NOT NULL / payout_ready), or (b) still INSERT the courier_payout_queue row when connectID=='' using a placehold…
- **ClaimOrder lets one courier hold multiple active deliveries (no busy-guard) → second order's live tracking freezes**
  - `backend/internal/handlers/courier_orders.go:218-227`
  - The manual claim path (ClaimOrder) atomically sets courier_id only on the predicates `status = 'ready' AND courier_id IS NULL AND external_provider IS NULL`. It does NOT check that the claiming courier isn't already on an active delivery. The auto-dispatcher deliberately does enforce this invariant (internal/scheduler/dispatcher.go:499-503 selects couriers via `NOT EXISTS (SELECT 1 FROM orders o2 …
  - _Fix:_ Add the same busy-check the auto-dispatcher uses to the ClaimOrder UPDATE so a courier can't claim while already on an active delivery. Change the WHERE clause to: `UPDATE orders SET courier_id = $1, claimed_at = NOW(), updated_at = NOW() WHERE id = $2 AND status = 'ready' AND courier_id IS NULL AND…
- **Available-deliveries feed leaks every consumer's exact home address + GPS coords to all couriers pre-claim**
  - `backend/internal/handlers/courier_orders.go:85-95 (ListAvailableDeliveries query)`
  - ListAvailableDeliveries (GET /api/v1/courier/deliveries/available) returns o.delivery_address, o.delivery_lat and o.delivery_lng for every unclaimed 'ready' delivery order to ANY approved courier — before that courier has claimed the order. Courier signup is public and self-onboarding, so the population of 'approved couriers' is large and not vetted for access to consumer home addresses. This mean…
  - _Fix:_ Redact the consumer dropoff details in ListAvailableDeliveries exactly as ListUpcomingDeliveries already does: replace `o.delivery_address, o.delivery_lat, o.delivery_lng` in the SELECT with `'' AS delivery_address, 0::float AS delivery_lat, 0::float AS delivery_lng` so the precise address/coords ar…
- **Pickup/Deliver/Location don't re-check approval — a courier suspended after claiming still completes the delivery and gets paid**
  - `backend/internal/handlers/courier_orders.go:248-402`
  - SetCourierOnline, ListAvailableDeliveries, ListUpcomingDeliveries and ClaimOrder all gate on `onboarding_status='approved'`, but PickupOrder (line 256), DeliverOrder (line 324) and UpdateCourierLocation (line 423) gate ONLY on `courier_id = $user` and the order status — they never re-read onboarding_status. So a courier who has already claimed an order and is then suspended/rejected by an admin ca…
  - _Fix:_ Add an `onboarding_status='approved'` predicate to the order UPDATEs in PickupOrder and DeliverOrder (e.g. by joining/subselecting courier_profiles: `... AND EXISTS (SELECT 1 FROM courier_profiles cp WHERE cp.user_id = $courier AND cp.onboarding_status='approved')`), and short-circuit UpdateCourierL…
- **Courier feed + claim ignore restaurant/external delivery_mode — KE courier can poach a self-delivery order and get paid the full fee**
  - `backend/internal/handlers/courier_orders.go:86-95 (ListAvailableDeliveries query), 210-217 (ClaimOrder UPDATE)`
  - The auto-dispatch sweep respects restaurants.delivery_mode (dispatcher.go tryAutoAssign: 'restaurant' is a no-op, 'external' goes only to Uber/DoorDash), but the manual courier path in courier_orders.go references delivery_mode nowhere (grep count = 0). ListAvailableDeliveries selects every order with status='ready' AND courier_id IS NULL AND fulfillment_type='delivery' regardless of the restauran…
  - _Fix:_ Join restaurants and filter the available/upcoming feeds to platform-eligible orders, and add the same guard to the claim CAS. In ListAvailableDeliveries and ListUpcomingDeliveries add `AND COALESCE(rest.delivery_mode,'platform') = 'platform'` to the WHERE clause. In ClaimOrder, change the UPDATE to…
- **RegisterDevice lets any user hijack another user's push token (notification interception/DoS)**
  - `backend/internal/handlers/devices.go:47-52`
  - RegisterDevice upserts device_tokens with `ON CONFLICT (token, app) DO UPDATE SET user_id = EXCLUDED.user_id`, and the table's only unique constraint is `(token, app)` (migration 004_push_notifications.sql line 14) — there is no per-user binding. The `token` is a client-supplied string with no proof the caller actually owns that device (no APNs/FCM ownership validation). So any authenticated user …
  - _Fix:_ Do not let a conflicting token silently change owners. Change the upsert so a token can only be (re)bound by the user who currently owns it or when it is unowned, e.g. add `WHERE device_tokens.user_id = EXCLUDED.user_id` to the DO UPDATE and, on a cross-user conflict, first `DELETE FROM device_token…
- **External (Uber/DoorDash) orders get permanently stuck if an intermediate status webhook is dropped**
  - `backend/internal/handlers/doordash_webhook.go:77-107`
  - For externally-dispatched orders the provider webhook is the ONLY mechanism that advances order status — there is no reconciliation sweep in scheduler/dispatcher.go and no status polling in uberdirect/doordash clients (confirmed by grep: no GetDelivery/Status/poll). The `delivered` handler updates only `WHERE id = $2 AND status = 'picked_up'` (doordash_webhook.go:100, uber_direct_webhook.go:132). …
  - _Fix:_ Make the `delivered` transition tolerant of a missed pickup event by widening its guard to also accept the pre-pickup states for external orders, e.g. `WHERE id = $2 AND status IN ('ready','picked_up') AND external_delivery_id IS NOT NULL` (and stamp picked_up_at via COALESCE so it isn't left null).…
- **LinkProvider lets one OAuth/phone identity bind to multiple accounts (racy app-only guard, no DB constraint)**
  - `backend/internal/handlers/link_auth.go:84-101`
  - LinkProvider's only protection against binding a provider identity that already belongs to another user is the SELECT at lines 84-92, which (a) is scoped to users with the *same* role AND vertical as the caller, and (b) is a check-then-act read with no row lock, followed by an unconstrained INSERT at lines 98-102. There is no DB uniqueness on (provider, provider_id): migration 022_user_auth_provid…
  - _Fix:_ Add a partial-or-full UNIQUE constraint on user_auth_providers(provider, provider_id) (a new migration: `CREATE UNIQUE INDEX uq_uap_provider_identity ON user_auth_providers (provider, provider_id);`) so the database is the single source of truth, and in LinkProvider replace the read-then-insert with…
- **CompleteOrder lets a seller skip the entire courier picked_up→delivered flow on delivery orders**
  - `backend/internal/handlers/orders.go:1052-1081`
  - CompleteOrder's UPDATE matches `orders.status = 'ready'` for ANY fulfillment_type (the pickup-specific relaxation is only the second OR-branch). A `delivery` order in `ready` status can therefore be flipped straight to `completed` by the seller — even after a KE courier has already self-claimed it (courier_id set, status still 'ready', courier en route but not yet hit /pickup). This skips the requ…
  - _Fix:_ Restrict the `ready` branch to pickup orders as well, so delivery orders can only reach a terminal via the courier/seller-deliver path. Change the WHERE to `AND ((orders.status = $4 OR orders.status = $5) AND orders.fulfillment_type = 'pickup')` (i.e. require fulfillment_type='pickup' for BOTH the r…
- **CreateOrder charges the customer but returns 500 with no order and no refund when the once-per-user deal unique index conflicts**
  - `backend/internal/handlers/orders.go:182-245`
  - VerifyPaymentSucceeded (line 182) confirms the Stripe charge BEFORE the orders INSERT. The INSERT's `ON CONFLICT (stripe_payment_id) WHERE stripe_payment_id != '' DO NOTHING` only handles a duplicate-payment-intent conflict. But migration 043 adds a SECOND partial unique index `uq_orders_user_deal_active` on (user_id, applied_deal_id) WHERE applied_deal_id IS NOT NULL AND status NOT IN ('rejected'…
  - _Fix:_ Detect the deal-conflict explicitly and convert it into a clean, refunding response. After the Scan, check for a pgconn.PgError with Code=="23505" and ConstraintName containing "uq_orders_user_deal_active"; on that, roll back the tx, refund the PaymentIntent via h.stripe.RefundPaymentIntent(req.Paym…
- **Refund issued before tx.Commit is not atomic — commit failure leaves a refunded order still fulfillable**
  - `backend/internal/handlers/orders.go:664-688 (CancelOrder); same pattern RejectOrder 1196-1212, scheduler tryStaleReject)`
  - CancelOrder performs the real Stripe refund (h.stripe.RefundPaymentIntent, line 676) BEFORE tx.Commit (line 685). The inline comment claims 'both succeed or both fail,' but a Stripe refund is not part of the Postgres transaction. If RefundPaymentIntent succeeds and then tx.Commit fails (connection blip, deadlock, shutdown), the deferred Rollback reverts the status UPDATE and the order returns to i…
  - _Fix:_ Make the money side recoverable rather than pretending it is transactional: commit the DB status flip FIRST, then issue the refund after commit (so a failed refund can be retried by a reconcile sweep against an already-cancelled/rejected order), OR record an intent row inside the tx and let an idemp…
- ✅ **CreateOrder cart-item scan loop never checks rows.Err() → charged customer, no order**
  - `backend/internal/handlers/orders.go:88-106`
  - The cart-items loop in CreateOrder (`for itemRows.Next() { ... subtotal += item.Price * item.Quantity ... }`) iterates the pgx.Rows but never checks `itemRows.Err()` after the loop. pgx surfaces a row-stream error (connection reset, decode failure on a later row, server-side error mid-result) only via rows.Err(); rows.Next() simply returns false and the loop exits early. When that happens after so…
  - _Fix:_ After the `for itemRows.Next()` loop (right before the `if len(items) == 0` check at line 108), add: `if err := itemRows.Err(); err != nil { slog.Error("failed reading cart items during order creation", "cart_id", cart.ID, "error", err); writeError(w, http.StatusInternalServerError, "failed to read …
- **Self-delivery tip charged to customer is dropped from seller earnings ledger (kept by platform)**
  - `backend/internal/handlers/orders.go:1400-1415`
  - On a restaurant self-delivery order (delivery_mode='restaurant'), the order is fulfillment_type='delivery', so CreateOrder does NOT zero the tip — the customer is charged courier_tip as part of the PaymentIntent total, intending it for the restaurant's own driver. But SellerDeliverOrder credits the seller only deliveryFee/2 and completely ignores courier_tip: `sellerShare := deliveryFee / 2` and `…
  - _Fix:_ Include the tip in the self-delivery credit. Load courier_tip alongside delivery_fee in the SELECT (SellerDeliverOrder already does `SELECT rest.delivery_mode, o.delivery_fee` — add `o.courier_tip`), then credit `sellerShare := deliveryFee/2 + courierTip` (the seller's driver gets the full tip plus …
- **CompleteOrder lets a seller force a delivery order from ready→completed, stranding the assigned courier and skipping payout**
  - `backend/internal/handlers/orders.go:1052-1057`
  - CompleteOrder's CAS is `AND (orders.status = 'ready' OR (orders.status = 'preparing' AND orders.fulfillment_type = 'pickup'))`. The `status = 'ready'` branch is unconditional — it matches ANY ready order, including platform-delivery and external-delivery orders, not just pickup. A courier claims a delivery order while it is still in 'ready' (ClaimOrder only sets courier_id; status stays 'ready' un…
  - _Fix:_ Restrict the ready→completed branch to pickup orders, mirroring what both clients already assume. Change the CAS predicate to `AND ((orders.status = 'ready' OR orders.status = 'preparing') AND orders.fulfillment_type = 'pickup')`, i.e. only pickup orders may be completed by the seller; delivery comp…
- **Revenue/order "today" totals use UTC calendar date, misattributing evening orders**
  - `backend/internal/handlers/seller.go:1027, 1035, 1042, 1052 (and admin.go:450-451)`
  - GetDashboardStats (seller home screen) and AdminStats both bucket "today" with `o.created_at::date = CURRENT_DATE`. orders.created_at is TIMESTAMPTZ (migrations/001_initial.sql:151) and the pgx pool is built from DATABASE_URL with no TimeZone runtime param set (database.go:18 — no RuntimeParams), so the session timezone defaults to the server default, which is UTC on Fly Postgres. `CURRENT_DATE` a…
  - _Fix:_ Pin a business timezone for the day boundary instead of relying on the UTC session. Replace `o.created_at::date = CURRENT_DATE` with `(o.created_at AT TIME ZONE 'America/New_York')::date = (NOW() AT TIME ZONE 'America/New_York')::date` in all four predicates in GetDashboardStats and both predicates …
- **Approved restaurant can change its kosher certification post-approval with no admin re-review**
  - `backend/internal/handlers/seller.go:413-447 (UpdateRestaurant UPDATE) and 429,435`
  - UpdateRestaurant lets a seller PUT /api/v1/seller/restaurant and update kosher_certification and kosher_certificate_url (params $15 and $22) via COALESCE on an already-approved restaurant, and the handler never resets approval_status back to 'pending' or clears is_active. The entire restaurant-approval flow (restaurant_approval.go magic links, AdminSetRestaurantApproval, the no-login admin dashboa…
  - _Fix:_ In UpdateRestaurant, detect when kosher_certification, kosher_certificate_url (and arguably certifying_agency / is_cholov_yisroel / is_pas_yisroel / is_glatt_kosher) are being changed on an already-approved restaurant, and on such a change set approval_status='pending', is_active=false, reviewed_at=…
- **External cancel webhook strips dispatch linkage with no status guard, permanently stranding orders past 'ready'**
  - `backend/internal/handlers/uber_direct_webhook.go:151-165 (and doordash_webhook.go 119-125)`
  - The Uber 'canceled' and DoorDash 'cancelled' webhook branches run `UPDATE orders SET external_delivery_id = NULL, external_provider = NULL, external_tracking_url = NULL WHERE id = $1` with NO status predicate. The intended effect is to re-open a still-'ready' order for re-dispatch (the auto-dispatch sweep at dispatcher.go:442 and ClaimOrder at courier_orders.go:221 both key off `external_provider …
  - _Fix:_ Scope the cleanup UPDATE so it only re-opens orders that are actually still re-dispatchable, and only when the linkage matches the canceling delivery. Match the delivery id to avoid acting on a stale/duplicate event, and guard on status: `UPDATE orders SET external_delivery_id = NULL, external_provi…
- **Courier payout double-pay / never-pay across the legacy↔Temporal payout mode switch (mismatched Stripe idempotency keys)**
  - `backend/internal/scheduler/dispatcher.go:857 (legacy) vs backend/internal/payout/payout.go:62-104 (Temporal)`
  - The same courier_payout_queue row can be transferred via two different Stripe idempotency keys depending on which code path runs it. The legacy sweep calls TransferToCourier with idempotencyKey = the queue row UUID `p.id` (dispatcher.go:857). The Temporal path calls TransferToCourier with idempotencyKey = WorkflowID(orderID) = "payout-"+orderID (payout.go:62, passed through StripeTransfer at :104)…
  - _Fix:_ Make the Stripe idempotency key a stable function of the payout identity that is identical across both regimes — e.g. derive it from the order_id (or the queue row id) in BOTH paths: pass `WorkflowID(in.OrderID)`... no — pass the queue row id in the Temporal path too, OR switch the legacy path to ke…
- **tryStaleReject refunds before commit — commit failure leaves a refunded order still fulfillable (free food)**
  - `backend/internal/scheduler/dispatcher.go:671-697`
  - tryStaleReject issues the Stripe refund (d.stripe.RefundPaymentIntent at line 672) BEFORE the transaction commits, then does the status UPDATE to 'rejected' (line 681) and commits (line 692). The refund is a real, irreversible side effect that is NOT part of the transaction. If tx.Commit fails at line 692 (network blip, ctx cancellation on shutdown, serialization error), the deferred tx.Rollback r…
  - _Fix:_ Flip the order status to a non-acceptable terminal/sentinel state inside the transaction and commit it FIRST (so the row can no longer be accepted), then issue the refund AFTER commit with its own durable retry; OR record the refund intent durably (a refund-queue row written in-tx) and have a separa…
- **Courier payout can be paid twice: Temporal workflow and legacy sweep use different Stripe idempotency keys**
  - `backend/internal/scheduler/dispatcher.go:857 (tryPayout) vs payout.go:62-63/98-104 (StripeTransfer)`
  - The two payout code paths transfer money to the courier's Stripe Connect account using DIFFERENT idempotency keys, so Stripe cannot dedup a payout processed by both paths. The Temporal workflow (internal/payout/payout.go) transfers with idemKey = WorkflowID(orderID) = "payout-<orderID>" (payout.go line 62-63, passed to StripeTransfer at line 98-104). The legacy direct-transfer sweep (dispatcher.go…
  - _Fix:_ Make both paths use the SAME order-derived idempotency key so Stripe dedups across paths. In dispatcher.go tryPayout, change the transfer call to use the order-scoped key instead of the row id: `d.stripe.TransferToCourier(p.connectID, p.amountCents, p.orderID, payout.WorkflowID(p.orderID))` (import …
- **Order tracking has no handling for external (Uber Direct / DoorDash) deliveries — frozen map, no courier, no tracking link**
  - `ios/consumer/KosherEatsConsumer/Models/Models.swift:527-616 (Order struct); OrderTrackingViewModel.swift:121-192; OrderTrackingView.swift:148-203`
  - The backend dispatches a large share of deliveries to Uber Direct / DoorDash (backend/internal/dispatch/external.go). For those orders NO platform courier is ever assigned: the order's `courier` stays nil, and the backend instead stamps `external_provider`, `external_delivery_id`, and `external_tracking_url` on the order (backend/internal/models/models.go:281-283, returned by GET /orders/:id). The…
  - _Fix:_ Add the three external fields to the Order Codable model: `var externalProvider: String?`, `var externalDeliveryID: String?`, `var externalTrackingURL: String?` with CodingKeys external_provider / external_delivery_id / external_tracking_url. In OrderTrackingView, when externalTrackingURL is non-nil…
- **Custom tip is capped at a flat $500 with no subtotal check, so small carts hit a server 400 ("tip cannot exceed subtotal") and the user can place an order with a tip different from the one shown in the field**
  - `ios/consumer/KosherEatsConsumer/ViewModels/CheckoutViewModel.swift:24-26, 131-141, 145-157`
  - updateCustomTip() (L135) and currentTipCents() (L155) only clamp the custom tip to maxTipCents = $500; neither validates against bundle.subtotal. The comment at L25 even claims this "Matches the backend cap (tip <= subtotal)" but it does not. The backend rejects any tip > subtotal with HTTP 400 "tip cannot exceed subtotal" (payments.go L170-172, createorder orders.go L176-178). So a user with, e.g…
  - _Fix:_ Cap the custom tip against the current subtotal, not a flat $500. In updateCustomTip and currentTipCents use `let cap = min(Self.maxTipCents, bundle?.subtotal ?? Self.maxTipCents)` and clamp/reject against `cap`, surfacing an inline message like "Tip can't exceed your item subtotal". Also, when refr…
- **An unrecoverable in-flight PaymentIntent permanently locks the user out of ALL future checkouts (no give-up/expiry/manual clear)**
  - `ios/consumer/KosherEatsConsumer/ViewModels/CheckoutViewModel.swift:516-528, 578-584 (key 444)`
  - After a successful charge, `placeOrder` persists the PaymentIntent id in UserDefaults (`persistedInflightPI = paymentIntentId`, line 488) BEFORE calling createOrder. The marker is ONLY ever cleared when an order is found: createOrder returns (line 506), recoverOrder finds it (line 519), or reconcileInflightOrder finds it (line 567). If a charge succeeds but no order is ever created server-side and…
  - _Fix:_ Store the marker as a struct/JSON with a created-at timestamp (or a parallel `inflight_payment_intent_at` key). In `passesInflightGuard()`, after reconcile still fails, only block if the marker is recent (e.g. < a few minutes old); once it ages past a TTL, clear it and allow a fresh order (the orpha…
- **Pickup orders record the consumer's home address as delivery_address — pickup screens show "Pickup from: <home address>" and a home pin on the map**
  - `ios/consumer/KosherEatsConsumer/Views/Cart/CheckoutView.swift:250-264 (pay/payWithApplePay), 150 (Retry)`
  - For a pickup order the app still sends the consumer's saved default address as the order's delivery_address. loadAddresses() always sets vm.selectedAddress to the default/first saved address regardless of fulfillment type, and the Pickup branch only hides the AddressCard — it never clears selectedAddress. pay() (line 255), payWithApplePay() (line 263), and the Retry handler (line 150) all call pla…
  - _Fix:_ For pickup, do not send a delivery address. At the three call sites pass nil for pickup, e.g. in pay()/payWithApplePay(): `let addr = vm.fulfillmentType == "pickup" ? nil : vm.selectedAddress` and call `finishIfSucceeded(bundle: bundle, address: addr)`; do the same in the Retry block. placeOrder alr…
- **Changing the delivery address at checkout never re-prices the order — customer is charged the old zone's delivery fee**
  - `ios/consumer/KosherEatsConsumer/Views/Cart/CheckoutView.swift:114-140 (and .task at 101-113)`
  - On checkout load the bundle is fetched once against the default address (`loadAddresses()` then `refreshBundle()`, lines 108-109). `refreshBundle()` sends `selectedAddress?.formatted` to /payments/intent so the PaymentIntent is priced (and the delivery fee is stamped into PI metadata) for THAT address. The view re-fetches the bundle on `.onChange(of: vm.tipSelection)` and `.onChange(of: vm.customT…
  - _Fix:_ Add an address-change trigger that re-fetches the bundle, mirroring the tip handlers. Because `Address` is not Equatable, key the observer on the id: add `.onChange(of: vm.selectedAddress?.id) { _, _ in bundleRefreshTask?.cancel(); pendingBundleRefresh = false; bundleRefreshTask = Task { await vm.re…
- **Default modifier pre-selection ignores single-select limit and availability**
  - `ios/consumer/KosherEatsConsumer/Views/Restaurant/MenuItemView.swift:221-230 (AddToCartSheet.onAppear); related: 133-140 unitPrice, 154-158 allSelectedIDs, 467-478 toggle`
  - When the add-to-cart sheet appears it seeds the selection for every modifier group with ALL options flagged is_default (`group.modifiers.filter(\.isDefault)`), with no regard for the group's maxSelections (so a single-select group, max_selections == 1, can start with 2+ options selected) and no regard for is_available (an unavailable option can be pre-selected). I confirmed there is no DB constrai…
  - _Fix:_ In the onAppear seeding loop, filter defaults to available options and respect the group's selection cap: `let defaults = group.modifiers.filter { $0.isDefault && $0.isAvailable }.sorted { $0.sortOrder < $1.sortOrder }.map(\.id); if !defaults.isEmpty { initial[group.id] = Set(defaults.prefix(group.i…
- **iOS seller cannot pick-up/deliver its own self-delivery orders (missing endpoints)**
  - `ios/seller/KosherEatsSeller/Services/APIService.swift:754-801 (and SellerOrderDetailView.swift actionButtons 434-458)`
  - The backend exposes two seller-only endpoints for restaurants that self-deliver (delivery_mode == 'restaurant'): PATCH /seller/orders/{id}/pickup (SellerPickupOrder, ready→picked_up) and PATCH /seller/orders/{id}/deliver (SellerDeliverOrder, picked_up→delivered, which also writes seller_delivery_earnings = delivery_fee/2). The Android seller app calls both (android/.../data/api/ApiService.kt lines…
  - _Fix:_ Add `func sellerPickupOrder(id:) async throws -> Order` (PATCH sellerPath("/seller/orders/\(id)/pickup")) and `func sellerDeliverOrder(id:) async throws -> Order` (PATCH .../deliver) to APIService, mirroring the existing markOrderReady shape; add `markSelfPickedUp`/`markSelfDelivered` to OrdersViewM…
- **Push-driven load() bypasses the in-flight mutation guard, stomping optimistic order-status updates**
  - `ios/seller/KosherEatsSeller/ViewModels/OrdersViewModel.swift:93 (load) vs 160/413 (mergeFresh)`
  - OrdersViewModel deliberately routes every BACKGROUND refresh of the order list through `mergeFresh(_:)`, whose own doc-comment (L405–412) states it exists to stop a stale `getOrders()` batch from stomping an accept/reject/prepare/ready mutation the seller is watching — it preserves any order in `inFlightOrderIDs`. The 15s poll loop (L160) and DashboardViewModel.fetchActiveOrders both call `mergeFr…
  - _Fix:_ Make `load()` use the same in-flight-preserving merge as the poll loop. Replace `self.orders = fresh` (L93) with `mergeFresh(fresh)`. mergeFresh already calls applyFilter(), so the following `applyFilter()` can be dropped. If callers need the full-replace semantics on a manual pull-to-refresh, gate …
- **Order detail shows courier card & escalate state from list copies that never carry courier/external_delivery_id**
  - `ios/seller/KosherEatsSeller/Views/Orders/SellerOrderDetailView.swift:130-140, 434-458, 547-631`
  - SellerOrderDetailView renders the courier card and the 'Dispatch to Uber' escalate gate off `order.courier` and `order.externalDeliveryId`, but the order it displays comes from the seller orders LIST (vm.orders / sharedOrdersVM), and the backend list query `ListSellerOrders` (backend/internal/handlers/orders.go ~L819-851) selects NEITHER the courier join NOR `external_delivery_id` — only the singl…
  - _Fix:_ Always fetch the authoritative full order when the detail screen appears, not only when order==nil. Change the `.task` to `await vm.fetchOrder(id: orderID)` unconditionally (keeping the seeded list copy only as an initial placeholder). To keep the full copy from being re-stomped, either (a) have the…

## MEDIUM (40)

- **Order history pagination is broken — older orders (#21+) are permanently unreachable**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/data/api/ApiService.kt:175-180 (getOrders); RestaurantRepository.kt 140-159; OrdersViewModel.kt 90-96`
  - The backend ListOrders handler (backend/internal/handlers/orders.go lines ~294-334) paginates purely by a `cursor` query param (the created_at of the last item, ORDER BY created_at DESC LIMIT n) and completely ignores `page`/`per_page`. The Android client never sends a cursor: ApiService.getOrders only declares @Query("page") and @Query("per_page"), RestaurantRepository.getOrders forwards only pag…
  - _Fix:_ Switch the client to cursor pagination to match the backend. Add a `@Query("cursor") cursor: String? = null` param to ApiService.getOrders, thread it through RestaurantRepository.getOrders, and in OrdersViewModel keep the created_at of the last loaded order (orders.last().createdAt) as the next curs…
- **Cart BOGO discount preview uses base item price, not per-unit price incl. modifiers**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/data/models/Models.kt:326-337 (specifically 334)`
  - Cart.computeDiscount() for a BOGO deal returns `items.minOfOrNull { it.menuItem.price }` — the BASE menu-item price, excluding any selected modifier deltas. Both the backend (deals.go resolveDealDiscount, `case "bogo"` uses `it.Price`, which OrderItem documents as 'Per-unit price in cents, inclusive of modifier deltas') and the iOS consumer (CartViewModel.swift `case .bogo: return cart.items.map(\…
  - _Fix:_ Change the BOGO branch to use each line item's full per-unit price including modifier deltas, matching backend/iOS. Add a per-unit helper to CartItem, e.g. `val unitPrice: Int get() = menuItem.price + selectedModifiers.sumOf { it.priceDelta }`, then in computeDiscount use `items.minOfOrNull { it.uni…
- **Order tracking has no handling for external (Uber Direct) deliveries — frozen map, no tracking link**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/data/models/Models.kt:373-409 (Order model); tracking screen android/.../ui/screens/tracking/OrderTrackingScreen.kt 214-270`
  - The backend Order JSON (models.go ~280) ships external dispatch fields `external_provider`, `external_tracking_url`, and `external_delivery_id` for orders fulfilled via Uber Direct, but the Android consumer `Order` data class decodes none of them. For an externally-dispatched order there is no internal courier, so OrderTrackingViewModel.launchLocationStream opens the /location/stream SSE that neve…
  - _Fix:_ Add the external-dispatch fields to the Order model: `@SerializedName("external_provider") val externalProvider: String? = null`, `@SerializedName("external_tracking_url") val externalTrackingUrl: String? = null`, `@SerializedName("external_delivery_id") val externalDeliveryId: String? = null`. In O…
- **Tracking screen shows "Delivering to: —" for pickup orders (pickup parity gap vs iOS)**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/screens/tracking/OrderTrackingScreen.kt:206, 473-504`
  - Active pickup orders route to OrderTrackingScreen (NavGraph onOrderClick: `if (order.status.isActive) navigate(OrderTracking...)`, and OrderConfirmation's Track button does the same). For pickup orders the backend returns `fulfillment_type == "pickup"`, an empty `delivery_address`, and `delivery_lat/lng == 0.0`. The Android AddressCard is hardcoded to the delivery case: it always renders a Home ic…
  - _Fix:_ Pass `order.fulfillmentType` (and a usable pickup location/name) into AddressCard and branch like iOS. Change `AddressCard(address = order.deliveryAddress)` to `AddressCard(order = order)` and inside: `val isPickup = order.fulfillmentType == "pickup"`; use `Icons.Filled.ShoppingBag` + label "Pickup …
- **Custom tip clamped to a flat $500 with no subtotal check hard-blocks checkout on small carts**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/viewmodels/CheckoutViewModel.kt:436-442 (currentTipCents Custom branch); 408-420 (updateCustomTip)`
  - The custom-tip branch of currentTipCents coerces the parsed tip only into 0..50000 cents (`cents.coerceIn(0, 50_000)`) with no check against the cart subtotal, and updateCustomTip performs no validation at all (no inline cap message, no subtotal guard). The backend rejects any order or PaymentIntent where tip > subtotal with HTTP 400 'tip cannot exceed subtotal' (backend/internal/handlers/payments…
  - _Fix:_ Cap the custom tip against the current subtotal and surface an inline message. In currentTipCents Custom branch use the subtotal already passed in: `cents.coerceIn(0, minOf(50_000, subtotalCents))`. In updateCustomTip, when the parsed amount exceeds min($500, current bundle subtotal), set errorMessa…
- **Android available-delivery model omits courier_tip entirely, so claims-feed payout and any tip-aware UI under-report on Android**
  - `android/courier/app/src/main/java/com/koshereats/courier/data/models/Models.kt:158-170`
  - The backend ListAvailableDeliveries/ListUpcomingDeliveries responses include `courier_tip` (the consumer tip, paid 100% to the courier), and the iOS AvailableDelivery model decodes it and shows the headline as `deliveryFee + courierTip` with an 'incl. tip' badge (ios DashboardView.swift line 308). The Android `AvailableDelivery` data class has NO courier_tip / courierTip field at all, so Gson sile…
  - _Fix:_ Add `@SerializedName("courier_tip") val courierTip: Int = 0` to the Android `AvailableDelivery` data class, and change the headline in DashboardScreen.kt's AvailableDeliveryCard to `(d.deliveryFee + d.courierTip).formatPrice()` with an 'incl. tip' affordance when courierTip > 0, matching iOS.
- **Percentage discount input strips the decimal point, turning '20.5' into '100' instead of rejecting it**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/deals/CreateDealScreen.kt:462-467`
  - The percentage onValueChange does `val digits = newVal.filter { c -> c.isDigit() }; if ((digits.toIntOrNull() ?: 0) > 100) "100" else digits`. The code comment says it intends to reject decimals, but filtering only digits collapses '20.5' into '205', which then trips the >100 clamp and becomes '100'. So a seller typing a decimal percentage silently gets 100% off (a free-order deal) rather than bei…
  - _Fix:_ Reject input containing a decimal point rather than stripping it: e.g. `if (newVal.any { it == '.' || it == ',' }) return@onValueChange` (keep the previous value), or clamp on the parsed integer only when the cleaned string contains no separator. Simplest: `val digits = newVal.filter { it.isDigit() …
- **KosherCertification.UNKNOWN sentinel is a selectable chip in restaurant-create picker**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/onboarding/CreateRestaurantScreen.kt:312`
  - The certification picker iterates `KosherCertification.entries.forEach { cert -> ... }`, which includes the `UNKNOWN` sentinel (displayName "Unknown"). That enum value exists only as a Moshi decode fallback (UnknownFallbackEnumAdapterFactory) and is NOT a real certification. If a seller taps the "Unknown" chip, `CreateRestaurantRequest.kosherCertification = KosherCertification.UNKNOWN` is serializ…
  - _Fix:_ Exclude the sentinel from the picker, e.g. `KosherCertification.entries.filter { it != KosherCertification.UNKNOWN }.forEach { cert -> ... }`. (Optionally also exclude it anywhere else `KosherCertification.entries` drives a user-facing selector.) This matches the iOS picker, which only offers real a…
- **Seller order detail never shows pickup vs delivery; labels any address as "Delivery Address"**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/orders/SellerOrderDetailScreen.kt:243-302`
  - The detail screen's status card shows only the status badge, "Placed X min ago", and (when non-blank) the order address rendered under a hardcoded "Delivery Address" header with a LocalShipping (truck) icon. There is no pickup/delivery indicator anywhere on this screen. iOS's SellerOrderDetailView.statusHeader (ios/seller/KosherEatsSeller/Views/Orders/SellerOrderDetailView.swift:183-194) shows an …
  - _Fix:_ Add a PICKUP/DELIVERY chip to the status card (mirroring ActiveOrderCard's existing chip and iOS statusHeader), keyed on order.isPickup. Gate the address block on !order.isPickup (or relabel it "Pickup at restaurant"/hide it for pickup), and swap the LocalShipping icon for a bag/storefront icon when…
- **Scheduled-order "Activates" time shown in UTC on the order-detail screen**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/orders/SellerOrderDetailScreen.kt:615-628`
  - In OrderActionButtons' SCHEDULED branch, the activation time is formatted with `java.time.ZonedDateTime.parse(scheduledFor).format(DateTimeFormatter.ofPattern("MMM d 'at' h:mm a"))` with NO conversion to the device's local zone. The backend sends `scheduled_for` as RFC3339 in UTC (orders.go:26 `ScheduledFor *time.Time ... RFC3339`, marshalled with a `Z` suffix), so ZonedDateTime.parse keeps the va…
  - _Fix:_ Convert to the local zone before formatting, matching ActiveOrderCard: `java.time.ZonedDateTime.parse(scheduledFor).withZoneSameInstant(java.time.ZoneId.systemDefault()).format(DateTimeFormatter.ofPattern("MMM d 'at' h:mm a"))` (keep the existing runCatching/getElse fallback).
- **Self-delivery "Mark Picked Up" button stays visible on a READY order already escalated to Uber, letting the seller corrupt an Uber-owned order's status**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/orders/SellerOrderDetailScreen.kt:722-739`
  - For a self-delivery restaurant (deliveryMode == "restaurant"), the READY branch shows "Mark Picked Up (self-delivery)" (-> viewModel.sellerPickupOrder) gated ONLY on `isPickup`/`isSelfDelivery`, not on whether the order has already been handed to an external courier. The same screen also shows the "Dispatch to Uber" escalate button at READY (canEscalate is true for a non-pickup READY order with no…
  - _Fix:_ Gate the self-delivery pickup/deliver affordance on the same one-way lock the escalate guard uses. Pass the order's courier/external state into OrderActionButtons (e.g. an `isExternallyDispatched = order.courier != null || !order.externalDeliveryId.isNullOrEmpty()` flag) and in the READY `else if (i…
- **Self-delivery 'Mark Picked Up' button shown when restaurant.deliveryMode is unknown/default mismatch can leave delivery orders with no actionable button at READY**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/orders/SellerOrderDetailScreen.kt:705-758`
  - The READY branch of OrderActionButtons decides between three states using `isSelfDelivery = deliveryMode == "restaurant"`, where deliveryMode comes from `authState.restaurant?.deliveryMode ?: "platform"`. If the AuthViewModel restaurant has not loaded yet, or its deliveryMode is anything other than the exact string "restaurant" (the Restaurant model also defaults this field to "platform" before th…
  - _Fix:_ Don't gate the self-delivery action on a possibly-stale/absent global restaurant string. Either (a) have the backend stamp the order's delivery_mode onto the Order payload and branch on `order.deliveryMode`, or (b) guard the READY/PICKED_UP branches so that when `authState.restaurant == null` (not y…
- **READY delivery order already dispatched to Uber/DoorDash shows misleading 'Awaiting Pickup…' instead of a partner-handoff state**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/orders/SellerOrderDetailScreen.kt:740-758`
  - In OrderActionButtons, a READY order that is delivery (not pickup) and not self-delivery falls into the `else` branch and renders a disabled 'Awaiting Pickup…' button. When that order has already been handed to an external provider (order.externalDeliveryId is non-null — e.g. the seller tapped Dispatch to Uber, or the backend auto-dispatched on Ready), the escalate button is correctly hidden (canE…
  - _Fix:_ In the READY non-pickup non-self-delivery branch, branch on dispatch state before the disabled button: if `order.externalDeliveryId` is non-empty (or a courier is attached), render an informational card matching iOS ('Handed to a delivery partner — a courier is on the way') rather than the disabled …
- **RestaurantSettings "Save Changes" reports success synchronously and never reflects per-field PUT failures**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/settings/RestaurantSettingsScreen.kt:733-743`
  - The Save handler sets isSaving=true, fires one fire-and-forget authViewModel.updateRestaurantField(key, value) per changed field (each launches its own coroutine in AuthViewModel.updateRestaurantField, lines 296-318), shows a "Saving changes…" toast, then immediately sets isSaving=false — all synchronously, before any of the network PUTs complete. As a result the button's spinner/disabled state ne…
  - _Fix:_ Make the save await its result: either add an AuthViewModel.updateRestaurantFields(changes: Map) that does one PUT and returns success/failure, or wrap the existing per-field calls in a coroutine that awaits all of them and only then sets isSaving=false and shows a success/failure toast. Keep isSavi…
- **UpdateCartItem silently drops the notes field**
  - `backend/internal/handlers/cart.go:262-280`
  - UpdateCartItemRequest decodes both `quantity` and `notes` (cart.go:21-24), but the UPDATE statement only writes quantity: `UPDATE cart_items SET quantity = $1 WHERE id = $2 ...`. The notes value the customer submits is parsed and then silently ignored — editing an existing cart line's special instructions (e.g. "no onions") appears to succeed (200 + full cart returned) but the note is never persis…
  - _Fix:_ Persist notes in the same UPDATE and re-validate its length: keep the existing `if len(req.Notes) > 500` guard used in AddToCart, then change the query to `UPDATE cart_items SET quantity = $1, notes = $2 WHERE id = $3 AND cart_id IN (SELECT id FROM carts WHERE user_id = $4)` and pass `req.Quantity, …
- **AddToCart merge silently discards the new notes on a quantity bump**
  - `backend/internal/handlers/cart.go:192-197`
  - AddToCart upserts on ON CONFLICT (cart_id, menu_item_id, selected_modifiers) with DO UPDATE SET quantity = cart_items.quantity + excluded.quantity. Because notes is not part of the conflict key and is not in the UPDATE SET list, adding the same item+modifiers a second time with a DIFFERENT notes value merges into the existing row and keeps the ORIGINAL notes, silently dropping the new request's no…
  - _Fix:_ Include notes in the upsert so the latest add wins (or branch by intent). Minimal fix: change the conflict clause to `DO UPDATE SET quantity = cart_items.quantity + excluded.quantity, notes = excluded.notes`. If preserving an existing non-empty note is preferred, use `notes = COALESCE(NULLIF(exclude…
- **ClaimOrder lets one courier claim unlimited concurrent orders; asymmetric with the auto-dispatch busy-courier guard**
  - `backend/internal/handlers/courier_orders.go:218-227`
  - The manual ClaimOrder CAS only checks `status='ready' AND courier_id IS NULL AND external_provider IS NULL` — it never checks whether the claiming courier already has an active delivery. The auto-dispatch path explicitly enforces single-tasking via `NOT EXISTS (SELECT 1 FROM orders o2 WHERE o2.courier_id = cp.user_id AND o2.status IN ('ready','picked_up'))` (dispatcher.go:499-503), so the two assi…
  - _Fix:_ Add the same busy guard to the manual claim so the two paths agree: `UPDATE orders SET courier_id=$1, claimed_at=NOW(), updated_at=NOW() WHERE id=$2 AND status='ready' AND courier_id IS NULL AND external_provider IS NULL AND NOT EXISTS (SELECT 1 FROM orders o2 WHERE o2.courier_id=$1 AND o2.status IN…
- **Suspended/rejected courier cannot toggle offline — stuck is_online=true forever**
  - `backend/internal/handlers/courier_orders.go:53-62`
  - SetCourierOnline's UPDATE is gated by `WHERE user_id = $4 AND onboarding_status = 'approved'` for BOTH the online AND the offline transition. The handler is reached by any user with role=courier (CourierMiddleware does not check onboarding_status). If a courier is moved to 'suspended' or 'rejected' (admin reject/suspend) while their profile has is_online=true, every subsequent attempt to set is_on…
  - _Fix:_ Only require approval when going ONLINE; always allow going offline. Split the predicate, e.g.: when req.Online is false run `UPDATE courier_profiles SET is_online=false, ... WHERE user_id=$1` (no status filter); when req.Online is true keep the `AND onboarding_status='approved'` guard. Equivalently…
- **ListSellerDeals and ListRestaurantDeals omit rows.Err(), silently truncating the deal list**
  - `backend/internal/handlers/deals.go:181-192`
  - ListSellerDeals (181-192), ListNearbyDeals (348-359) and ListRestaurantDeals (393-404) each iterate `for rows.Next()` with `continue` on scan error and return the accumulated slice without ever calling rows.Err(). A connection/decode error mid-stream is dropped: the endpoint returns HTTP 200 with a silently shortened list. For the seller this means deals they created can vanish from their manageme…
  - _Fix:_ Add `if err := rows.Err(); err != nil { writeError(w, http.StatusInternalServerError, "failed to list deals"); return }` immediately after each `for rows.Next()` loop in ListSellerDeals, ListNearbyDeals, and ListRestaurantDeals.
- **Deal-order idempotent replay returns 400 "deal already used" instead of the existing order**
  - `backend/internal/handlers/orders.go:161-241`
  - CreateOrder calls resolveDealDiscount (orders.go:161) BEFORE the ON CONFLICT (stripe_payment_id) DO NOTHING idempotent-replay path (orders.go:207-241). resolveDealDiscount enforces one-use-per-customer by checking for any existing order with the same applied_deal_id and status NOT IN ('rejected','cancelled') (deals.go:276-284). On a legitimate CreateOrder retry (client confirmed payment but lost t…
  - _Fix:_ Make the deal-usage check idempotent with respect to the same PaymentIntent. Either (a) exclude the order being replayed from the usage check by also matching on payment_intent: change the EXISTS query in resolveDealDiscount to `... AND stripe_payment_id <> $3` and pass req.PaymentIntentID, or (b) s…
- **loadOrderByID silently pushes item-less / truncated kitchen tickets to the POS**
  - `backend/internal/handlers/orders.go:997-1012`
  - loadOrderByID is the order loader used exclusively by the POS push path (pushAcceptedOrderToPOS -> PushOrderToPOS, which builds the Clover/Square kitchen-ticket payload from order.Items). It has two silent failure modes that corrupt the kitchen ticket: (1) On the order_items query error it does `return &o, nil` (line 1001), so a transient DB error returns an order with an EMPTY Items slice and NO …
  - _Fix:_ In loadOrderByID, treat the items query as load-critical instead of best-effort: on the `rows, err := ...Query(...)` error return `nil, err` (or at minimum log loudly) rather than `return &o, nil`. After the `for rows.Next()` loop, check `if err := rows.Err(); err != nil { return &o, err }` so a tru…
- **Marketplace 'order ready' courier broadcast advertises bare delivery_fee, omitting tip and platform bump**
  - `backend/internal/handlers/orders.go:1147-1149`
  - When a seller marks a platform-delivery order ready, MarkOrderReady broadcasts to all online couriers via `h.notify.OrderReady(context.Background(), orderID, restaurantName, deliveryFee)` — passing only the raw delivery_fee column as the advertised payout. But the actual amount a courier receives on delivery (DeliverOrder, courier_orders.go:321-322) is `delivery_fee + courier_tip + subtotal*25/100…
  - _Fix:_ Compute the advertised payout the same way DeliverOrder does before broadcasting. In MarkOrderReady, the SELECT already loads deliveryFee, subtotal, and courierTip; pass `deliveryFee + courierTip + subtotal*25/1000` to h.notify.OrderReady instead of bare deliveryFee, so the broadcast, the auto-assig…
- **AcceptOrder and MarkOrderPreparing fetch consumer push data with the already-dead request context, dropping the customer notification on client disconnect**
  - `backend/internal/handlers/orders.go:946 and 1018`
  - updateSellerOrderStatus writes the full HTTP response (writeJSON at line 1550) before returning true. AcceptOrder (line 946) and MarkOrderPreparing (line 1018) then call consumerAndRestaurantForOrder(r, orderID), which runs a DB query on r.Context(). Because the response has already been flushed, r.Context() is now subject to cancellation the instant the client closes the connection (mobile client…
  - _Fix:_ Use context.Background() (or a fresh detached context with a short timeout) for the consumerAndRestaurantForOrder lookup in AcceptOrder and MarkOrderPreparing, since the work is fire-and-forget and the response is already sent. Mirror the pattern already used in MarkOrderReady (orders.go:1106), whic…
- **CreateOrder idempotent retry of a deal order returns 400 "deal already used" instead of the existing order**
  - `backend/internal/handlers/orders.go:161-165 (resolveDealDiscount call) blocking the replay path at 226-241`
  - CreateOrder is explicitly designed to be idempotent on PaymentIntentID: when a client confirms payment but loses the response and retries, the INSERT's `ON CONFLICT (stripe_payment_id) DO NOTHING` returns no row (pgx.ErrNoRows) and the handler returns the already-created order with HTTP 200 (orders.go:226-241). This recovery path is broken whenever a deal was applied. On the retry, `resolveDealDis…
  - _Fix:_ Make the replay converge instead of 400ing. Cleanest: at the top of CreateOrder, before computing the discount, short-circuit on an already-existing order for this PaymentIntent — `if existing, err := h.loadOrderByPaymentIntent(r, req.PaymentIntentID, user["user_id"]); err == nil { writeJSON(w, http…
- **CreatePaymentIntent omits rows.Err() when summing the cart, risking an under-charge / wasted charge**
  - `backend/internal/handlers/payments.go:99-115`
  - CreatePaymentIntent computes the authoritative Stripe charge total from the cart by iterating cart_items rows (lines 101-111) but never calls rows.Err() after the loop, and the per-row Scan error path (line 103-106) is the only error handling. If the row stream is truncated by a connection error after some rows have been read, `subtotal` is silently undercounted and the PaymentIntent is created (a…
  - _Fix:_ After the `for rows.Next()` loop in CreatePaymentIntent, add `if err := rows.Err(); err != nil { rows.Close(); writeError(w, http.StatusInternalServerError, "failed to read cart"); return }` before using `subtotal`, so a truncated read aborts the PaymentIntent rather than charging the customer a wro…
- **StartPhoneLogin resets the OTP brute-force lockout, defeating attempt limiting**
  - `backend/internal/handlers/phone_auth.go:66-70`
  - OTP brute-force protection is DB-backed in phone_otp_starts (failed_attempts, locked_until), and VerifyPhoneLogin locks an account for 10 minutes after otpMaxAttempts (3) bad codes (lines 118-121, 129-136). However StartPhoneLogin's upsert uses `ON CONFLICT (phone) DO UPDATE SET started_at = NOW(), failed_attempts = 0, locked_until = NULL` (line 69). An attacker who gets locked out can simply call…
  - _Fix:_ Do not reset failed_attempts/locked_until on a new start when the phone is still inside an active lockout window. In the ON CONFLICT clause, preserve the lock: e.g. `ON CONFLICT (phone) DO UPDATE SET started_at = NOW(), failed_attempts = CASE WHEN phone_otp_starts.locked_until IS NOT NULL AND phone_…
- **Seller "today" dashboard stats bucket orders by UTC calendar day, not the restaurant's local day**
  - `backend/internal/handlers/seller.go:1027, 1035, 1042, 1052`
  - GetDashboardStats computes today_orders, today_revenue, today_delivery_earnings and avg_prep_time with `o.created_at::date = CURRENT_DATE`. created_at is TIMESTAMPTZ, and `::date` / CURRENT_DATE are evaluated in the Postgres SESSION time zone. The pgx connection (internal/database/database.go) sets no time zone, no `TimeZone` runtime param is passed, and there is no TZ env or per-restaurant timezo…
  - _Fix:_ Anchor the day boundary to the business's local zone instead of the session zone. Simplest correct fix: convert before truncating, e.g. replace `o.created_at::date = CURRENT_DATE` with `(o.created_at AT TIME ZONE 'America/New_York')::date = (NOW() AT TIME ZONE 'America/New_York')::date` at all four …
- **Clover POS order push sends full-price line items but a discounted+fees 'total', misrepresenting restaurant-owed money**
  - `backend/internal/pos/clover/clover.go:89-106 (orderPayload); fed by loadOrderByID in orders.go 978-1012`
  - orderPayload builds Clover lineItems from it.Price (the modifier-adjusted, FULL per-unit price — no deal discount applied) and separately sets top-level 'total': order.Total. order.Total is subtotal - discount + delivery_fee + service_fee + tax + tip. So the line items sum to the undiscounted subtotal while 'total' is inflated by delivery fee, tax, and tip (money the restaurant does not collect — …
  - _Fix:_ Send Clover only the food money the restaurant is actually owed, or send a fully consistent breakdown. Simplest: drop the inflated top-level 'total' (let Clover compute from line items) or set it to the discounted item subtotal (order.Subtotal - order.Discount), not order.Total. If a true total is w…
- **History pagination cursor truncates to milliseconds and can permanently skip orders**
  - `ios/consumer/KosherEatsConsumer/Services/APIService.swift:671-682 (listOrders cursor) and 30-34 (iso8601Fractional)`
  - listOrders(cursor:) serializes the keyset cursor with ISO8601DateFormatter configured with .withFractionalSeconds, which emits only millisecond (3-digit) precision. The backend (orders.go ~L311) parses it with time.RFC3339Nano and filters WHERE o.created_at < $2 against Postgres timestamptz columns that carry microsecond precision. When orders.last.createdAt is e.g. 2026-06-25T12:00:00.123456Z, th…
  - _Fix:_ Stop round-tripping the timestamp as a lossy formatted string. Either (a) have the backend return an opaque next-cursor token (e.g. created_at as RFC3339Nano plus id) and send that verbatim, or (b) page by the order id of orders.last in addition to created_at (keyset on (created_at, id)). At minimum…
- **Cart 'Estimated Total' shows discounted subtotal only, omitting delivery/service/tax that are already known to apply**
  - `ios/consumer/KosherEatsConsumer/Views/Cart/CartView.swift:134-148, 165-169`
  - orderSummary lists 'Delivery Fee' and 'Service Fee' as 'Calculated at checkout' and then renders 'Estimated Total' (and the Checkout button price) as Money.dollars(cartVM.discountedSubtotal), i.e. subtotal - deal discount, with NO delivery/service/tax component. Because delivery + service + tax are mandatory on every delivery order, the 'Estimated Total' and the dollar amount on the Checkout butto…
  - _Fix:_ Either relabel the figure unambiguously (e.g. 'Items subtotal' instead of 'Estimated Total', and show 'Plus delivery, fees & tax' beneath it) so the cart never presents a number that reads as the order total, or fetch the real bundle totals (createPaymentSheet) before showing a total here. At minimu…
- **Pickup orders show courier/"on the way" delivery copy throughout OrderTrackingView**
  - `ios/consumer/KosherEatsConsumer/Views/Orders/OrderTrackingView.swift:240-301 (statusHeader ETA label, phaseText, phaseSubtext) + 422-428 (timeline step)`
  - OrderTrackingView is reachable for pickup orders: OrderDetailView shows "Track your order" for any active non-pending order regardless of fulfillment_type, and OrderConfirmationView's onTrack routes there for pickup too. Only addressCard(for:) branches on order.fulfillmentType == "pickup"; the rest of the screen assumes delivery. A pickup customer sees: phaseText(.ready) = "Waiting for a courier",…
  - _Fix:_ Thread isPickup (order.fulfillmentType == "pickup") into phaseText/phaseSubtext and the timeline. For pickup return ready="Ready for pickup" / "Your order is ready to collect at the restaurant.", pickedUp="Picked up" / "You've collected your order."; change the statusHeader label and accessibilityVa…
- **Tracking ETA lacks the epoch/garbage-time guard used in OrderConfirmationView**
  - `ios/consumer/KosherEatsConsumer/Views/Orders/OrderTrackingView.swift:240-246 (statusHeader ETA)`
  - statusHeader formats `"ETA: \(order.estDeliveryTime.formatted(date: .omitted, time: .shortened))"` whenever the order is active and not pending, with no sanity check on estDeliveryTime. OrderConfirmationView.etaText (OrderConfirmationView.swift:327-339) deliberately guards against an epoch-zero / unreasonable est_delivery_time (timeIntervalSince1970 > 1 and within the last 24h) and falls back to "…
  - _Fix:_ Apply the same guard as OrderConfirmationView.etaText: only show the concrete time when estDeliveryTime.timeIntervalSince1970 > 1 and the time is within a sane window (e.g. > now - 24h); otherwise show "Updating ETA…" / "Ready soon". Factor the guard into a shared helper so both screens stay consist…
- **Courier history/earnings 'today' total can show stale/zero on iOS dashboard while deliveries exist (loadTodayEarnings failure not surfaced as count)**
  - `ios/courier/KosherEatsCourier/ViewModels/DashboardViewModel.swift:189-204`
  - loadTodayEarnings() fetches history and sets dashVM.todayEarnings; on any thrown error it sets errorMessage but leaves todayEarnings at its previous value (0 on first load). EarningsView shows 'Today's earnings' from dashVM.todayEarnings (line 32/51) but computes the delivery COUNT independently from its own locally-loaded `history` (lines 33-41). These two loads are independent network calls with…
  - _Fix:_ Derive iOS 'today total' from the same `history` array that EarningsView already loads (filter isDateInToday and sum courierPayout) instead of a separate loadTodayEarnings() fetch, mirroring Android. If the separate dashboard pill is kept, compute both the pill amount and any count from one fetched …
- **iOS courier deliver: lost response on first attempt leaves a delivered order stuck in the active list and shows a spurious error**
  - `ios/courier/KosherEatsCourier/ViewModels/DashboardViewModel.swift:deliver(_:proofURL:) ~lines 209-225 and withRetry ~lines 150-170`
  - deliver() wraps the network call in withRetry, which retries up to 3x on transient (non-4xx) errors. If the first DeliverOrder request reaches the backend and commits (status -> 'delivered', payout enqueued) but the HTTP response is dropped (network blip), withRetry retries. The retry's backend UPDATE has `WHERE ... status='picked_up'`, which now matches 0 rows because status is already 'delivered…
  - _Fix:_ On a 400 from the deliver call, re-fetch the order/active list and treat 'already delivered' as success: in the catch, if the error is httpError(400, _), call `active.removeAll { $0.id == order.id }`, update locationManager?.hasActiveDelivery, and `await refresh()` / `await loadTodayEarnings()` inst…
- **iOS courier sees spurious 'order no longer available' error when a claim succeeds but the response is lost**
  - `ios/courier/KosherEatsCourier/ViewModels/DashboardViewModel.swift:131-141 (claim) / 157-175 (withRetry)`
  - claim() wraps the non-idempotent claim call in withRetry, which retries on any non-4xx error (including APIError.networkError from a dropped connection). The backend ClaimOrder (courier_orders.go line 218-227) is a CAS UPDATE guarded by `courier_id IS NULL`. If the first claim request reaches the server and sets courier_id=<this courier> but the HTTP response is lost in transit, URLSession throws …
  - _Fix:_ Do not blindly retry the non-idempotent claim on transient errors, or treat a post-retry 409 as a likely-success. Simplest: drop withRetry for claim and instead, on a network failure, call refresh() and check whether the order now appears in active/owned-by-me before surfacing an error. Alternativel…
- **Courier-facing available-delivery payout in-app cards understate actual payout by the 2.5% platform bump**
  - `ios/courier/KosherEatsCourier/Views/Dashboard/DashboardView.swift:308 (AvailableDeliveryCard) / 368 (UpcomingDeliveryCard); android DashboardScreen.kt:177`
  - The available-delivery cards advertise payout as delivery_fee + courier_tip (iOS AvailableDeliveryCard line 308: `Double(delivery.deliveryFee + delivery.courierTip)`; Android AvailableDeliveryCard DashboardScreen.kt line 177 shows only delivery_fee, missing tip entirely — the tip omission is separately tracked). But the amount the courier is actually credited at delivery time, computed in DeliverO…
  - _Fix:_ Include the platform bump in the displayed payout so the card matches DeliverOrder's computation. The backend ListAvailableDeliveries already SELECTs subtotal (courier_orders.go line 86), so either (a) add a computed `courier_payout_estimate` field = delivery_fee + courier_tip + subtotal*25/1000 to …
- **Orders sorted by raw RFC3339 string instead of parsed Date — mis-orders newest-first list**
  - `ios/seller/KosherEatsSeller/ViewModels/OrdersViewModel.swift:277-283 (also DashboardViewModel.swift:127)`
  - OrdersViewModel.applyFilter() sorts every filter ('Active'/'Completed'/'All') with `$0.createdAt > $1.createdAt`, a lexicographic comparison on the raw timestamp STRING. DashboardViewModel.fetchActiveOrders() does the same (`.sorted { $0.createdAt > $1.createdAt }`). The backend serializes Order.CreatedAt as a plain Go time.Time, which marshals as RFC3339Nano and STRIPS trailing zero fractional di…
  - _Fix:_ Sort on the parsed Date, falling back so nil dates don't reorder unpredictably. In OrdersViewModel.applyFilter() replace the three `.sorted { $0.createdAt > $1.createdAt }` with `.sorted { ($0.createdAtDate ?? .distantPast) > ($1.createdAtDate ?? .distantPast) }`, and apply the same change in Dashbo…
- **Restaurant switch resets knownPendingIDs to empty without re-seeding, false-pinging the new-order alert for every existing pending ticket**
  - `ios/seller/KosherEatsSeller/ViewModels/OrdersViewModel.swift:206-220 (subscribeToRestaurantChanges) vs 129-131 (seed)`
  - loadAndAutoRefresh seeds `knownPendingIDs` from the current pending orders BEFORE entering the poll loop (L129–131) specifically so existing pending tickets don't trigger the new-order chime+haptic on the first tick. But the restaurant-switch Combine sink (subscribeToRestaurantChanges, L206–220) sets `knownPendingIDs = []` and calls `load()` WITHOUT re-seeding from the freshly loaded orders. The n…
  - _Fix:_ In the subscribeToRestaurantChanges sink, after `await self.load()`, re-seed: `self.knownPendingIDs = Set(self.orders.filter { $0.status == .pending }.map(\.id))`. This mirrors the first-run seeding in loadAndAutoRefresh and makes the new-order alert fire only for orders that genuinely arrive after …
- **Phone OTP never auto-submits on iOS SMS autofill (gated on max length, not actual code length)**
  - `ios/seller/KosherEatsSeller/Views/Auth/PhoneOTPView.swift:59-66`
  - The auto-submit branch fires only when `code.count == maxCodeLength` (10). Twilio Verify codes are 6 digits by default, and iOS one-time-code autofill (`.textContentType(.oneTimeCode)`) inserts exactly the real code length (6), which never equals 10. So the intended "auto-submit on autofill" behavior — the comment explicitly says "e.g. iOS autofill from the SMS" — never triggers for the normal 6-d…
  - _Fix:_ Auto-submit when the entered code reaches a plausible complete length rather than the hard max. Simplest correct fix: trigger submit once `code.count >= minCodeLength` AND the field just received a multi-character paste/autofill, or track the expected length. A pragmatic fix matching the 6-digit Twi…
- **Pickup orders show a 'Delivery' section with the customer's address as a delivery destination**
  - `ios/seller/KosherEatsSeller/Views/Orders/SellerOrderDetailView.swift:39, 261-291, 349`
  - deliverySection(order) is rendered unconditionally in the body (line 39). It always shows a section header 'Delivery' with a location.fill icon, a mappin.circle.fill, and order.deliveryAddress presented as a delivery destination, plus 'ETA: …'. For a pickup order (order.isPickup) there is no delivery — and because pickup orders record the consumer's home address as delivery_address (see the known …
  - _Fix:_ Branch deliverySection on order.isPickup: for pickup, retitle the section 'Pickup' with a bag.fill icon and copy like 'Customer collects at the counter' (omit the customer's home address / map pin and the ETA-as-delivery framing). In priceSection, hide the 'Delivery Fee' row when order.isPickup (or …

## LOW (23)

- **Checkout totals show a $0.00 "Delivery fee" row on pickup orders (iOS hides it)**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/screens/checkout/CheckoutScreen.kt:617`
  - In CheckoutScreen.TotalsCard the "Delivery fee" row is rendered unconditionally: `TotalRow("Delivery fee", bundle.deliveryFee)`. For a pickup order the backend returns deliveryFee = 0 (refreshBundle sends fulfillmentType="pickup" and an empty deliveryAddress), so the user sees a confusing "Delivery fee  $0.00" line on the final payment screen. iOS deliberately guards this: ios/consumer/koshereatsc…
  - _Fix:_ Match iOS by only rendering the delivery-fee row when it is non-zero. Replace the unconditional `Spacer(Modifier.height(8.dp)); TotalRow("Delivery fee", bundle.deliveryFee)` block with:  if (bundle.deliveryFee > 0) {     Spacer(Modifier.height(8.dp))     TotalRow("Delivery fee", bundle.deliveryFee) …
- **Delivery-fee / ETA quote is fetched on every address change but never displayed**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/viewmodels/CheckoutViewModel.kt:71-73, 317-345`
  - CheckoutUiState.deliveryQuoteCents and deliveryQuoteMinutes are documented as 'Quick delivery-fee preview from /delivery-quote — shown while bundle is loading,' and fetchDeliveryQuote() POSTs to /delivery-quote/ on selectAddress()/addAddress() and writes the result into state. But a grep shows these two fields are never read by any composable (CheckoutScreen renders only bundle.deliveryFee in Tota…
  - _Fix:_ Either (a) render the preview: in CheckoutScreen, while ui.isLoadingBundle && ui.bundle == null && ui.fulfillmentType == "delivery", show a lightweight row using ui.deliveryQuoteCents/deliveryQuoteMinutes (e.g. 'Est. delivery fee ~$X.XX · NN min'); or (b) if no preview is wanted, delete the delivery…
- **SSE courier-location events are not range-validated before moving the map pin (iOS validates)**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/viewmodels/OrderTrackingViewModel.kt:248-258 (handleLocationEvent)`
  - handleLocationEvent only rejects the exact null-island coordinate (lat==0.0 && lng==0.0) before splicing event.lat/lng into the courier and re-centering the map. The iOS OrderTrackingViewModel guards the same stream with full range checks (`event.lat >= -90 && event.lat <= 90, event.lng >= -180 && event.lng <= 180, event.lat != 0 && event.lng != 0` — see ios/consumer/.../OrderTrackingViewModel.swi…
  - _Fix:_ Match iOS: in handleLocationEvent, after parsing, reject out-of-range values before applying — `if (event.lat !in -90.0..90.0 || event.lng !in -180.0..180.0 || (event.lat == 0.0 && event.lng == 0.0)) return@withContext`. This keeps obviously-bad coordinates from ever reaching the courier object or t…
- **Order card item count shows distinct-line count, not total quantity (iOS parity break)**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/dashboard/ActiveOrderCard.kt:152-159`
  - ActiveOrderCard (used by BOTH the Dashboard and the Orders tab on Android) builds its items summary from `order.items.size`, which is the number of distinct line items, not the number of units ordered. The iOS seller app shows `order.itemCount` everywhere (Order.itemCount = items.reduce { $0 + $1.quantity } — the sum of quantities). See ios/seller/.../Views/Dashboard/ActiveOrderCard.swift:69 `Labe…
  - _Fix:_ Add a computed `val itemCount: Int get() = items.sumOf { it.quantity }` to the Android `Order` model (Models.kt), then change ActiveOrderCard line 153 to use it for the count prefix, e.g. `text = "${order.itemCount} item${if (order.itemCount != 1) "s" else ""} " + order.items.take(2).joinToString(",…
- **Fixed-amount deal has no client-side $100 cap (parity gap with iOS) → server 400 instead of inline error**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/deals/CreateDealScreen.kt:458-489, 626-630`
  - For a FIXED-amount deal, the discount-value field uses `filterDollarCents` with no upper bound, and `canCreate`/`hasDiscountValue` only require the value to be > 0. On submit, `discountValueCents = dollarsToCents(discountValue).coerceAtLeast(1)` with no cap. The backend rejects fixed discounts over 10000 cents ($100) with HTTP 400 (deals.go:77 `if req.DiscountValue > 10000`). So a seller entering …
  - _Fix:_ In the FIXED branch of the discountValueCents computation (and/or in the validationHint/canCreate gate), reject values over 10000 cents with an inline hint mirroring iOS, e.g. add to `validationHint`: `discountType == DiscountType.FIXED && dollarsToCents(discountValue) > 10000 -> "Fixed discount can…
- **Deal 'time left' badge uses truncating integer division, mislabeling expiry**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/deals/DealsScreen.kt:211-214`
  - The active-deal countdown computes `val hours = ChronoUnit.HOURS.between(now, parsedExpiry)` then renders `if (hours < 24) "${hours}h left" else "${hours / 24}d left"`. ChronoUnit.HOURS.between truncates toward zero, and the day branch divides again, so the label systematically under-reports remaining time and drops the partial unit: a deal expiring in 47.9h shows "1d left" (loses almost a full da…
  - _Fix:_ Round up the partial unit and special-case sub-hour. e.g. compute `val minutes = ChronoUnit.MINUTES.between(now, parsedExpiry)`; then `when { minutes < 60 -> "${minutes}m left"; minutes < 24*60 -> "${(minutes + 59) / 60}h left"; else -> "${(minutes + 24*60 - 1) / (24*60)}d left" }`. This rounds rema…
- **FCM status-change push silently lost for orders not on the current page (no list re-fetch)**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/viewmodels/OrdersViewModel.kt:193-218`
  - refreshSingleOrder (the FCM path taken for every non-"new_order" event with an orderId — e.g. courier_assigned, order_picked_up, order_delivered) updates the list purely in place: `s.orders.map { if (it.id == orderId) updated else it }`. If the pushed order is not currently in `s.orders` (it aged off the newest-20 page-1 window, or the seller is on a status filter that excludes it), the freshly-fe…
  - _Fix:_ In refreshSingleOrder, when the fetched `updated` order's id is absent from the current list (and not filtered out), either insert it at the correct sorted position or fall back to a silent page-1 poll. Minimal fix: if `s.orders.none { it.id == orderId }`, call `pollSilently()` instead of dropping t…
- **Admin platform "today" orders/revenue use UTC calendar day**
  - `backend/internal/handlers/admin.go:450-451`
  - AdminStats computes TodayOrders (`SELECT COUNT(*) FROM orders WHERE created_at::date = CURRENT_DATE`) and TodayRevenue (`SELECT COALESCE(SUM(total),0) FROM orders WHERE created_at::date = CURRENT_DATE`) with the same UTC-session `created_at::date = CURRENT_DATE` pattern as the seller dashboard. Because no session time zone is configured (database.go passes no TimeZone, no TZ env, TIMESTAMPTZ colum…
  - _Fix:_ Replace both `created_at::date = CURRENT_DATE` expressions with a local-zone comparison, e.g. `(created_at AT TIME ZONE 'America/New_York')::date = (NOW() AT TIME ZONE 'America/New_York')::date`, matching the fix applied in seller.go GetDashboardStats so admin and seller figures stay consistent.
- **VerifyCourierPhone marks phone_verified without binding the OTP'd phone to the courier's account**
  - `backend/internal/handlers/courier_auth.go:253-326 (VerifyCourierPhone)`
  - VerifyCourierPhone verifies an SMS OTP for the client-supplied req.Phone and then sets courier_profiles.phone_verified=true for the authenticated user, but never checks that req.Phone matches the user's own users.phone (or links the verified number to the account). A courier can therefore satisfy the 'phone verified' onboarding gate using any phone number whose OTP they can pass (e.g. a number the…
  - _Fix:_ Require the OTP'd phone to match the authenticated user's registered phone before flipping phone_verified: fetch users.phone for user_id and reject with 400 when normalizePhone(req.Phone) != normalizePhone(users.phone), or alternatively persist the verified number onto the user/courier record (and u…
- **DeliverOrder reloads consumer with the request context after the response context is effectively done, before firing the delivery push**
  - `backend/internal/handlers/courier_orders.go:391-399`
  - After committing the delivery transaction and (for Temporal) spawning the background payout start, DeliverOrder fetches the consumer id with h.db.Pool.QueryRow(r.Context(), ...) at line 392 and then fires `go h.notify.OrderDelivered(context.Background(), ...)`. The QueryRow still uses the request-scoped r.Context(). If the courier's client has already disconnected (common right after a successful …
  - _Fix:_ Use a fresh detached context for the post-commit consumer lookup that drives the notification, matching MarkOrderReady's pattern. Replace `h.db.Pool.QueryRow(r.Context(), ...)` at courier_orders.go:392 (and orders.go:1427 in SellerDeliverOrder) with a context.Background() (optionally wrapped in a sh…
- **Externally-dispatched orders still appear in the courier available-deliveries feed (claimable-looking after external dispatch)**
  - `backend/internal/handlers/courier_orders.go:85-95`
  - ListAvailableDeliveries selects orders WHERE status='ready' AND courier_id IS NULL AND fulfillment_type='delivery' but does NOT filter on external_provider IS NULL. When a seller escalates an order to Uber/DoorDash (EscalateToUber) or the scheduler auto-dispatches externally, dispatch.Dispatch sets external_provider ('dispatching' then a real provider) and external_delivery_id while leaving status…
  - _Fix:_ Add `AND o.external_provider IS NULL` to the WHERE clause of both ListAvailableDeliveries (line ~92) and ListUpcomingDeliveries (line ~160), mirroring the guard already in ClaimOrder and tryAutoAssign so externally-dispatched orders never surface as claimable in the courier feed.
- **ListCourierHistory drains rows without checking rows.Err(), can silently drop delivered/earned orders**
  - `backend/internal/handlers/courier_orders.go:554-577`
  - ListCourierHistory powers the courier earnings screen (delivered orders with courier_payout). The `for rows.Next()` loop (line 554) appends each scanned HistoryRow but there is no `rows.Err()` check after the loop (it returns the list at line 577 unconditionally). If the row stream errors partway through (the pgx error is only visible via rows.Err(), not rows.Next()), the courier silently sees a t…
  - _Fix:_ After the `for rows.Next()` loop, before `if list == nil`, add: `if err := rows.Err(); err != nil { writeError(w, http.StatusInternalServerError, "failed to fetch history"); return }` so a partial result surfaces as an error the client can retry rather than a silently incomplete earnings list.
- **ListAvailableDeliveries drains rows without checking rows.Err() — a delivery can silently vanish from the courier feed**
  - `backend/internal/handlers/courier_orders.go:108-124`
  - ListAvailableDeliveries loops `for rows.Next()` with `continue` on scan error and then returns the list WITHOUT calling rows.Err(). pgx surfaces a mid-stream read error (connection reset, decode failure) via rows.Err() after Next() returns false — without that check the handler returns HTTP 200 with a truncated list and no error. A 'ready', unclaimed delivery order that should be claimable can sil…
  - _Fix:_ After the `for rows.Next()` loop in both ListAvailableDeliveries and ListUpcomingDeliveries, add `if err := rows.Err(); err != nil { writeError(w, http.StatusInternalServerError, "failed to fetch available deliveries"); return }` so a partial read is reported as a 500 (which the client retries) inst…
- **Public restaurant endpoints expose owner_id (internal user UUID) to anonymous callers**
  - `backend/internal/handlers/restaurants.go:60-104`
  - ListRestaurants, GetRestaurant, and SearchRestaurants run through scanRestaurants (line 60-76), which selects and serializes `owner_id` into the public models.Restaurant JSON. These routes are mounted under /api/v1/restaurants with OptionalAuthMiddleware (cmd/api/main.go:213-222), i.e. reachable by fully anonymous clients. owner_id is the seller's internal users.id UUID; leaking it to the public c…
  - _Fix:_ Add `json:"-"` to the OwnerID field on the public restaurant DTO, or split the seller/admin restaurant model (which legitimately needs owner_id) from the public one and stop selecting owner_id in scanRestaurants for the public ListRestaurants/GetRestaurant/SearchRestaurants paths.
- **ListMenuImports and ListChatMessages drain rows without rows.Err()**
  - `backend/internal/handlers/seller.go:957-966`
  - ListMenuImports (seller.go 957-966) loops `for rows.Next()` calling scanMenuImport and returns the jobs slice with no rows.Err() check; a mid-stream error yields a 200 with a truncated import-job list, so a seller polling import progress can see a job spuriously disappear or never observe its terminal status. ListChatMessages (chat.go 116-125) has the same omission: it accumulates messages with `c…
  - _Fix:_ In ListMenuImports add `if err := rows.Err(); err != nil { writeError(w, http.StatusInternalServerError, "failed to read imports"); return }` after the loop. In ListChatMessages add `if err := rows.Err(); err != nil { writeError(w, http.StatusInternalServerError, "failed to fetch messages"); return …
- **DeleteAccount discards errors from every intermediate tx.Exec, masking partial-deletion failures**
  - `backend/internal/handlers/user.go:242-259`
  - DeleteAccount runs ~11 tx.Exec statements (device_tokens, cart_items, carts, addresses, orders anonymize, restaurants deactivate, courier anonymize, courier_locations, courier_profiles, chat_messages, courier_ratings) with their return values fully ignored — only the final `DELETE FROM users` checks err. Under pgx, the first failing statement aborts the whole transaction, after which every subsequ…
  - _Fix:_ Check and short-circuit on each Exec error, or at least capture and log the first failure. Wrap each statement like `if _, err := tx.Exec(ctx, ...); err != nil { writeError(w, http.StatusInternalServerError, "failed to delete account"); return }`, or accumulate into a helper that returns on first er…
- **Courier payout push notifications understate the actual amount paid (omit the 2.5% platform bump)**
  - `backend/internal/notify/notifier.go:179-196 (OrderReady, CourierAutoAssigned); driven by backend/internal/scheduler/dispatcher.go:435 and backend/internal/handlers/orders.go:1148`
  - The payout figure shown to couriers in the 'New delivery available' (OrderReady) and 'New delivery assigned' (CourierAutoAssigned) pushes is computed as delivery_fee + courier_tip only. OrderReady is called in MarkOrderReady (orders.go:1148) with the raw o.delivery_fee, and sweepAutoDispatch (dispatcher.go:435) computes payout = o.delivery_fee + COALESCE(o.courier_tip,0) and passes it to CourierAu…
  - _Fix:_ Compute the courier-facing payout the same way DeliverOrder does so the preview matches the booked payout. Factor the payout formula into one helper (e.g. courierPayout(deliveryFee, tip, subtotal int) int { return deliveryFee + tip + subtotal*25/1000 }) and use it in three places: (1) sweepAutoDispa…
- **Auto-dispatch courier push advertises a payout missing the 2.5% platform bump it will actually be paid**
  - `backend/internal/scheduler/dispatcher.go:435 and 548`
  - sweepAutoDispatch computes the payout it shows the courier as `o.delivery_fee + COALESCE(o.courier_tip,0)` (SQL alias `payout`, line 435) and passes it to `d.notify.CourierAutoAssigned(ctx, orderID, courierID, restaurantName, o.payout)` (line 548). But the amount the courier is actually paid at delivery, computed in DeliverOrder (courier_orders.go line 321-322), is `delivery_fee + tip + platformBu…
  - _Fix:_ Compute the advertised payout the same way as DeliverOrder so the notification matches what is booked: change the SELECT to `o.delivery_fee + COALESCE(o.courier_tip,0) + (o.subtotal * 25 / 1000) AS payout` (or compute platformBump in Go from o.subtotal and add it before calling CourierAutoAssigned).…
- **Idle SSE location stream kept open indefinitely for orders that will never emit a courier position**
  - `ios/consumer/KosherEatsConsumer/ViewModels/OrderTrackingViewModel.swift:121-192 (startLocationStream / reconnect loop)`
  - startLocationStream() unconditionally opens (and on clean server close, reconnects every 1s — lines 173-179) the /orders/:id/location/stream SSE connection for the entire time the tracking screen is visible, for ANY active order. For external (Uber/DoorDash) deliveries and for pickup orders there is never a platform courier feeding the broker, so the stream produces only 25s heartbeats and the 1s …
  - _Fix:_ Gate startLocationStream() on the order actually being a platform-courier delivery: only start it when order.fulfillmentType != 'pickup' AND externalTrackingURL == nil (after adding that field). For pickup/external orders rely solely on the existing status poll + push refresh path. Optionally also s…
- **OrderConfirmationView and OrderDetailView render a "Delivery fee" row on pickup orders**
  - `ios/consumer/KosherEatsConsumer/Views/Orders/OrderConfirmationView.swift:266 (priceCard) and OrderDetailView.swift:347 (priceBreakdown)`
  - Both post-order price breakdowns call priceRow("Delivery fee", order.deliveryFeeFormatted) / SummaryRow(label: "Delivery Fee", ...) unconditionally. For pickup orders deliveryFee is 0, so the customer sees a literal "Delivery fee  $0.00" line on an order that was never a delivery. This is inconsistent with the checkout TotalsCard (CheckoutView.swift:602) which already guards `if bundle.deliveryFee…
  - _Fix:_ Gate the delivery-fee row on a non-pickup / non-zero condition in both views, e.g. `if order.fulfillmentType != "pickup" && order.deliveryFee > 0 { priceRow("Delivery fee", order.deliveryFeeFormatted) }`, matching the TotalsCard guard. Optionally relabel the address/eta copy that already branches on…
- **DiscountType enum lacks an unknown/fallback case, so a new server discount type breaks the entire deals list decode**
  - `ios/seller/KosherEatsSeller/Models/Models.swift:724-738 (Deal decode 740-778)`
  - DiscountType only declares .percentage/.fixed/.bogo and uses the default synthesized Codable conformance — no custom init(from:) and no .unknown fallback. By contrast OrderStatus (93-112) and KosherCertification (62-75) both add a custom decoder that maps unrecognized raw values to a safe fallback specifically to avoid one bad value failing a whole list. The backend Deal.DiscountType is a free str…
  - _Fix:_ Add a `case unknown` to DiscountType and a custom `init(from decoder:)` that does `self = DiscountType(rawValue: raw) ?? .unknown`, matching the pattern already used by OrderStatus and KosherCertification. Give .unknown a sensible displayName ('Discount') and treat it as non-editable / display-only …
- **Raw ISO 8601 timestamp shown to seller as the delivery ETA**
  - `ios/seller/KosherEatsSeller/Views/Orders/SellerOrderDetailView.swift:275-283`
  - deliverySection renders `if let eta = order.estDeliveryTime { Text("ETA: \(eta)") }`. `order.estDeliveryTime` is decoded as a raw String (Order.swift decodes `est_delivery_time` via decodeIfPresent(String.self)). The backend column defaults to `NOW() + INTERVAL '45 minutes'` (migration 001_initial.sql:150) and is a non-nullable time.Time with no omitempty, so it is essentially always present and e…
  - _Fix:_ Add a parsed/formatted computed property on Order, e.g. `var estDeliveryTimeFormatted: String? { guard let s = estDeliveryTime else { return nil }; let d = Self.iso8601Frac.date(from: s) ?? Self.iso8601Plain.date(from: s); return d.map { DateFormatter.localizedString(from: $0, dateStyle: .none, time…
- **Escalate-to-Uber button shown on accepted/preparing orders that are already dispatched or courier-claimed**
  - `ios/seller/KosherEatsSeller/Views/Orders/SellerOrderDetailView.swift:400-432`
  - In actionButtons, the `.accepted` and `.preparing` branches render `escalateButton(order)` gated only on `!order.isPickup`, unlike the `.ready` branch (L452-456) which correctly also requires `order.courier == nil && (order.externalDeliveryId ?? "").isEmpty`. The backend EscalateToUber handler (backend/internal/handlers/orders.go L1331-1342) only accepts orders with `courier_id IS NULL AND externa…
  - _Fix:_ Apply the same guard the `.ready` branch uses to the `.accepted` and `.preparing` branches: only show `escalateButton(order)` when `!order.isPickup && order.courier == nil && (order.externalDeliveryId ?? "").isEmpty`. Best extracted into a computed helper like `private func canEscalate(_ order: Orde…
