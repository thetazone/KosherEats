# Bug-hunt backlog — cycles 2-3 (2026-06-25)

Second adversarial sweep (cycle-1's 102 excluded). 136 confirmed: 1 critical, 45 high, 48 medium, 42 low. Same caveat: single-skeptic verify, re-verify before fixing. ✅ = fixed this session.

## CRITICAL (1)

- ✅ **Phone-account synthetic password is fully derivable from the victim's phone number → password-login account takeover (OTP bypass)**
  - `backend/internal/handlers/phone_auth.go:234-273 (createPhoneUser); exploited via Login at backend/internal/handlers/auth.go:214-245`
  - When a user signs in for the first time with phone OTP, createPhoneUser provisions a real users row with TWO deterministic, attacker-derivable values: (1) a synthetic email `<digits>@phone.koshereats.local` built as fmt.Sprintf("%s@phone.koshereats.local", strings.TrimPrefix(phone,"+")) (phone_auth.go:237), and (2) a password hash of the literal string "phone-"+phone via bcrypt
  - _Fix:_ Stop using a guessable string as the synthetic password. Two layers: (1) In createPhoneUser (and the social_auth.go OAuth-create path), replace bcrypt.GenerateFromPassword([]byte("phone-"+phone)) with a hash of cryptographically random bytes, e.g. b:=make([]byte,32); rand.Read(b)

## HIGH (45)

- **Cart schedule-picker pre-selects the wrong day (UTC off-by-one) for evening users**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/screens/cart/DeliveryTimeCard.kt:161 (initialSelectedDateMillis in ScheduledTimePickerDialog)`
  - Material3 DatePicker treats selectedDateMillis as UTC-start-of-day, and the decode-back here correctly uses ZoneId.of('UTC') (lines 168-170, 179-180). But the SEED value is computed as initial.atZone(ZoneId.systemDefault()).toInstant().toEpochMilli() — the raw local instant, not the UTC midnight of the local date. For users in zones behind UTC (all of the US), an evening 'initi
  - _Fix:_ Compute the seed the same way SchedulePickerSheet does: val initialMillis = initial.toLocalDate().atStartOfDay(ZoneOffset.UTC).toInstant().toEpochMilli(). This makes the pre-selected day match the user's local 'initial' date and stay consistent with the UTC decode-back.
- **Menu load failure rendered as empty restaurant; Retry is a dead no-op**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/screens/restaurant/RestaurantDetailScreen.kt:424-475 (menu empty/error branch) and 451 (onClick)`
  - RestaurantViewModel already exposes a dedicated menu-load state (menuState: MenuLoadState, menuError) and a working retryMenu() method, but RestaurantDetailScreen ignores all of it. The empty-menu region gates only on the RESTAURANT-level uiState.isLoading / uiState.error. The menu fetch (loadMenu) never touches uiState.isLoading and reports failures via menuState/menuError, so
  - _Fix:_ Gate the menu region on uiState.menuState instead of the restaurant-level flags: render shimmer when menuState == Loading, render the error+Retry card when menuState == Error (using menuError for the message), and only show 'no menu items yet' when menuState == Loaded && menuCate
- **Scheduled-time check runs AFTER the card is charged (charged-but-no-order)**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/viewmodels/CheckoutViewModel.kt:516-562 (onPayTapped), 624-627 (finalizeOrder guard)`
  - iOS validates the scheduled delivery/pickup time BEFORE charging the card (CheckoutViewModel.swift scheduledTimeIsValid() is called inside presentAndChargePaymentSheet at line ~256, before passesInflightGuard and before presenting the PaymentSheet), with an explicit comment that a stale scheduled time 'must abort before the card is charged, never after — once the card is charge
  - _Fix:_ Add the scheduled-time validity check to onPayTapped() before any charge — mirror iOS scheduledTimeIsValid(): in onPayTapped(), right after the _serverCartCorrupted / zero-cost guards and before setting isProcessing/presenting the sheet, check `if (_uiState.value.scheduledFor != 
- **Non-geocoded address gate runs after the card is charged → charged-but-no-order with no recovery marker**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/viewmodels/CheckoutViewModel.kt:609-639`
  - In finalizeOrder() the address validation `if (!isPickup && address != null && !address.isGeocoded) { ... return }` (line 613-616) runs AFTER Stripe's PaymentSheet has already completed (the card is charged — finalizeOrder is only reached from onPaymentResult(success=true) or the stub path), and BEFORE persistInflightPI() (line 637-639) and the createOrder loop. So if the user 
  - _Fix:_ Move the geocode validation out of the post-charge path. Either (a) gate it in onPayTapped()/canPay BEFORE presenting the PaymentSheet (refuse to charge when the selected delivery address is non-geocoded, prompting the user to re-add it), or (b) drop the client-side isGeocoded bl
- **Android courier foreground service skips permission guard on the delivery-active path, crashing with SecurityException on Android 14+**
  - `android/courier/app/src/main/java/com/koshereats/courier/services/LocationForegroundService.kt:42-50`
  - In onStartCommand, the EXTRA_DELIVERY_ACTIVE=true branch (the path taken when a courier toggles offline while still mid-delivery, see DashboardViewModel.toggleOnline lines 90-93) calls startForeground(NOTIFICATION_ID, notification, ServiceInfo.FOREGROUND_SERVICE_TYPE_LOCATION) WITHOUT first checking locationTracker.hasPermission(). The normal (online) branch deliberately guards
  - _Fix:_ Move the permission check to the very top of onStartCommand so it covers BOTH branches: if (!locationTracker.hasPermission()) { notify the permission-error notification and stopSelf(); return START_NOT_STICKY }. Run this guard before either startForeground(... FOREGROUND_SERVICE_
- **Pending auto-reject countdown anchors on created_at, not updated_at — scheduled-then-promoted orders show "Auto-rejecting…" immediately**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/dashboard/ActiveOrderCard.kt:135-136, 204-241`
  - The live pending countdown is fed `order.createdAt` (ActiveOrderCard line 136: `PendingCountdown(createdAt = order.createdAt)`), and `PendingCountdown` computes elapsed time as `Duration.between(placedAt, now)` against a 10-minute TTL (lines 212-233). The backend's stale-rejection sweep explicitly measures the pending window from `updated_at`, NOT `created_at` (backend/internal
  - _Fix:_ Pass the pending-entry time to the countdown instead of the placement time. In ActiveOrderCard.kt change the call to `PendingCountdown(anchor = order.updatedAt.ifBlank { order.createdAt })` and rename the `PendingCountdown(createdAt: String)` parameter to `anchor`, parsing that f
- **Onboarding kosher-cert dropdown lists the UNKNOWN sentinel and shows raw enum names**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/onboarding/OnboardingScreen.kt:921-947 (KosherStep dropdown); display value at 922, items at 938-946`
  - The KosherStep certification dropdown iterates `KosherCertification.entries` with NO filter, so the Moshi fallback sentinel `KosherCertification.UNKNOWN` is a selectable option, and it renders each option with the raw Kotlin enum name (`cert.name`) instead of `cert.displayName`. Two concrete problems: (1) the labels are wrong/ugly — sellers see "KOF_K", "STAR_K", "CHOF_K", "CRC
  - _Fix:_ In KosherStep, change the dropdown source and label to match RestaurantSettingsScreen: iterate `KosherCertification.entries.filter { it != KosherCertification.UNKNOWN }`, render each item as `cert.displayName` (not `cert.name`), and set the field `value = certification.displayNam
- **Seller detail screen never shows the assigned courier's contact for delivery orders (ready/picked_up)**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/orders/SellerOrderDetailScreen.kt:740-799`
  - The Android `Order` model carries a populated `courier: CourierPublic?` (Models.kt line 198), but OrderActionButtons never renders it. For a READY non-pickup non-self-delivery order, Android always shows a static disabled "Awaiting Pickup…" button (lines 740-752) regardless of whether a courier has already claimed the order; for PICKED_UP non-self-delivery it shows a static "Ou
  - _Fix:_ In OrderActionButtons, for the READY (non-pickup, non-self-delivery) and PICKED_UP (non-self-delivery) branches, render a courier card driven by `order.courier`: when non-null show name/rating/vehicleSummary and a tel: IntentDial like the customer card already does (lines 330-343
- **Escalated-to-Uber self-delivery order still shows seller self-pickup/self-deliver buttons**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/orders/SellerOrderDetailScreen.kt:705-799 (OrderActionButtons READY + PICKED_UP self-delivery branches)`
  - In OrderActionButtons, the READY branch shows the 'Mark Picked Up (self-delivery)' button whenever `isSelfDelivery` is true (line 722), and the PICKED_UP branch shows 'Mark Delivered' whenever `isSelfDelivery` is true (line 760). Neither branch checks `order.courier` or `order.externalDeliveryId`. The seller can escalate a self-delivery order to Uber Direct while it is in accep
  - _Fix:_ Mirror the iOS guard: only take the self-delivery action path when the order is not already handed off. Pass the order's courier/external state into OrderActionButtons (e.g. a `isHandedOff = order.courier != null || !order.externalDeliveryId.isNullOrEmpty()` flag) and in both the
- **Seller order detail never shows the assigned courier (name/phone/vehicle) and mislabels a claimed order as "Awaiting Pickup…"**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/orders/SellerOrderDetailScreen.kt:705-799 (READY/PICKED_UP branches); order.courier only used at 476`
  - For a platform-delivery order (not pickup, not self-delivery), the Android seller detail screen completely ignores order.courier. The CourierPublic object (firstName, phone, vehicleSummary, rating, totalDeliveries) is decoded from the API but never rendered. At READY it always shows a disabled "Awaiting Pickup…" button — even after a courier has already claimed the order (order
  - _Fix:_ In OrderActionButtons, branch the non-pickup READY/PICKED_UP UI on order.courier: when order.courier != null, render a courier card (firstName, rating, totalDeliveries, vehicleSummary, and a Phone IconButton dialing ACTION_DIAL tel:${courier.phone}, mirroring the existing custome
- **user_auth_providers has no unique constraint on (provider, provider_id) — one external identity can bind to multiple accounts, making provider login resolve non-deterministically**
  - `backend/internal/database/migrations/022_user_auth_providers.sql:5-17 (table + idx_uap_provider_lookup); exploited in backend/internal/handlers/link_auth.go:84-102, social_auth.go:185-195, phone_auth.go:195-203`
  - The user_auth_providers junction table is the authoritative map from an external auth identity (Apple sub, Google sub, or E.164 phone) to a local account. It declares only UNIQUE(user_id, provider) and a NON-unique lookup index idx_uap_provider_lookup ON (provider, provider_id). There is no unique constraint on (provider, provider_id), so the DB happily lets the SAME identity b
  - _Fix:_ Add a unique index and make the junction authoritative. New migration:    -- de-dupe any existing collisions first (keep the oldest link per identity)   DELETE FROM user_auth_providers a USING user_auth_providers b    WHERE a.provider = b.provider AND a.provider_id = b.provider_i
- **External-dispatch persist error leaves a paid delivery un-recorded, then the reaper re-dispatches a SECOND paid delivery**
  - `backend/internal/dispatch/external.go:207-216`
  - In ExternalDispatcher.Dispatch, after the (paid, irreversible) provider CreateDelivery succeeds, the code records external_delivery_id/external_provider with a single UPDATE guarded by external_provider='dispatching'. If that UPDATE returns a DB ERROR (uerr != nil — a transient pool error, a context cancellation when the inline MarkOrderReady caller's goroutine ctx is torn down
  - _Fix:_ On the persist UPDATE error, do not just return — the provider delivery already exists and money is spent, so the recording MUST be made durable before any reaper can clear the sentinel. Retry the recording UPDATE on a fresh context.Background() with a short timeout (the original
- **EscalateToUber claim CAS lacks a status guard — dispatches (and pays for) a courier on a just-cancelled, already-refunded order**
  - `backend/internal/dispatch/external.go:72-78 (claim CAS); triggered via backend/internal/handlers/orders.go:1355-1378 EscalateToUber`
  - EscalateToUber lets a seller hand an order to Uber/DoorDash while it is in status 'accepted' or 'preparing' (orders.go:1367 `o.status IN ('accepted','preparing','ready')`). It deliberately takes NO row lock and relies entirely on ExternalDispatcher.Dispatch's claim-before-create CAS. But that claim UPDATE (external.go:72-78) guards only on `courier_id IS NULL AND external_deliv
  - _Fix:_ Add a status guard to the claim CAS in ExternalDispatcher.Dispatch so it can only claim orders in a deliverable state, e.g. change the WHERE in external.go:72-78 to also require `AND status IN ('accepted','preparing','ready')`, and add the same `AND status IN (...)` predicate to 
- **ListCourierActiveOrders drains rows without rows.Err() — courier's in-flight delivery silently vanishes**
  - `backend/internal/handlers/courier_orders.go:510-530`
  - The active-orders loop (for rows.Next() at ~line 514) appends scanned orders and falls straight to the nil-check + writeJSON without ever calling rows.Err(). pgx's rows.Next() returns false both at a clean end AND when iteration hits an error mid-stream (connection blip, decode failure, context deadline). In the error case the handler returns HTTP 200 with a truncated (or empty
  - _Fix:_ After the for-rows loop and before the `if list == nil` check, add:      if err := rows.Err(); err != nil {         writeError(w, http.StatusInternalServerError, "failed to fetch active orders")         return     }  Mirroring the guard already present in ListOrders (orders.go:38
- **Courier who delivers before Stripe Connect onboarding is never paid (no payout row, no backfill)**
  - `backend/internal/handlers/courier_orders.go:370-387`
  - In DeliverOrder, the courier_payout_queue INSERT is gated on `connectID != ""` (the courier's stripe_connect_id). Stripe Connect onboarding (CreatePayoutAccount) is a completely separate flow that does NOT gate courier approval — an approved courier can go online, claim, pick up, and deliver orders with an empty stripe_connect_id. The DashboardView even ships a permanent 'Set u
  - _Fix:_ Stop dropping the payout when stripe_connect_id is empty, and backfill on onboarding completion. (1) In DeliverOrder, always enqueue the payout row when payout > 0, using an empty/placeholder stripe_connect_id; relax the migration's `stripe_connect_id VARCHAR(255) NOT NULL` to nu
- **In-house courier payout pays the full external-provider delivery quote (consumer fee) + platform markup, overpaying every KE-fleet delivery**
  - `backend/internal/handlers/courier_orders.go:356-367`
  - DeliverOrder computes the in-house courier's payout as `payout := deliveryFee + tip + platformBump`, where `deliveryFee` is the order's stored `delivery_fee`. The inline comment claims that delivery_fee is a fixed "$3.99 base" plus a consumer 2.5%, but that base does not exist anywhere. For a platform (KE-fleet) delivery — the only kind DeliverOrder can run on, since ClaimOrder
  - _Fix:_ Decouple the courier payout from the consumer-facing delivery_fee. Introduce an explicit courier base rate (e.g. cfg.CourierBaseDeliveryCents, defaulting to the intended ~$3.99) and compute `payout := courierBase + tip + platformBump`, OR subtract the retained platform markup fro
- ✅ **ClaimOrder lets a courier claim (and get paid for) a PICKUP order**
  - `backend/internal/handlers/courier_orders.go:236-247`
  - The atomic-claim UPDATE in ClaimOrder gates on status='ready', courier_id IS NULL, external_provider IS NULL, and delivery_mode='platform' — but it never requires fulfillment_type='delivery'. The available-deliveries feed (ListAvailableDeliveries) does filter fulfillment_type='delivery', but ClaimOrder takes an arbitrary order id from the URL, so the feed filter does not protec
  - _Fix:_ Add `AND orders.fulfillment_type = 'delivery'` to the WHERE clause of the ClaimOrder atomic-claim UPDATE (and, defensively, to the PickupOrder and DeliverOrder status-CAS UPDATEs) so only delivery orders are claimable/progressable by a courier.
- **ClaimOrder has no per-courier active-order cap; second claim freezes that consumer's tracking and is un-actionable in both apps**
  - `backend/internal/handlers/courier_orders.go:236-251`
  - ClaimOrder enforces no limit on how many in-flight orders a single courier may hold. The auto-dispatch sweep deliberately only assigns to couriers with no active order (tryAutoAssign uses NOT EXISTS over status IN ('ready','picked_up'), dispatcher.go:528-532), but a courier self-claiming from the marketplace can stack two or more 'ready'/'picked_up' orders. Two concrete consequ
  - _Fix:_ In ClaimOrder, before/within the claim, reject the claim if the courier already holds an active order, e.g. add a guard `AND NOT EXISTS (SELECT 1 FROM orders o2 WHERE o2.courier_id = $1 AND o2.status IN ('ready','picked_up'))` to the UPDATE (mirroring the dispatcher's unbusy-cour
- **Delivered-order payout is permanently lost when the courier hasn't onboarded to Stripe yet (no payout-queue row, no backfill)**
  - `backend/internal/handlers/courier_orders.go:366-408`
  - In DeliverOrder, the status-transition UPDATE stamps orders.courier_payout = deliveryFee + tip + platformBump UNCONDITIONALLY (line 372) and increments total_deliveries, so the courier immediately sees the money in their earnings/history screen. But the only thing that actually triggers a Stripe transfer — the INSERT INTO courier_payout_queue — is gated behind `if connectID != 
  - _Fix:_ Don't drop the payout when the courier isn't onboarded yet. Always enqueue the courier_payout_queue row on delivery (stripe_connect_id can be NULL/empty at enqueue time), and have sweepCourierPayouts skip/retry rows whose courier still has no usable connect account instead of nev
- **CreatePayoutAccount TOCTOU creates duplicate Stripe Express accounts and orphans the connect id**
  - `backend/internal/handlers/courier_payouts.go:42-74`
  - CreatePayoutAccount reads cp.stripe_connect_id, and only if it is empty calls h.stripe.CreateExpressAccount(...) then UPDATEs courier_profiles SET stripe_connect_id. The read (line 43-47) and the write (line 65-67) are two separate, unguarded statements with no row lock, no upstream serialization (it is a plain authenticated POST /courier/payouts/account, main.go:386), and stri
  - _Fix:_ Make the claim atomic so only one caller ever creates an account. Either (a) before creating, atomically reserve the slot with an UPDATE ... WHERE user_id = $1 AND (stripe_connect_id IS NULL OR stripe_connect_id = '') and only proceed to CreateExpressAccount when RowsAffected()==
- ✅ **DoorDash picked_up webhook only matches status='ready', stranding orders escalated while accepted/preparing**
  - `backend/internal/handlers/doordash_webhook.go:77-94`
  - The DoorDash 'picked_up' webhook handler updates the order only WHERE status = 'ready' AND external_delivery_id IS NOT NULL. But an external delivery can be dispatched to DoorDash before the order reaches 'ready': EscalateToUber (orders.go ~line 1367) dispatches via the shared dispatcher.Dispatch on orders with status IN ('accepted','preparing','ready'), and Dispatch picks the 
  - _Fix:_ Widen the DoorDash 'picked_up' WHERE clause to match the Uber webhook: change `WHERE id = $2 AND status = 'ready' AND external_delivery_id IS NOT NULL` to `WHERE id = $2 AND status IN ('accepted', 'preparing', 'ready') AND external_delivery_id IS NOT NULL`.
- **DoorDash 'delivered' webhook strands the order when 'picked_up' was dropped or arrives out of order**
  - `backend/internal/handlers/doordash_webhook.go:96-117`
  - The DoorDash 'delivered' branch updates the order with `WHERE id = $2 AND status = 'picked_up' AND external_delivery_id IS NOT NULL`. DoorDash Drive delivers webhooks at-least-once and can drop or reorder them, so if the preceding 'picked_up' webhook was never processed (network drop, out-of-order delivery), the order is still in status 'ready' when 'delivered' arrives. The upd
  - _Fix:_ Make the 'delivered' update accept the order from either an in-flight or pre-pickup external state, e.g. `WHERE id = $2 AND external_delivery_id IS NOT NULL AND status IN ('ready','picked_up')`, stamping `picked_up_at = COALESCE(picked_up_at, $1)` so a skipped 'picked_up' webhook
- ✅ **DoorDash picked_up webhook only matches status='ready', stranding accepted/preparing escalated orders**
  - `backend/internal/handlers/doordash_webhook.go:77-94`
  - The DoorDash 'picked_up' webhook UPDATE is gated on `status = 'ready'` (line 80), but a seller can escalate a self-delivery order to an external provider via EscalateToUber while the order is still 'accepted' or 'preparing' — orders.go EscalateToUber explicitly allows `o.status IN ('accepted','preparing','ready')` (orders.go:1367), and dispatch.Dispatch sets external_delivery_i
  - _Fix:_ Widen the DoorDash 'picked_up' UPDATE source-status set to match the Uber handler: change the WHERE clause to `WHERE id = $2 AND status IN ('accepted', 'preparing', 'ready') AND external_delivery_id IS NOT NULL`. This makes a DoorDash pickup transition the order to 'picked_up' re
- **Phone-link in LinkProvider bypasses the DB-backed OTP brute-force lockout used by phone login**
  - `backend/internal/handlers/link_auth.go:54-70`
  - VerifyPhoneLogin (phone_auth.go:111-148) gates OTP checks behind phone_otp_starts: it enforces a TTL, counts failed_attempts, and applies a 10-minute lockout after otpMaxAttempts. The phone branch of LinkProvider calls h.sms.Check(ctx, phone, req.Code) directly with no TTL check, no failed-attempt counter, and no lockout. An authenticated user can therefore brute-force OTP code
  - _Fix:_ Route the phone branch of LinkProvider through the same phone_otp_starts TTL + failed_attempts + locked_until enforcement that VerifyPhoneLogin uses (factor that logic into a shared helper and call it from both), so OTP verification on the link path is rate-limited and locks out 
- ✅ **Self-delivery pickup and Uber escalation can both win — order picked up by seller AND paid-dispatched to Uber**
  - `backend/internal/handlers/orders.go:1295-1311 (SellerPickupOrder) + backend/internal/dispatch/external.go:72-78 (Dispatch claim)`
  - A `restaurant`-mode (self-delivery) order qualifies for BOTH SellerPickupOrder (requires delivery_mode='restaurant', status='ready') and EscalateToUber (requires fulfillment_type='delivery', status IN ('accepted','preparing','ready'), no delivery_mode restriction). Neither path mutually excludes the other:  - SellerPickupOrder's lock SELECT (line 1292-1297) and UPDATE (line 130
  - _Fix:_ Make the two paths mutually exclusive at the DB level. (1) In SellerPickupOrder add `AND o.external_provider IS NULL AND o.external_delivery_id IS NULL` to the FOR UPDATE lock SELECT (orders.go:1295) and the same to the status UPDATE WHERE clause (orders.go:1310), so a seller can
- **CancelOrder refunds an order already paid-dispatched to Uber/DoorDash, with no provider cancel**
  - `backend/internal/handlers/orders.go:678-711 (CancelOrder); compare 1367 EscalateToUber`
  - CancelOrder lets the consumer cancel+refund any order in status IN ('pending','accepted') (lines 682-684). Its guard checks only user_id and status — never external_provider or external_delivery_id, and it never cancels the external delivery. EscalateToUber (line 1367) dispatches a PAID Uber Direct / DoorDash delivery for orders in status IN ('accepted','preparing','ready') and
  - _Fix:_ Add an external-dispatch guard to CancelOrder's locking SELECT so an already-dispatched order can't be silently cancelled+refunded: change the WHERE to `... AND status IN ($3,$4) AND external_provider IS NULL AND external_delivery_id IS NULL AND courier_id IS NULL FOR UPDATE`. Wh
- **CreateOrder keeps live re-quoted delivery fee on StampedDeliveryFee error → charged-but-rejected order**
  - `backend/internal/handlers/orders.go:136-199`
  - CreateOrder first computes a FRESH live delivery-fee quote (lines 136-149 call quoteDeliveryFee, which hits the live Uber/DoorDash quote API whose price drifts second-to-second), then tries to override it with the fee that was actually stamped onto the PaymentIntent at charge time (lines 158-166 via h.stripe.StampedDeliveryFee). The stamp-reuse exists precisely so the recorded 
  - _Fix:_ On a StampedDeliveryFee error for a delivery order, do NOT silently proceed with the divergent live quote. Either (a) fail the request explicitly with a retryable 503/409 so the idempotent client retry re-reads the stamp once Stripe recovers (the ON CONFLICT(stripe_payment_id) pa
- **Seller can self-pickup/self-deliver an order already dispatched to Uber/DoorDash (double delivery, wasted external fee, premature 'delivered')**
  - `backend/internal/handlers/orders.go:1274-1330 (SellerPickupOrder), 1400-1474 (SellerDeliverOrder)`
  - EscalateToUber (orders.go:1339, route PATCH /seller/orders/{id}/escalate) is explicitly designed to hand an OPEN SELF-DELIVERY (delivery_mode='restaurant') order off to an external provider. Its eligibility query (lines 1355-1368) has NO delivery_mode filter: it accepts any fulfillment_type='delivery' order in status accepted/preparing/ready with courier_id IS NULL AND external
  - _Fix:_ Add `AND o.external_delivery_id IS NULL` (and ideally `AND o.external_provider IS NULL`) to the eligibility SELECT in both SellerPickupOrder (the `FOR UPDATE OF o` query at lines 1292-1297) and SellerDeliverOrder (the `FOR UPDATE OF o` query at lines 1419-1424), and mirror it in 
- **EscalateToUber dispatches on the request context, orphaning a paid delivery on client disconnect (→ double-pay re-dispatch)**
  - `backend/internal/handlers/orders.go:1378 (and dispatch/external.go:207)`
  - EscalateToUber calls h.dispatcher.Dispatch(r.Context(), in) — the seller's HTTP request context. ExternalDispatcher.Dispatch uses that same ctx for both the *paid* provider call (CreateDelivery, external.go:157/181) AND the subsequent persist UPDATE that records external_delivery_id (external.go:207-211). If the seller's request is cancelled (client disconnect, app backgrounded
  - _Fix:_ Detach the paid+persist portion from the request lifetime. Simplest: in EscalateToUber, call h.dispatcher.Dispatch(context.Background(), in) instead of r.Context() (the handler then reads back external_tracking_url with r.Context() for the response, which is harmless to cancel). 
- **Pickup PaymentIntent reused for a delivery order yields a $0 delivery fee (free delivery exploit)**
  - `backend/internal/handlers/orders.go:136-166, 192`
  - CreateOrder reuses ONLY the delivery fee stamped on the PaymentIntent (StampedDeliveryFee) but never verifies that the fulfillment_type used to build that PaymentIntent matches the fulfillment_type of the order being created. The PI metadata (CreatePaymentSheet in payments/stripe.go ~L507) stamps only user_id, the orphan marker, and delivery_fee — not fulfillment_type. A client
  - _Fix:_ Bind the PaymentIntent to its fulfillment_type the same way it is bound to user_id and delivery_fee. In CreatePaymentSheet add a metadata key (e.g. "fulfillment_type": req.FulfillmentType normalized to delivery/pickup) and, in CreateOrder, read it back and reject (402) when it do
- **charge.refunded / charge.dispute webhooks never halt a pending courier payout for a refunded order**
  - `backend/internal/handlers/payments.go:400-422`
  - The Stripe webhook handler's `charge.refunded` and `charge.dispute.created` cases only log and email the admin (alertSubject/alertBody). They never reconcile the order: no order-status flip and, critically, no check against `courier_payout_queue`. The courier payout is enqueued in DeliverOrder and transferred by the dispatcher's sweepCourierPayouts on a background tick. If a ch
  - _Fix:_ In the `charge.refunded` (and ideally `charge.dispute.created`) case, after resolving orderID via lookupOrderByPaymentIntent, cancel any not-yet-sent payout inside the same tx: `UPDATE courier_payout_queue SET status='failed_permanent', last_error='order refunded/disputed', updat
- **CreatePaymentIntent prices the order from a silently-truncated cart (no rows.Err())**
  - `backend/internal/handlers/payments.go:99-114`
  - CreatePaymentIntent computes the authoritative Stripe charge by summing cart rows in a `for rows.Next()` loop, then calls `rows.Close()` and proceeds without ever checking `rows.Err()`. A mid-iteration row error (decode/network/connection reset) makes Next() return false exactly like a clean end of rows, so the loop silently stops early and `subtotal` (plus the BOGO cheapest-un
  - _Fix:_ After the loop and `rows.Close()`, add `if err := rows.Err(); err != nil { writeError(w, http.StatusInternalServerError, "failed to read cart"); return }` (mirroring orders.go CreateOrder) so a truncated cart fails the whole price computation instead of charging against an underc
- ✅ **External-delivery 'canceled' webhook strands a picked_up order with no recovery path**
  - `backend/internal/handlers/uber_direct_webhook.go:156-160 (and doordash_webhook.go:122-125)`
  - The Uber Direct (and DoorDash) `canceled`/`cancelled` webhook resets `external_delivery_id = NULL, external_provider = NULL, external_tracking_url = NULL` with NO status guard (`WHERE id = $1` only). The intent is to make the order re-dispatchable, which works only while status='ready' because sweepAutoDispatch (dispatcher.go:471) selects exclusively `status='ready' AND courier
  - _Fix:_ On the cancel branch, only clear the external fields when the order is still recoverable, and otherwise route it for re-dispatch/refund explicitly. Minimal fix: scope the reset to non-terminal states and re-arm re-dispatch, e.g. `UPDATE orders SET external_delivery_id=NULL, exter
- **Uber/DoorDash 'delivered' webhook fires the consumer 'delivered' push even when the status UPDATE matched zero rows**
  - `backend/internal/handlers/uber_direct_webhook.go:128-149`
  - In the 'delivered' case the handler runs `UPDATE orders SET status='delivered' ... WHERE id=$2 AND status='picked_up' AND external_delivery_id IS NOT NULL`, but only checks the Exec error — it never checks RowsAffected. It then unconditionally sends `notify.OrderDelivered` to the consumer whenever a consumerID is found. Provider webhooks are not ordered or guaranteed: if the pr
  - _Fix:_ Capture the command tag and gate the push on a real state transition: ```go tag, err := h.db.Pool.Exec(ctx, `UPDATE orders SET status='delivered', delivered_at=$1, updated_at=$1 WHERE id=$2 AND status='picked_up' AND external_delivery_id IS NOT NULL`, now, externalID) if err != n
- **Uber Direct 'delivered' webhook strands the order when 'pickup_complete' was dropped or arrives out of order**
  - `backend/internal/handlers/uber_direct_webhook.go:128-133`
  - The Uber Direct webhook's `pickup_complete` branch correctly advances an external order to 'picked_up' from any of status IN ('accepted','preparing','ready'). But the `delivered` branch's UPDATE only matches `WHERE id = $2 AND status = 'picked_up' AND external_delivery_id IS NOT NULL`. Webhook delivery is at-least-once and NOT ordered, so a `delivered` event can arrive after a 
  - _Fix:_ Make the terminal 'delivered' transition tolerant of a missed intermediate state by accepting the pre-delivery statuses an external order can legitimately be in, e.g. `WHERE id = $2 AND status IN ('accepted','preparing','ready','picked_up') AND external_delivery_id IS NOT NULL`, 
- **UpdateProfile writes an unverified phone number, enabling phone-OTP-login account hijack/squatting**
  - `backend/internal/handlers/user.go:54-100 (phone set at 70-72 and 88-90); attack lands via backend/internal/handlers/phone_auth.go:171-177`
  - PUT /api/v1/user/profile (UpdateProfile) writes the caller-supplied `phone` straight into `users.phone` with NO ownership proof of that number — no OTP, no E.164 validation, no normalization. The only gate is the partial unique index `idx_users_phone_role_vertical (phone, role, vertical) WHERE phone <> ''`. Phone-OTP login (VerifyPhoneLogin) treats *possession of a verified pho
  - _Fix:_ Stop letting UpdateProfile set/replace `phone` without proof of possession. Concretely: (1) remove `phone` from the UpdateProfile request/SQL entirely and require phone changes to go through an OTP-gated path that reuses the existing Twilio Verify flow (StartPhoneLogin/VerifyPhon
- **Courier payout failure path drops retry bookkeeping when the reaper reset the row mid-transfer, causing a no-backoff retry storm**
  - `backend/internal/scheduler/dispatcher.go:968-981 (tryPayout failure UPDATE)`
  - In the legacy direct-transfer payout sweep, tryPayout claims a row as 'processing', calls the blocking Stripe transfer, then records the outcome with an UPDATE guarded `WHERE id=$5 AND status='processing'`. The reaper (lines 830-842) resets any row that has been 'processing' longer than payoutProcessingTimeout (15 min) back to 'pending'. The SUCCESS path explicitly handles the 
  - _Fix:_ Mirror the success path's reaped-row handling in the failure path. After the failure UPDATE, check `ct.RowsAffected()`; if it is 0, the row was reaped mid-flight, so re-apply the retry/permanent-failure state with a guard that also matches the reaped 'pending' state, e.g. run the
- **Abandoned courier claim is never released — order stranded with no delivery path**
  - `backend/internal/scheduler/dispatcher.go:262-288 (reapStaleDispatchClaims), 455-505 (sweepAutoDispatch query)`
  - A courier claims a 'ready' order via ClaimOrder, which sets orders.courier_id and claimed_at but leaves status='ready' (no status change — see the flow comment at courier_orders.go:26). There is NO reaper that ever clears a stale courier claim. If the claiming courier then goes offline (SetCourierOnline online=false leaves courier_id intact), force-quits the app, or simply neve
  - _Fix:_ Add a stale-claim reaper to the runAll sweep, modeled on reapStaleDispatchClaims, that releases a courier claim when the courier has not progressed the order within a grace window. Concretely, in dispatcher.go add (and call from runAll before sweepAutoDispatch): `UPDATE orders SE
- **reapStaleDispatchClaims can release a still-in-flight external-dispatch claim, orphaning a paid delivery and enabling re-dispatch**
  - `backend/internal/scheduler/dispatcher.go:272-288 (reapStaleDispatchClaims) vs backend/internal/dispatch/external.go:65-224 (Dispatch)`
  - ExternalDispatcher.Dispatch stamps external_provider='dispatching', updated_at=NOW() at claim time (external.go:72-78) and only resets the row after the provider's quote+create calls return. Those calls are blocking with a 30s-per-call HTTP timeout each (uberdirect/client.go:50 and doordash/client.go:44), and Dispatch makes up to three serially: an Uber quote, a DoorDash quote,
  - _Fix:_ Widen the reaper threshold to comfortably exceed the maximum possible Dispatch wall-clock time (sum of provider client timeouts plus headroom — e.g. INTERVAL '5 minutes'), and/or pass a bounded context.WithTimeout into ExternalDispatcher.Dispatch that is strictly shorter than the
- **iOS courier going offline mid-delivery freezes the consumer's live tracking pin**
  - `ios/courier/KosherEatsCourier/ViewModels/DashboardViewModel.swift:63-78`
  - In DashboardViewModel.toggleOnline, the offline branch unconditionally tears down location reporting: it sets location.hasActiveDelivery = false, calls location.stopHeartbeat() (which sets isHeartbeatActive=false so didUpdateLocations stops POSTing to /courier/location), and calls stopPolling(). It does this even when the courier still has an in-progress delivery (an order in s
  - _Fix:_ Mirror the Android logic: in the offline branch, do not blanket-stop the heartbeat/polling when an active delivery exists. Compute `let stillDelivering = !active.isEmpty` (active is already populated from the last refresh) and, after `try await api.setOnline(false, ...)`, set `is
- **Dashboard tab ignores order pushes — no new-order chime and up-to-30s stale list on the default screen**
  - `ios/seller/KosherEatsSeller/ViewModels/DashboardViewModel.swift:37-63, 65-92`
  - The Dashboard is the app's default landing tab (MainTabView selectedTab = .dashboard) and is where a restaurant counter phone sits all service. But neither DashboardViewModel nor DashboardView observes the push event bus (.orderStatusUpdated / .newOrderReceived posted by AppDelegate via PushEvents.postIfOrderEvent). The Dashboard refreshes its Active Orders list and stat cards 
  - _Fix:_ Wire the Dashboard into the same push pipeline the Orders tab uses. In DashboardViewModel.init (or startAutoRefresh), register an NSObjectProtocol observer for .orderStatusUpdated (object: nil, queue: .main) that, on the @MainActor, calls `await self?.load()` (mirroring OrdersVie
- **Poll/dashboard merge strips courier, external_delivery_id, and delivery_mode from the order the seller is viewing**
  - `ios/seller/KosherEatsSeller/ViewModels/OrdersViewModel.swift:450-461 (mergeFresh); consumed by SellerOrderDetailView.swift:141-145`
  - The seller order-list endpoint (backend ListSellerOrders, orders.go:847-864) does NOT select courier, external_delivery_id, or delivery_mode — those columns are only returned by the per-order detail endpoint GetSellerOrder via loadOrderWithCourier (orders.go:519-567). All three Order JSON fields are emitted with omitempty (models.go:256/281/289), so list responses omit them ent
  - _Fix:_ Stop letting a stripped list copy overwrite a richer detail copy. Preferred fix (backend-free): make mergeFresh preserve courier/externalDeliveryId/deliveryMode from the existing in-memory order when the incoming fresh copy lacks them. Since these Order fields are `let`, add a me
- **Modifier option price field is unusable for non-whole-dollar amounts — the formatted get/set binding clobbers in-progress input**
  - `ios/seller/KosherEatsSeller/Views/Menu/ModifierGroupsEditorView.swift:326-332`
  - The per-option price TextField uses a Binding whose get always reformats from the stored cents: get returns String(format: "%.2f", Double(opt.priceDelta) / 100). On every keystroke SwiftUI re-reads get and overwrites the field with the fully-formatted value, so intermediate states like '5.' or '5.5' are immediately replaced. Concretely: typing '5' sets priceDelta=500 and the fi
  - _Fix:_ Back the field with a plain @State String per option (a draft price string), parse to cents only on commit/save like MenuItemFormView does (it stores priceText separately and converts via CurrencyFormat.parseCents at submit, not on every keystroke). Do not derive the displayed te
- **Modifier-option price field re-rounds on every keystroke (cannot enter e.g. $5.50)**
  - `ios/seller/KosherEatsSeller/Views/Menu/ModifierGroupsEditorView.swift:314-338`
  - The per-option price input in ModifierGroupEditorView.optionRow uses a computed Binding whose get formats the cents back to a 2-decimal dollar string (`String(format: "%.2f", Double(opt.priceDelta) / 100)`) and whose set immediately parses+rounds to cents (`CurrencyFormat.parseCents(newVal)`) and writes `binding.wrappedValue.priceDelta = cents`. Because every keystroke round-tr
  - _Fix:_ Stop formatting on every keystroke. Back the field with a plain `@State private var priceText: String` per option (seed it from `opt.priceDelta` in hydrate/onAppear via `String(format: "%.2f", Double(opt.priceDelta)/100)`), apply the same onChange comma→'.'/single-dot filter Menu
- **Orders-list "Waiting m:ss" timer is keyed off created_at, so promoted scheduled orders show a huge bogus wait and instantly flag overdue**
  - `ios/seller/KosherEatsSeller/Views/Orders/SellerOrdersView.swift:330-356 (OrderRowView.timestampLine)`
  - OrderRowView shows a live elapsed counter ("Waiting m:ss", flipping red/overdue at 15 min) for orders in pending/accepted/preparing/ready, computed as `context.date.timeIntervalSince(order.createdAtDate)`. created_at is when the order was originally PLACED. A scheduled order is placed now but only promoted to pending near its delivery window; the backend sets updated_at=NOW() o
  - _Fix:_ Drive the elapsed timer off the order's updated_at (the status-transition timestamp) rather than created_at, mirroring the backend's auto-reject clock. Add an `updatedAtDate` computed property to Order (same ISO8601 frac/plain fallback chain as createdAtDate) and use it as the `c

## MEDIUM (48)

- **Scheduled-order tracking progress bar renders fully empty (stepIndex -2 vs iOS 0)**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/data/models/Models.kt:42-52`
  - OrderStatus.stepIndex maps SCHEDULED to -2, but iOS maps `.scheduled` to 0 (Models.swift lines 121-133, same as pending). OrderTrackingScreen.ProgressBar fills a segment when `index <= status.stepIndex` (OrderTrackingScreen.kt lines 345-348). Because the lowest segment index is 0 and `0 <= -2` is false, a SCHEDULED order shows a completely empty/grey 6-step progress bar, signal
  - _Fix:_ Change `SCHEDULED -> -2` to `SCHEDULED -> 0` in OrderStatus.stepIndex so the first timeline step fills for scheduled orders, matching iOS. (The StatusHeader ETA suppression for SCHEDULED is keyed off the enum value directly, not stepIndex, so this change does not regress the ETA-
- **OrderConfirmationScreen has no pickup/delivery ETA or address card (iOS parity gap)**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/screens/checkout/OrderConfirmationScreen.kt:48-128 (screen body) and 130-196 (OrderSummaryCard)`
  - The Android order-confirmation screen renders only a generic success header ("We've sent the order to the restaurant. You'll get a notification once it's accepted.") plus a price-summary card. It is completely fulfillment-type-agnostic: it shows no ETA and no destination card. The iOS counterpart (ios/consumer/KosherEatsConsumer/Views/Orders/OrderConfirmationView.swift) shows a
  - _Fix:_ Add a fulfillment-aware ETA + destination section to OrderConfirmationScreen, mirroring iOS. Compute `val isPickup = order.fulfillmentType == "pickup"`. Render an ETA line that formats `order.estimatedDeliveryTime` with the same epoch/garbage-time guard used elsewhere (parse via 
- **MenuItemSheet lets the user select paused (is_available=false) modifiers and charges their priceDelta**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/screens/restaurant/MenuItemSheet.kt:87-96, 206-253`
  - The customization sheet renders every modifier in a group with `for (option in group.modifiers)` and makes each row `clickable { toggle(group, option.id) }`, and `toggle()` has no availability guard. `isAvailable` is consulted only when pre-selecting defaults (line 76: `filter { it.isDefault && it.isAvailable }`); the visible option list and the toggle path never check it. So a
  - _Fix:_ In `toggle()` (line 87), short-circuit when the option is unavailable: look up the Modifier by id in `group.modifiers` and `return` early if `!isAvailable` (mirroring iOS `guard mod.isAvailable else { return }`). In the option row (line 206-253), make the row non-interactive and 
- **Unavailable menu items remain tappable in RestaurantDetailScreen and open the add-to-cart sheet**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/screens/restaurant/RestaurantDetailScreen.kt:527-535, 879-963`
  - In the menu list, each item is rendered with `VerticalMenuItemCard(menuItem = menuItem, isOrderable = restaurant.isOpen, onClick = { if (restaurant.isOpen) sheetItem = menuItem })`. Both the gating prop and the onClick check only `restaurant.isOpen` and never consult `menuItem.isAvailable`. Inside VerticalMenuItemCard the whole row is `clickable(enabled = isOrderable, ...)` (li
  - _Fix:_ Pass availability into the gate: compute `val orderable = restaurant.isOpen && menuItem.isAvailable` and use it for both `isOrderable = orderable` and `onClick = { if (orderable) sheetItem = menuItem }`. Inside VerticalMenuItemCard, additionally dim the row (reduced alpha) and sh
- **Pickup orders show 'Delivering to' + home icon in tracking address card (iOS shows 'Pickup from')**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/screens/tracking/OrderTrackingScreen.kt:557-588`
  - The tracking screen's AddressCard is hard-coded to a house icon and the label 'Delivering to', and it is always rendered (OrderTrackingScreen.kt line 218 calls AddressCard(address = order.deliveryAddress) regardless of fulfillment type). iOS's addressCard (OrderTrackingView.swift line 385-402) branches on order.fulfillmentType == "pickup" to show a bag icon and 'Pickup from' in
  - _Fix:_ Pass order.fulfillmentType (or an isPickup flag) into AddressCard and branch like iOS: when fulfillmentType == "pickup", use a bag/storefront icon and the label 'Pickup from', otherwise the home icon and 'Delivering to'. Ideally also show the restaurant address (order has restaur
- **External-delivery (Uber/DoorDash) tracking card hardcodes 'Your order is on its way' for every status**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/screens/tracking/OrderTrackingScreen.kt:516-527 (ExternalDeliveryCard)`
  - ExternalDeliveryCard renders a static title 'Delivered by <provider>' and a static subtitle 'Your order is on its way.' regardless of the order's actual status. For an externally-dispatched order that is still accepted/preparing/ready (i.e. before the Uber/DoorDash courier has picked it up), the card tells the customer 'Your order is on its way' which is false — the food is sti
  - _Fix:_ Pass order.status into ExternalDeliveryCard and derive the subtitle from it, mirroring phaseText/phaseSubtext: e.g. accepted/preparing -> 'Your order is being prepared.', ready -> 'Ready and waiting for the <provider> driver.', picked_up -> 'Your order is on its way.', delivered 
- **Cart never merges identical lines that carry modifiers (iOS parity break, duplicate lines)**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/viewmodels/CartViewModel.kt:195-209`
  - addItem() only attempts to merge a re-added item into an existing cart line when `selectedModifiers.isEmpty()` (the `existingIndex` lookup is guarded by `if (selectedModifiers.isEmpty()) ... else -1`). So adding the same menu item with the SAME modifier selection twice always falls through to the `else` branch and appends a brand-new CartItem instead of bumping the existing lin
  - _Fix:_ Mirror iOS: compute existingIndex regardless of whether modifiers are present, matching on menuItem id AND note AND the modifier set. Replace the `if (selectedModifiers.isEmpty()) { indexOfFirst { ...empty... } } else -1` block with a single lookup, e.g.: `val existingIndex = cur
- **Custom checkout tip not capped at subtotal → cryptic "Failed to price order (400)" dead-end (iOS parity gap)**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/viewmodels/CheckoutViewModel.kt:436-442 (currentTipCents) and 408-420 (updateCustomTip)`
  - The Android custom-tip path only clamps the entered tip to the flat $500 ceiling (`cents.coerceIn(0, 50_000)` at line 441) and `updateCustomTip` (408-420) does no validation at all — it just stores the text and debounce-triggers refreshBundle. The backend, however, rejects any order/intent where tip > subtotal: both backend/internal/handlers/payments.go:170 (`if tip > subtotal 
  - _Fix:_ Clamp the custom tip against the subtotal, matching iOS. In currentTipCents (line 441) replace `cents.coerceIn(0, 50_000)` with a subtotal-aware cap, e.g. `val cap = if (subtotal <= 0) 50_000 else minOf(50_000, subtotal); cents.coerceIn(0, cap)` (using the `subtotal`/`subtotalCen
- **409 order-recovery matches by restaurant only, can surface a stale prior order and clear the in-flight marker**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/viewmodels/CheckoutViewModel.kt:669-692 (409 branch), 844-851 (fetchMostRecentOrder)`
  - In finalizeOrder()'s 409 (duplicate payment_intent_id) branch, the code calls fetchMostRecentOrder() (which is `getOrders(page=1).firstOrNull()`) and accepts it as the just-placed order whenever `existing.restaurantId == _restaurantId`. It never confirms the recovered order corresponds to THIS PaymentIntent. If the user has any earlier order from the same restaurant, fetchMostR
  - _Fix:_ Drop the fetchMostRecentOrder()/restaurantId heuristic in the 409 branch and recover strictly by PaymentIntent, matching iOS. Replace the `val existing = fetchMostRecentOrder(); val matches = existing != null && existing.restaurantId == _restaurantId` logic with a direct `recover
- **Tracking pin sticks to old courier's position after reassignment (missing courier-id match in poll merge)**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/viewmodels/OrderTrackingViewModel.kt:108-119`
  - In loadOnce(), the poll response is merged with the in-memory order to preserve the SSE-streamed courier lat/lng so a stale poll snapshot doesn't overwrite the live pin. The guard only checks `fetchedCourier != null && existing != null && (existing.lat != null || existing.lng != null)` and then copies `existing.lat`/`existing.lng` onto the fetched courier. It never verifies tha
  - _Fix:_ Add the same identity check iOS uses before reusing the prior position: only splice `existing.lat`/`existing.lng` onto `fetchedCourier` when `fetchedCourier.id == existing.id`. e.g. `if (fetchedCourier != null && existing != null && fetchedCourier.id == existing.id && (existing.l
- **Android earnings 'All time' total and lifetime count are silently capped at 100**
  - `android/courier/app/src/main/java/com/koshereats/courier/ui/screens/earnings/EarningsScreen.kt:115-121, 169-170`
  - The Android courier earnings screen derives every aggregate — Today, This week, All time, and the 'X deliveries total' count — purely from `state.history`, which comes from `repo.listHistory()`. The backend `ListCourierHistory` (backend/internal/handlers/courier_orders.go:576) returns at most `LIMIT 100` rows ordered by `delivered_at DESC`. So for any courier who has completed 
  - _Fix:_ Do not compute lifetime/all-time aggregates from the capped recent-deliveries list. Either (a) add a dedicated backend earnings-summary endpoint that returns SUM(courier_payout) and COUNT(*) over all delivered orders for the courier (and today/week subtotals computed server-side 
- **Android Order model omits the per-order `delivery_mode` field the backend sends**
  - `android/seller/app/src/main/java/com/koshereats/seller/data/models/Models.kt:180-207 (data class Order)`
  - The backend serializes a per-order delivery_mode snapshot on every seller order: models.Order has `DeliveryMode string `json:"delivery_mode,omitempty"`` (backend/internal/models/models.go:289) and loadOrderWithCourier selects `COALESCE(rest.delivery_mode, 'platform')` for the seller scope (backend/internal/handlers/orders.go:555). The struct comment states this exists 'so the s
  - _Fix:_ Add `@Json(name = "delivery_mode") val deliveryMode: String = "platform"` to the Android `Order` data class and an `val isSelfDelivery: Boolean get() = deliveryMode == "restaurant"` computed property, then have SellerOrderDetailScreen derive isSelfDelivery from `order.isSelfDeliv
- **Modifier-group editor has no control to set a default option (is_default always false)**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/menu/MenuItemFormScreen.kt:103 (OptionEntry.isDefault); 811-859 (option row UI); 876-885 (request mapping)`
  - `OptionEntry` carries an `isDefault` field and the submit maps `isDefault = opt.isDefault` into `ModifierOptionRequest`, but the option-editor row (lines 811-859) renders only Name, +$ price, an availability Switch, and a delete button — there is NO checkbox/toggle to mark an option as the default. Existing options preserve whatever default the server returned, but for any NEW 
  - _Fix:_ Add a small selectable affordance per option in the dialog (e.g. a radio/checkbox labeled "Default") that sets `option.isDefault`. For single-select groups (maxSelections == 1) enforce mutual exclusivity by clearing `isDefault` on the other options when one is chosen, mirroring h
- **Menu category chip taps trigger a full network reload + blank-list spinner; the instant selectCategory() is dead code**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/menu/MenuManagementScreen.kt:219 (chip onClick); MenuViewModel.kt:167-177 (unused selectCategory)`
  - MenuViewModel.selectCategory() was written specifically for category-chip taps — its KDoc says "chip taps must be instant and work offline, since state.categories already holds every category and its items. No network round-trip needed." But it is never called anywhere in the app (verified: grep finds zero call sites outside the function definition). Instead, the category Filte
  - _Fix:_ Change the chip onClick from `onClick = { viewModel.loadMenuItems(category) }` to `onClick = { viewModel.selectCategory(category) }` so category switching uses the already-loaded in-memory tree (instant, offline-safe). Keep loadMenuItems() only for the explicit pull-to-refresh (l
- **Onboarding ZIP field accepts non-digits and skips the 5-digit validation iOS enforces**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/onboarding/OnboardingScreen.kt:819-828 (ZIP input); 838-848 (AddressStep validation)`
  - The onboarding AddressStep ZIP input uses `onValueChange = { if (it.length <= 10) zipCode = it }` — it does not filter to digits (unlike the dead CreateRestaurantScreen and RestaurantSettingsScreen, which both apply `.filter { it.isDigit() }`), so letters/symbols can be entered. The Continue validation (lines 838-848) only checks blank, never the length/format. iOS's equivalent
  - _Fix:_ Filter the ZIP input to digits and cap at 5 (`onValueChange = { zipCode = it.filter { c -> c.isDigit() }.take(5) }`), and in the Continue handler add a length check mirroring iOS: when the address is being provided (not the import-all-blank case), require `zipCode.length == 5` an
- **Seller order actions gate on the restaurant's live delivery_mode, not the order's own snapshot**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/orders/SellerOrderDetailScreen.kt:98, 473`
  - The backend emits a per-order `delivery_mode` field on every order (models.go Order.DeliveryMode, json `delivery_mode`) specifically so the seller app can gate the self-delivery pickup/deliver actions on the mode the order was created/dispatched under. iOS decodes this per-order (Order.deliveryMode, default "platform") and gates its action buttons on `order.isSelfDelivery` (Sel
  - _Fix:_ Add `@Json(name = "delivery_mode") val deliveryMode: String = "platform"` (and a `val isSelfDelivery: Boolean get() = deliveryMode == "restaurant"`) to the Android `Order` data class in Models.kt, matching iOS Order.deliveryMode/isSelfDelivery. In SellerOrderDetailScreen.kt, repl
- **Order detail self-pickup/deliver buttons keyed on live restaurant deliveryMode, not the order's mode, so a delivery-mode switch strands in-flight self-delivery orders**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/orders/SellerOrderDetailScreen.kt:98 and 472-473`
  - isSelfDelivery is derived from authState.restaurant?.deliveryMode (the restaurant's CURRENT mode), not from the order being viewed. The backend already stamps a per-order delivery_mode (models.go line 289, populated in orders.go line 532/555 via COALESCE(rest.delivery_mode,...)), and the iOS app branches the self-pickup/deliver actions on that. If a seller flips "Who delivers" 
  - _Fix:_ Add @Json(name="delivery_mode") val deliveryMode: String? = null to the Order model and compute isSelfDelivery from order.deliveryMode == "restaurant" (falling back to the restaurant-level mode only when the per-order field is absent), so a later restaurant-mode change can't retr
- **Restaurant Settings "Save Changes" toggles isSaving synchronously, so it never blocks double-taps (duplicate PUTs)**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/settings/RestaurantSettingsScreen.kt:733-743`
  - The Save Changes onClick sets `isSaving = true`, fires `changes.forEach { authViewModel.updateRestaurantField(key, value) }` (each launches its own async PUT /seller/restaurant), shows a Toast, then immediately sets `isSaving = false` — all synchronously, without awaiting the network calls. Because the button's `enabled = !isSaving` is reset before any request completes, the bu
  - _Fix:_ Either keep `isSaving = true` until the updates actually finish (e.g. add an AuthViewModel.updateRestaurantFields(changes: Map) suspend that does a single batched PUT and flips a saving flag in AuthState that the screen observes), or at minimum gate re-entry: track an in-flight f
- **Admin platform 'today revenue' and 'today orders' include cancelled/rejected (refunded) orders**
  - `backend/internal/handlers/admin.go:450-451`
  - AdminStats computes today_orders as `(SELECT COUNT(*) FROM orders WHERE created_at::date = CURRENT_DATE)` and today_revenue as `(SELECT COALESCE(SUM(total),0) FROM orders WHERE created_at::date = CURRENT_DATE)` with NO status filter. Orders that were cancelled or rejected — and whose charge was already refunded (see CancelOrder/RejectOrder, which call RefundPaymentIntent) — are
  - _Fix:_ Add the same status guard the seller dashboard uses to both today_* subqueries: `... WHERE created_at::date = CURRENT_DATE AND status NOT IN ('cancelled','rejected')`. (And, to match the seller dashboard's day boundary, consider `(created_at AT TIME ZONE 'America/New_York')::date
- **AdminStats 'today' uses UTC date boundary while seller dashboard uses America/New_York**
  - `backend/internal/handlers/admin.go:450-451`
  - AdminStats computes platform-wide today-orders and today-revenue with `created_at::date = CURRENT_DATE`. The pgx pool sets no session TimeZone (backend/internal/database/database.go never sets one), so on Fly.io managed Postgres `CURRENT_DATE` resolves in UTC. The seller dashboard (GetDashboardStats in seller.go:1027-1052) deliberately anchors the same 'today' boundary in `Amer
  - _Fix:_ Change both today filters in the AdminStats query to the same ET-anchored expression the seller dashboard already uses: `(created_at AT TIME ZONE 'America/New_York')::date = (NOW() AT TIME ZONE 'America/New_York')::date` for the today-orders count and the today-revenue SUM. Bette
- **AdminListOrders and AdminListCouriers can silently truncate the admin tables (missing rows.Err)**
  - `backend/internal/handlers/admin.go:412-423 (AdminListOrders), 255-269 (AdminListCouriers)`
  - Both AdminListOrders (line 412) and AdminListCouriers (line 255) iterate `for rows.Next()` and write the JSON response without ever calling rows.Err() after the loop. A mid-stream read error terminates the loop indistinguishably from a normal end-of-rows, so the admin dashboard renders a partial orders/couriers list and reports HTTP 200 — a courier in the approval queue or a re
  - _Fix:_ Add `if err := rows.Err(); err != nil { writeError(w, http.StatusInternalServerError, "failed to list orders") ; return }` after the AdminListOrders loop and the equivalent ("failed to list couriers") after the AdminListCouriers loop, before the writeJSON call.
- **AdminRestaurantsPage drains rows without rows.Err() — a pending restaurant can silently vanish from the approval queue**
  - `backend/internal/handlers/admin_dashboard.go:57-84`
  - The key-gated admin approval page iterates the pending-restaurant result set with `for rows.Next() { ... }` and never checks `rows.Err()` after the loop. pgx surfaces a mid-stream read error (connection blip, decode failure, statement timeout on a large pending list) by making Next() return false exactly like a clean end-of-rows. When that happens the page renders only the rest
  - _Fix:_ After the `for rows.Next()` loop (before the count==0 check at line 81), add: ```go if err := rows.Err(); err != nil {     w.WriteHeader(http.StatusInternalServerError)     writeAdminPage(w, "Error", `<p>Could not load restaurants.</p>`)     return } ``` This fails the page loudl
- **Add-to-cart accepts paused (is_available = false) modifiers and charges their price delta**
  - `backend/internal/handlers/cart.go:222-227`
  - snapshotModifiers validates each supplied modifier id by joining menu_item_modifiers to menu_item_modifier_groups and checking `m.id = ANY($1) AND g.menu_item_id = $2`, but it never filters `m.is_available = true`. The consumer menu (attachModifierGroups in restaurants.go:241) DOES filter `AND is_available = true`, so a paused modifier is hidden from the menu — yet if a client 
  - _Fix:_ Add `AND m.is_available = true` to the WHERE clause in snapshotModifiers. The existing `len(out) != len(modifierIDs)` count check will then reject the whole add-to-cart with 'invalid modifier selection' when any selected option is paused, matching the menu's availability semantic
- **ListChatMessages can silently truncate an order's chat history (missing rows.Err)**
  - `backend/internal/handlers/chat.go:116-129`
  - ListChatMessages drains the chat_messages rows in a `for rows.Next()` loop and returns the slice with HTTP 200 without checking rows.Err(). A mid-iteration error ends the loop like a clean finish, so the consumer/seller/courier polling the order chat can be served a partial message list (e.g. missing the most recent messages) with no error surfaced. Because clients poll, a tran
  - _Fix:_ After the loop, before `writeJSON`, add `if err := rows.Err(); err != nil { writeError(w, http.StatusInternalServerError, "failed to fetch messages"); return }`.
- **ListCourierHistory drains pgx rows without checking rows.Err() — earnings history can be silently truncated**
  - `backend/internal/handlers/courier_orders.go:599-623`
  - ListCourierHistory iterates `for rows.Next()` to build the HistoryRow list, `continue`s on per-row scan errors, and returns the list without ever calling `rows.Err()`. A read error partway through iteration causes rows.Next() to stop early and the loop to exit cleanly, so the handler returns HTTP 200 with a partial delivery history. Because both the iOS and Android earnings scr
  - _Fix:_ After the `for rows.Next()` loop (before `if list == nil`), add: 	if err := rows.Err(); err != nil { 		writeError(w, http.StatusInternalServerError, "failed to fetch history") 		return 	} so a partial read returns an error the client retries on, instead of presenting a silently i
- **ListCourierActiveOrders drops rows.Err() — an in-flight delivery can vanish from the courier's active list**
  - `backend/internal/handlers/courier_orders.go:538-557`
  - ListCourierActiveOrders returns the order(s) the courier is currently delivering (status IN ('ready','picked_up')). The row loop appends scanned orders and `continue`s on per-row scan errors, but there is NO `rows.Err()` check after the loop (unlike the consumer/seller ListOrders paths, which all check it). pgx's rows.Next() returns false on a mid-iteration error exactly as it 
  - _Fix:_ After the `for rows.Next()` loop and before the `if list == nil` guard, add:      if err := rows.Err(); err != nil {         writeError(w, http.StatusInternalServerError, "failed to fetch active orders")         return     }  matching the rows.Err() handling already used in ListO
- **ListNearbyDeals drains rows without checking rows.Err() — consumer deals feed silently truncated**
  - `backend/internal/handlers/deals.go:347-361`
  - ListNearbyDeals (the main consumer-facing /deals/nearby discovery feed) iterates the result set with `for rows.Next() { ... }` and then immediately calls writeJSON(w, http.StatusOK, interleaveDeals(deals)) without ever checking rows.Err(). A connection/decode error mid-iteration makes rows.Next() return false exactly like a clean end-of-rows, so the handler returns HTTP 200 wit
  - _Fix:_ After the `for rows.Next()` loop and before the writeJSON call, add: `if err := rows.Err(); err != nil { slog.Error("ListNearbyDeals: row iteration failed", slog.String("error", err.Error())); writeError(w, http.StatusInternalServerError, "failed to list deals"); return }`.
- **Scheduled order fires immediate 'new order — tap to accept' seller push it can't act on, then double-pushes at promotion**
  - `backend/internal/handlers/orders.go:316-317`
  - CreateOrder fires h.notify.OrderCreated unconditionally after commit, even when the order was inserted with status='scheduled' (initialStatus is set to models.OrderScheduled at line 206 when ScheduledFor is >30 min out). For a scheduled order this is wrong on two counts: (1) the seller gets a 'New order — tap to accept' chime immediately for an order that is in 'scheduled' stat
  - _Fix:_ Guard the immediate notification on the order actually being pending: replace the unconditional `go h.notify.OrderCreated(...)` at line 317 with `if order.Status == models.OrderPending { go h.notify.OrderCreated(context.Background(), order.RestaurantID, order.RestaurantName, orde
- **Tax diverges between PaymentIntent and CreateOrder the moment Stripe Tax is wired (StripeTaxEnabled seam only applied at PI time)**
  - `backend/internal/handlers/orders.go:177`
  - CreatePaymentIntent computes tax through the StripeTaxEnabled branch: when the flag is set it calls h.taxForOrder(discountedSubtotal) (payments.go:158-162), otherwise the flat `discountedSubtotal * TaxRatePercent / 100`. CreateOrder, which produces the authoritative recorded order total that must equal pi.Amount, hardcodes only the flat path: `tax := discountedSubtotal * h.cfg.
  - _Fix:_ Make CreateOrder use the same tax seam as CreatePaymentIntent so they can never diverge: replace orders.go:177 `tax := discountedSubtotal * h.cfg.TaxRatePercent / 100` with the same StripeTaxEnabled branch (`var tax int; if h.cfg.StripeTaxEnabled { tax = h.taxForOrder(discountedS
- **Courier 'new delivery available' broadcast displays only delivery_fee, undercounting actual payout (tip + 2.5% bump omitted)**
  - `backend/internal/handlers/orders.go:1182`
  - MarkOrderReady broadcasts the marketplace push via notify.OrderReady(..., deliveryFee), passing only the base delivery_fee as the payout figure shown to couriers ("New delivery available — <restaurant> • $X.XX"). But the actual amount a courier is paid on DeliverOrder (courier_orders.go ~L366-367) is deliveryFee + courier_tip + (subtotal*2.5%) platformBump, and the auto-assigne
  - _Fix:_ Compute the same figure the courier will actually be paid (delivery_fee + courier_tip + subtotal*25/1000) at the OrderReady call site and pass that to notify.OrderReady, matching CourierAutoAssigned's o.payout (delivery_fee + courier_tip) at minimum. Factor the payout formula use
- **Payment-intent handler drops default_card_brand/last4, so the saved-card preview never renders**
  - `backend/internal/handlers/payments.go:201-214`
  - CreatePaymentSheet (backend/internal/payments/stripe.go:523-531) looks up the customer's default card and returns DefaultCardBrand / DefaultCardLast4 on the bundle, and the iOS client is fully wired to consume them: APIService.PaymentSheetBundle decodes default_card_brand / default_card_last4 (APIService.swift:589-602) and CheckoutView renders the 'Paying with saved card — Visa
  - _Fix:_ In payments.go's response map add the two fields from the bundle: "default_card_brand": bundle.DefaultCardBrand, and "default_card_last4": bundle.DefaultCardLast4 (omit/empty-string when no saved card, which iOS already treats as 'no preview').
- **ListAddresses drains rows without rows.Err() — a saved delivery address can silently disappear at checkout**
  - `backend/internal/handlers/user.go:129-144`
  - The address loop (for rows.Next() at ~line 130) appends each scanned address then returns the slice without ever checking rows.Err(). If iteration errors part-way (network blip, context deadline, a decode failure on one row), Next() returns false exactly as it does at a clean end, so the user receives HTTP 200 with a partial address list and no indication anything was dropped. 
  - _Fix:_ After the for-rows loop (before `if addresses == nil`), add:      if err := rows.Err(); err != nil {         writeError(w, http.StatusInternalServerError, "failed to fetch addresses")         return     }
- **Order JSON never includes scheduled_for, so the consumer can never see the time they scheduled**
  - `backend/internal/models/models.go:231-293`
  - The consumer app lets users schedule an order for a future time (CheckoutView DeliveryTimeCard) and CheckoutViewModel sends scheduled_for to both /payments/intent and /orders. The backend stores scheduled_for and the dispatcher uses it (dispatcher.go:389), and the iOS Order model is wired to decode it: Models.swift declares var scheduledFor: Date? with CodingKey scheduled_for (
  - _Fix:_ Add `ScheduledFor *time.Time `json:"scheduled_for,omitempty"`` to models.Order and populate it in the order SELECT/scan paths (orders.go GetOrder/ListOrders), so the iOS Order.scheduledFor decodes and the scheduled time can be surfaced on the confirmation/detail screens.
- **Courier payout uses two different Stripe idempotency keys across the legacy and Temporal paths, enabling a double transfer**
  - `backend/internal/payout/payout.go:62-63`
  - The same courier payout can be transferred to Stripe under two different idempotency keys depending on which payout path runs. The legacy sweep calls TransferToCourier with the courier_payout_queue row id as the idempotency key (dispatcher.go:893 -> p.id), while the Temporal StripeTransfer activity uses the workflow id 'payout-<orderID>' (payout.go:62-63, passed to TransferToCo
  - _Fix:_ Derive one canonical idempotency key from the order in BOTH paths. Either have the legacy tryPayout pass payout.WorkflowID(p.orderID) (i.e. 'payout-<orderID>') as the key instead of p.id, or have the Temporal StripeTransfer activity pass the queue row id. Since order_id is UNIQUE
- **Auto-dispatch double-books a courier (TOCTOU between unbusy-courier select and order-only CAS)**
  - `backend/internal/scheduler/dispatcher.go:520-567 (tryAutoAssign)`
  - tryAutoAssign enforces the marketplace's single-active-order invariant ('nearest free online courier' — the package doc literally says 'unbusy courier') only inside the courier-SELECT, via NOT EXISTS(orders o2 WHERE o2.courier_id=cp.user_id AND o2.status IN ('ready','picked_up')). It picks courier C, then in a SEPARATE statement does the order claim UPDATE whose CAS only guards
  - _Fix:_ Collapse the select-then-update into a single atomic statement so the unbusy predicate is re-evaluated at write time. Replace the two statements with one UPDATE whose WHERE re-asserts both the order CAS and the courier's freedom, choosing the courier inline, e.g.: UPDATE orders S
- **sweepAutoDispatch assign UPDATE never re-checks courier capacity, double-booking a courier under concurrent self-claim**
  - `backend/internal/scheduler/dispatcher.go:520-555`
  - tryAutoAssign selects the nearest free courier with a NOT EXISTS guard that the courier has no active ('ready'/'picked_up') order (dispatcher.go:528-532), then assigns via an UPDATE whose CAS only re-checks the ORDER side: WHERE id=$2 AND status='ready' AND courier_id IS NULL AND external_provider IS NULL (dispatcher.go:550-555). The COURIER's capacity is read in the SELECT but
  - _Fix:_ Make the assign UPDATE re-assert courier capacity atomically, e.g. add to the WHERE clause: AND NOT EXISTS (SELECT 1 FROM orders o2 WHERE o2.courier_id = $1 AND o2.status IN ('ready','picked_up')). Then RowsAffected()==0 cleanly means the courier got busy in the interval and the 
- **Order Again (reorder) silently wipes an existing cart from a different restaurant**
  - `ios/consumer/KosherEatsConsumer/ViewModels/CartViewModel.swift:302-320 (reorder); invoked from ios/consumer/KosherEatsConsumer/Views/Orders/OrdersListView.swift:88-110`
  - OrderRowView's "Order Again" button calls CartViewModel.reorder(items:restaurantID:), which loops over addItem(...) directly. addItem bypasses the restaurant-switch guard (wouldSwitchRestaurant / pendingRestaurantSwitch / confirmation alert) that requestAddItem provides. The backend AddToCart (backend/internal/handlers/cart.go:151-155) deletes ALL existing cart_items and reassi
  - _Fix:_ Route reorder through the same guard the menu add path uses. Before the loop, check cartVM.wouldSwitchRestaurant(to: restaurantID); if true, surface the existing PendingRestaurantSwitch confirmation (or a reorder-specific confirmation) instead of adding immediately. Concretely, i
- **reorder() loses the per-item 'notes' and stops on first failure mid-way, leaving a partial cart**
  - `ios/consumer/KosherEatsConsumer/ViewModels/CartViewModel.swift:302-320`
  - reorder() iterates the past order's items and calls addItem for each. It correctly forwards modifierIDs (SelectedModifier.id maps to the real modifier id, verified against backend models.go SelectedModifier), but if any single addItem fails (e.g. an item is now unavailable / out of stock — the backend rejects unavailable menu items in cart.go), it does `await loadCart(); return
  - _Fix:_ Make reorder all-or-nothing or clearly communicate partial success. Simplest robust fix: collect the per-item results, and if any add fails, either (a) keep the items that succeeded but change the surfaced message to 'Some items couldn't be added (e.g. now unavailable)' rather th
- **Inflight PaymentIntent marker survives logout and blocks a different user's checkout**
  - `ios/consumer/KosherEatsConsumer/ViewModels/CheckoutViewModel.swift:457-492, 615-631`
  - The charged-but-unrecovered guard persists the PaymentIntent id in UserDefaults under the global key "inflight_payment_intent" (and a companion timestamp). passesInflightGuard() blocks any NEW charge for up to inflightMarkerMaxAge (5 minutes) whenever this marker is set and reconcile can't resolve it. The marker is never cleared on logout: APIService.logout()/clearToken() (APIS
  - _Fix:_ Clear the inflight marker on logout. Add a public method on CheckoutViewModel (or move the marker to a small helper) that does UserDefaults.standard.removeObject(forKey:) for both inflightPaymentIntentKey and inflightPaymentIntentAtKey, and call it from APIService.clearToken()/lo
- **External (Uber Direct) tracking shows contradictory 'Waiting for a courier' header above the 'Delivered by Uber' card**
  - `ios/consumer/KosherEatsConsumer/Views/Orders/OrderTrackingView.swift:33-46, 275-318`
  - For an external/third-party delivery (order.isExternalDelivery == true) OrderTrackingView correctly swaps the live map for externalDeliveryCard ('Delivered by Uber' + 'Track delivery'), but it still renders statusHeader(for: order) right below it (line 39). statusHeader uses phaseText/phaseSubtext, which are written entirely around a PLATFORM courier and are not branched on isE
  - _Fix:_ Branch the header copy for external deliveries: when order.isExternalDelivery, replace the .ready/.pickedUp phaseText/phaseSubtext with partner-handoff wording (e.g. .ready -> 'Handing off to <provider>', .pickedUp -> 'On its way with <provider>'), reusing OrderTrackingView.provi
- **External-delivery card hardcodes "Your order is on its way" for every status, including pre-pickup states**
  - `ios/consumer/KosherEatsConsumer/Views/Orders/OrderTrackingView.swift:407-435 (externalDeliveryCard), used at 33-46`
  - When an order's delivery is fulfilled by Uber Direct / DoorDash Drive, OrderTrackingView swaps the live map for externalDeliveryCard(for:). That card always renders the static subtext "Your order is on its way." regardless of order.status. But isExternalDelivery flips true as soon as the dispatch row is written, which the backend allows while the order is still 'accepted', 'pre
  - _Fix:_ Drive the external card's subtext from order.status instead of a constant. e.g. compute a string: for .pickedUp -> "Your order is on the way."; for .ready -> "Your order is ready and a \(providerName) courier is on the way to pick it up."; for .accepted/.preparing -> "Your order 
- **OrderTrackingView shows courier-centric status copy and timeline for pickup orders**
  - `ios/consumer/KosherEatsConsumer/Views/Orders/OrderTrackingView.swift:248-325, 481-488`
  - The live tracking screen's status header (`statusHeader(for:)`) and progress timeline are driven solely by `order.status`, with no awareness of `order.fulfillmentType`. For a pickup order — which by backend design has no courier and is collected by the customer — this renders wrong, courier-implying copy:  - `phaseText` for `.ready` returns "Waiting for a courier" (line 281) an
  - _Fix:_ Thread `order.fulfillmentType` into the header/timeline so pickup gets accurate copy. Change `phaseText`/`phaseSubtext`/`progressBar` to take a fulfillment flag (e.g. `isPickup: order.fulfillmentType == "pickup"`). For pickup, return: `.ready` -> "Ready for pickup" / "Your order 
- **External-delivery tracking screen shows courier-centric status header + 6-step timeline that contradict the Uber/DoorDash card**
  - `ios/consumer/KosherEatsConsumer/Views/Orders/OrderTrackingView.swift:33-46, 248-325, 407-435`
  - When `order.isExternalDelivery` is true (Uber Direct / DoorDash Drive fulfills the delivery, so there is no platform courier), the view correctly swaps the live map for `externalDeliveryCard(...)`, but it then ALSO renders `statusHeader(for: order)` directly underneath. `statusHeader` calls `phaseText(for:)`/`phaseSubtext(for:)` and `progressBar` (the 6-step `DeliveryTimeline`)
  - _Fix:_ Branch the status copy on external delivery the same way the map is branched. For external orders, drive the header text from the actual order state without courier-assignment language (e.g. .pending/.scheduled -> "Waiting for the restaurant", .accepted/.preparing -> "Your order 
- **Optional single-select modifier cannot be deselected, forcing an unremovable priced add-on into the cart**
  - `ios/consumer/KosherEatsConsumer/Views/Restaurant/MenuItemView.swift:474-485`
  - ModifierGroupView.toggle() handles single-select groups (maxSelections == 1, rendered as radios) with `if group.isSingleSelect { selected = [mod.id]; return }`. There is no branch that removes the current selection, and tapping the already-selected option just re-sets it. For a single-select group that is OPTIONAL (is_required == false, min_selections == 0 — allowed by the sche
  - _Fix:_ Allow deselecting a radio option in optional single-select groups. In toggle(), for the isSingleSelect branch, toggle off when the tapped option is already selected and the group is not mandatory: `if group.isSingleSelect { if selected.contains(mod.id) && !(group.isRequired || gr
- **Pending auto-reject countdown keys off created_at, not updated_at — scheduled orders show 'Auto-rejecting…' the instant they go pending**
  - `ios/seller/KosherEatsSeller/Views/Dashboard/ActiveOrderCard.swift:87-92, 135-168`
  - ActiveOrderCard's PendingCountdown computes time-in-pending from order.createdAtDate (placedAt = order.createdAtDate, then remaining = max(0, pendingOrderTTL - (now - placedAt))). But the backend's stale-rejection sweep deliberately measures the 10-minute TTL from updated_at (the moment the order ENTERED pending), NOT created_at — see backend/internal/scheduler/dispatcher.go:63
  - _Fix:_ Add an updatedAtDate computed property to Order (mirroring createdAtDate's two-format ISO8601 fallback chain in Models.swift:670-672), and in ActiveOrderCard render the countdown from order.updatedAtDate instead of order.createdAtDate (the `if order.status == .pending { if let pl
- **Dashboard pending-order countdown anchors on created_at, falsely showing 'Auto-rejecting…' on every promoted scheduled order**
  - `ios/seller/KosherEatsSeller/Views/Dashboard/ActiveOrderCard.swift:87-92, 135-168`
  - The dashboard's active-order card renders a live auto-reject countdown via PendingCountdown(placedAt: order.createdAtDate) and computes remaining = pendingOrderTTL(10m) - (now - created_at). This anchors on created_at, but the backend's stale-pending auto-reject is keyed off updated_at, not created_at — the dispatcher stamps updated_at = NOW() when a scheduled order is promoted
  - _Fix:_ Add `var updatedAtDate: Date? { Self.iso8601Frac.date(from: updatedAt) ?? Self.iso8601Plain.date(from: updatedAt) }` to Order (Models.swift, mirroring createdAtDate), then in ActiveOrderCard.swift line 88 pass `order.updatedAtDate` (falling back to createdAtDate only if nil) to P
- **Seller cannot set a default modifier option — the editor has no isDefault control and always sends is_default=false for new options**
  - `ios/seller/KosherEatsSeller/Views/Menu/ModifierGroupsEditorView.swift:238-246, 314-354, 393-406`
  - The modifier group editor renders only name, price, and a delete button per option (optionRow, lines 314-354). There is no UI to mark an option as the default. New options are created with isDefault: false hardcoded (line 243) and the save payload just passes through opt.isDefault (line 402), so a seller can never set or change which option is pre-selected. The Modifier model a
  - _Fix:_ Add a default toggle/selector to optionRow (a star/checkmark per row, or a single-select radio for required groups). For maxSelections == 1 enforce that at most one option is isDefault; for multi-select allow multiple. Wire it into the @State options array so opt.isDefault is edi
- **Seller "Call customer" button dials the masked phone (last 4 digits only)**
  - `ios/seller/KosherEatsSeller/Views/Orders/SellerOrderDetailView.swift:324-335 (customerSection); phone source masked at backend/internal/handlers/orders.go:627-629`
  - The order-detail customer card renders a tappable phone-call affordance built directly from order.customerPhone. But the backend deliberately masks the customer's phone for the seller scope: loadOrderWithCourier() (which backs GetSellerOrder, the call the detail screen makes via vm.fetchOrder) runs `o.CustomerPhone = maskPhone(o.CustomerPhone)` for scope=="restaurant_owner", tu
  - _Fix:_ Treat a masked value as non-callable. In customerSection, detect masking (e.g. `let isMasked = phone.contains("*")`) and only render the Link/tel: button when `!isMasked`; show the masked digits as plain, non-tappable text otherwise. Better long-term: have the backend expose a te

## LOW (42)

- **BOGO deal preview ignores modifier prices, diverging from server and iOS**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/data/models/Models.kt:332-335`
  - Cart.computeDiscount()'s BOGO branch computes the 'free' item value as items.minOfOrNull { it.menuItem.price } — the BASE menu-item price, EXCLUDING any selected-modifier deltas. The backend (resolveDealDiscount in backend/internal/handlers/deals.go line 298-311) and iOS (CartViewModel.swift line 70: cart.items.map(\.price).min(), where CartItem.price is documented 'per-unit, i
  - _Fix:_ In computeDiscount's BOGO branch, use the cheapest per-unit price including modifier deltas instead of the bare base price: replace `items.minOfOrNull { it.menuItem.price } ?: 0` with `items.minOfOrNull { it.menuItem.price + it.selectedModifiers.sumOf { m -> m.priceDelta } } ?: 0
- **Tracking screen shows courier-centric status header + 6-step progress bar above the external (Uber/DoorDash) delivery card**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/screens/tracking/OrderTrackingScreen.kt:172-180 (StatusHeader rendered for external), 284-385 (StatusHeader/phaseText/ProgressBar)`
  - For external deliveries the screen replaces the live map with ExternalDeliveryCard (good), but then unconditionally renders StatusHeader(status, ...) below it (line 180). StatusHeader uses the courier-centric phaseText/phaseSubtext: e.g. for READY it shows 'Waiting for a courier' / 'A courier will claim your order shortly', and renders the generic 6-step ProgressBar keyed on st
  - _Fix:_ For external deliveries, suppress or specialize the courier-centric copy in StatusHeader: pass order.isExternalDelivery into StatusHeader (or branch in OrderTrackingScreen) and, when external, use provider-neutral phase text (e.g. ACCEPTED/PREPARING -> 'Your order is being prepar
- **Percent-tip computed/displayed with integer truncation instead of rounding (iOS parity break)**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/viewmodels/CheckoutViewModel.kt:54 (TipChoice.label) and 435 (currentTipCents Percent branch)`
  - Both the tip-chip preview label (TipChoice.label: (subtotalCents * bps) / 10_000) and the actual charged tip (currentTipCents Percent branch: (subtotal * choice.bps) / 10_000) use integer division, which truncates. iOS computes the same percent tip with Int((Double(subtotal) * Double(bps) / 10_000).rounded()) in both CheckoutViewModel.TipChoice.label(for:) and currentTipCents()
  - _Fix:_ Round instead of truncate to match iOS, e.g. ((subtotalCents.toLong() * bps) / 10_000.0).roundToInt() (or compute with Math.round) in both TipChoice.label and the Percent branch of currentTipCents.
- **External-delivery orders open a doomed courier location SSE stream that never emits**
  - `android/consumer/app/src/main/java/com/koshereats/consumer/ui/viewmodels/OrderTrackingViewModel.kt:61-80, 155`
  - OrderTrackingViewModel.start() unconditionally calls launchLocationStream(orderId) for every order, with no awareness of order.isExternalDelivery. For an order fulfilled by Uber Direct / DoorDash Drive there is no platform courier and no one ever calls UpdateCourierLocation, so the SSE endpoint (backend StreamOrderLocation in orders.go:1484) accepts the connection (HTTP 200) an
  - _Fix:_ Guard the stream the same way the screen guards the map: in start() (and retryStream()), only launch launchLocationStream(orderId) for non-external deliveries. Since isExternalDelivery is only known after the first loadOnce(), check it after loadOnce returns: in the setupJob coro
- **Android courier delivery screen cannot call the customer (iOS parity gap)**
  - `android/courier/app/src/main/java/com/koshereats/courier/ui/screens/delivery/DeliveryMapScreen.kt:245-330 (DeliveryOverlayCard action Row) and data/models/Models.kt CourierOrder (lines ~168-184)`
  - The backend's ListCourierActiveOrders (backend/internal/handlers/courier_orders.go) returns the consumer's real first_name and phone as customer_name / customer_phone on each active order, and the iOS courier app (ios/courier/.../DeliveryMapView.swift) renders a tel: call button from liveOrder.customerPhone so a driver can phone the customer at handoff (gate codes, 'I'm outside
  - _Fix:_ Add `@SerializedName("customer_name") val customerName: String? = null` and `@SerializedName("customer_phone") val customerPhone: String? = null` to the Android CourierOrder data class so the backend-provided fields deserialize. Then in DeliveryOverlayCard, when `order.customerPh
- **Dashboard 'Active Orders' count undercounts the list it sits above (scheduled orders)**
  - `android/seller/app/src/main/java/com/koshereats/seller/data/models/Models.kt:27-31`
  - `OrderStatus.isActive` returns true for every status except DELIVERED/COMPLETED/CANCELLED/REJECTED/UNKNOWN, so SCHEDULED counts as active. DashboardViewModel builds the visible 'Active Orders' list with `ordersResponse.body().orEmpty().filter { it.status.isActive }` (DashboardViewModel.kt lines 197/233/272), which therefore INCLUDES scheduled orders. But the 'Active Orders' Sta
  - _Fix:_ Make the client 'active' definition match the backend's active_orders filter by excluding SCHEDULED. Add SCHEDULED to the false branch of OrderStatus.isActive (`DELIVERED, COMPLETED, CANCELLED, REJECTED, UNKNOWN, SCHEDULED -> false`). Scheduled orders are not yet actionable by th
- **Dashboard avg-prep-time truncates instead of rounding (iOS parity break)**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/dashboard/DashboardScreen.kt:299`
  - The Avg Prep Time StatCard renders the backend's avg_prep_time_min (a Double) with `"${state.stats.avgPrepTime.toInt()} min"`. `Double.toInt()` truncates toward zero, so an average of 29.7 min shows as "29 min". The iOS seller dashboard formats the identical value with `String(format: "%.0f min", vm.stats.avgPrepTime)` (DashboardView.swift line 208), which rounds half-up, so iO
  - _Fix:_ Round instead of truncate to match iOS: change `value = "${state.stats.avgPrepTime.toInt()} min"` to `value = "${kotlin.math.roundToInt(state.stats.avgPrepTime)} min"` (i.e. `state.stats.avgPrepTime.roundToInt()` with the kotlin.math import).
- **Create-deal fixed discount has no $100 client cap → confusing backend 400**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/deals/CreateDealScreen.kt:626-630`
  - When the discount type is FIXED, the submit handler computes discountValueCents = dollarsToCents(discountValue).coerceAtLeast(1) with no upper bound, and canCreate only checks (discountValue.toDoubleOrNull() ?: 0.0) > 0.0 — there is no client-side cap. The backend's CreateDeal handler rejects any fixed discount > 10000 cents ($100) with HTTP 400 "fixed discount cannot exceed $1
  - _Fix:_ Mirror iOS: in the FIXED branch of the onClick discountValueCents computation (or in canCreate/validationHint), reject amounts over 10000 cents before submitting. E.g. compute val fixedCents = dollarsToCents(discountValue); add `discountType == DiscountType.FIXED && fixedCents > 
- **Deal card shows "0h left" for any deal expiring in under an hour**
  - `android/seller/app/src/main/java/com/koshereats/seller/ui/screens/deals/DealsScreen.kt:211-214`
  - expiryText computes hours = ChronoUnit.HOURS.between(now, parsedExpiry) and renders "${hours}h left" when hours < 24. HOURS.between truncates toward zero, so a deal expiring in 1-59 minutes yields hours = 0 and the card displays "0h left" — confusing for a deal that is in fact still active and about to expire. A deal expiring in 90 minutes shows "1h left" (off by 30 min). iOS d
  - _Fix:_ Use minutes for the sub-hour case: val minutes = ChronoUnit.MINUTES.between(now, parsedExpiry); val expiryText = when { minutes < 60 -> "${minutes}m left"; minutes < 24*60 -> "${minutes/60}h left"; else -> "${minutes/(24*60)}d left" }. This keeps a meaningful label (e.g. "30m lef
- **No DB uniqueness on (provider, provider_id) — provider-identity binding rests on a racy, scope-limited app check**
  - `backend/internal/database/migrations/022_user_auth_providers.sql:16-17 (index); enforced in backend/internal/handlers/link_auth.go:83-109`
  - user_auth_providers has UNIQUE(user_id, provider) but only a NON-unique index idx_uap_provider_lookup on (provider, provider_id). So one external identity (a single Google sub, Apple sub, or phone number) can be bound to multiple distinct user rows. The ONLY guard against a collision is the app-level SELECT in LinkProvider (link_auth.go:83-96), and it has two weaknesses: (1) it
  - _Fix:_ Add a partial-or-full UNIQUE constraint that matches the security model. Since lookups are scoped by (role, vertical), enforce uniqueness of an external identity within a (role, vertical) at the DB layer — e.g. add a generated/duplicated role+vertical onto user_auth_providers (or
- **AdminListOrders and AdminListCouriers drain rows without rows.Err() — admin order/courier tables silently truncate**
  - `backend/internal/handlers/admin.go:412-427 and 255-269`
  - Both AdminListOrders (loop at line 412) and AdminListCouriers (loop at line 255) build their slices with `for rows.Next() { ... }` and then writeJSON the result with no rows.Err() check after the loop. A mid-iteration driver error makes Next() return false identically to a clean end, so the admin dashboard's platform-wide order table and courier-approval queue return HTTP 200 w
  - _Fix:_ After each loop, before the `if out == nil` / writeJSON, add: `if err := rows.Err(); err != nil { slog.Error("AdminListOrders/AdminListCouriers: row iteration failed", slog.String("error", err.Error())); writeError(w, http.StatusInternalServerError, "failed to list orders/courier
- **ListUpcomingDeliveries drains rows without rows.Err() — upcoming deliveries can be silently truncated**
  - `backend/internal/handlers/courier_orders.go:188-205`
  - The upcoming-deliveries loop (for rows.Next() at ~line 189) builds the list and returns it without calling rows.Err(). On a mid-iteration error pgx's Next() returns false just like a clean end, so the courier gets HTTP 200 with a truncated/empty 'upcoming' feed and no error. This is the pre-positioning feed couriers use to stage near a restaurant before an order flips to 'ready
  - _Fix:_ After the for-rows loop (before `if list == nil`), add:      if err := rows.Err(); err != nil {         writeError(w, http.StatusInternalServerError, "failed to fetch upcoming deliveries")         return     }
- **UpdateCourierLocation accepts and broadcasts pings from non-approved (e.g. suspended) couriers**
  - `backend/internal/handlers/courier_orders.go:451-509`
  - UpdateCourierLocation only checks getUserFromContext and the lat/lng range; unlike PickupOrder/DeliverOrder it never calls requireApprovedCourier. A courier whose onboarding_status was flipped away from 'approved' (e.g. admin suspension) mid-delivery can keep POSTing /courier/location, which still writes courier_profiles.last_lat/lng, appends to courier_locations, and fans the 
  - _Fix:_ Add `if !h.requireApprovedCourier(w, r, user["user_id"]) { return }` near the top of UpdateCourierLocation (after readJSON/range validation), consistent with PickupOrder and DeliverOrder, so a non-approved courier's heartbeats are rejected with 403.
- **ListAvailableDeliveries drains pgx rows without checking rows.Err() — courier's available-deliveries feed can be silently truncated**
  - `backend/internal/handlers/courier_orders.go:122-138`
  - ListAvailableDeliveries loops `for rows.Next()` building the deliveries slice, `continue`s past any per-row scan error, then returns the list without ever calling `rows.Err()`. If the connection/read fails partway through iteration (the canonical reason rows.Next() stops early), pgx returns the rows gathered so far and the loop exits normally, so the handler returns HTTP 200 wi
  - _Fix:_ After the `for rows.Next()` loop and before the `if list == nil` normalization, add: 	if err := rows.Err(); err != nil { 		writeError(w, http.StatusInternalServerError, "failed to fetch available deliveries") 		return 	} so a mid-stream read failure returns a real error instead o
- **ListUpcomingDeliveries drops rows.Err() — courier pre-positioning feed can be silently truncated**
  - `backend/internal/handlers/courier_orders.go:195-206`
  - ListUpcomingDeliveries (orders in 'accepted'/'preparing' with no courier) drains pgx rows in a `for rows.Next()` loop that `continue`s on scan error and never calls `rows.Err()` afterward. A mid-iteration read error makes rows.Next() return false the same as a clean finish, so the courier's 'upcoming deliveries' list is silently shortened and returned as a 200 success. This is 
  - _Fix:_ Add a rows.Err() check after the loop (before the `if list == nil` guard):      if err := rows.Err(); err != nil {         writeError(w, http.StatusInternalServerError, "failed to fetch upcoming deliveries")         return     }
- **ListRestaurantDeals leaks deals from deactivated restaurants (missing is_active filter)**
  - `backend/internal/handlers/deals.go:379-385`
  - The public per-restaurant deals endpoint GET /api/v1/restaurants/{id}/deals (registered at cmd/api/main.go:221 under OptionalAuth) filters on d.is_active, d.starts_at/expires_at, r.approval_status='approved' and r.vertical — but it omits the `r.is_active = true` guard. Its sibling ListNearbyDeals (deals.go:336) DOES include `AND r.is_active = true`, and the adjacent GetRestaura
  - _Fix:_ Add `AND r.is_active = true` to the WHERE clause of the ListRestaurantDeals query so it matches ListNearbyDeals and the restaurant/menu visibility rules. Change the predicate block to:    WHERE d.restaurant_id = $1      AND d.is_active = true      AND d.starts_at <= NOW()      AN
- **ListSellerDeals and ListRestaurantDeals drain rows without checking rows.Err()**
  - `backend/internal/handlers/deals.go:181-191, 393-404`
  - ListSellerDeals (181-191) and ListRestaurantDeals (393-404) both loop over rows.Next() and never check rows.Err() before writing the JSON response. A mid-stream read error makes Next() return false indistinguishably from a clean end-of-rows, so the endpoint returns HTTP 200 with a silently truncated deals list rather than surfacing the error. ListNearbyDeals in the same file ha
  - _Fix:_ After each deals-collection loop, add `if err := rows.Err(); err != nil { writeError(w, http.StatusInternalServerError, "failed to list deals"); return }` before the writeJSON call, matching the rows.Err() handling already present in ListOrders/GetCart/scanRestaurants.
- **DoorDash webhook fires the consumer 'courier assigned' push twice per delivery**
  - `backend/internal/handlers/doordash_webhook.go:62-75`
  - The DoorDash webhook fires notify.OrderClaimed (the consumer 'your courier has been assigned' push) on BOTH the 'confirmed' and 'enroute_to_pickup' statuses (the case label at line 63 covers both). DoorDash Drive emits these as two separate lifecycle events for essentially every delivery (created -> confirmed -> enroute_to_pickup -> ...), so the consumer reliably receives the s
  - _Fix:_ Fire OrderClaimed on only one DoorDash status. Split the combined case so 'confirmed' triggers the OrderClaimed push and 'enroute_to_pickup' is either handled separately (e.g. a no-op or a distinct 'courier on the way' event) or omitted — matching the single-push behavior of the 
- **ListLinkedProviders drains rows without rows.Err() — a linked auth provider can silently disappear from the account-security list**
  - `backend/internal/handlers/link_auth.go:184-193`
  - ListLinkedProviders loops `for rows.Next()` over user_auth_providers and returns the slice without ever checking `rows.Err()`. A mid-iteration read error truncates the list silently (Next() returns false the same as a clean finish). This is more than cosmetic: the linked-providers list is the account-security surface the client uses to decide what the user can unlink, and Unlin
  - _Fix:_ After the loop add: ```go if err := rows.Err(); err != nil {     writeError(w, http.StatusInternalServerError, "failed to list providers")     return } ``` Return an error on a partial read rather than serving a truncated provider list as complete.
- **MarkOrderReady broadcasts an already-Uber-dispatched order to every online courier**
  - `backend/internal/handlers/orders.go:1181-1183 (guard); externalDeliveryID fetched at 1146/1154`
  - A seller can escalate a platform-mode (or any non-'external') delivery order to Uber Direct / DoorDash while it is still in 'accepted' or 'preparing' status: EscalateToUber (line 1339) only requires status IN ('accepted','preparing','ready') AND courier_id IS NULL AND external_delivery_id IS NULL, and on success Dispatch (dispatch/external.go) stamps both external_provider AND 
  - _Fix:_ Add the same external-delivery guard the inline branch uses to the platform-broadcast branch. Change line 1181 from `if fulfillmentType == "delivery" && deliveryMode == "platform" && restaurantName != "" {` to `if fulfillmentType == "delivery" && deliveryMode == "platform" && ext
- **ListPOSIntegrations drains rows without checking rows.Err() — a connected POS can silently disappear**
  - `backend/internal/handlers/pos_integrations.go:160-167`
  - ListPOSIntegrations iterates `for rows.Next() { ... }` to build the integrations list and then calls writeJSON(w, http.StatusOK, out) with no rows.Err() check. If iteration aborts mid-stream (connection reset, decode error), rows.Next() returns false just like normal completion, so the seller's Integrations screen receives HTTP 200 with a partial or empty list. A seller could t
  - _Fix:_ After the loop, before writeJSON, add: `if err := rows.Err(); err != nil { slog.Error("ListPOSIntegrations: row iteration failed", slog.String("error", err.Error())); writeError(w, http.StatusInternalServerError, "failed to list integrations"); return }`.
- **ListPOSIntegrations and ListMenuImports omit rows.Err, can drop integration/import rows**
  - `backend/internal/handlers/pos_integrations.go:160-167 (ListPOSIntegrations); also backend/internal/handlers/seller.go 958-966 (ListMenuImports)`
  - ListPOSIntegrations (pos_integrations.go:160) iterates `for rows.Next()` and returns the slice with HTTP 200 without calling rows.Err(); a mid-stream read error makes a connected POS integration silently disappear from the seller's Integrations screen, so a still-active Clover link can look disconnected. The same pattern exists in seller.go ListMenuImports (line 958), which ret
  - _Fix:_ In ListPOSIntegrations add `if err := rows.Err(); err != nil { writeError(w, http.StatusInternalServerError, "failed to list integrations"); return }` before writeJSON. In ListMenuImports add the analogous `if err := rows.Err(); err != nil { writeError(w, http.StatusInternalServe
- **attachSellerModifierGroups drains both rows iterators without rows.Err() (parity gap with public attachModifierGroups)**
  - `backend/internal/handlers/seller.go:572-581, 597-605`
  - attachSellerModifierGroups iterates groupRows (572-581) and modRows (597-605) and never calls groupRows.Err() or modRows.Err() after the loops. Its public-side mirror attachModifierGroups in restaurants.go explicitly checks both (restaurants.go:229 `if groupRows.Err() != nil { return }` and restaurants.go:257 `if modRows.Err() != nil { return }`). pgx's rows.Next() returns fals
  - _Fix:_ After `groupRows.Close()` (seller.go:581) add `if groupRows.Err() != nil { return }`, and after the modRows loop ends (seller.go:605) add `if modRows.Err() != nil { return }`, matching attachModifierGroups in restaurants.go. Since both functions bail out (return) on read error, r
- **ListAddresses drops rows.Err() — a user's saved addresses (incl. default) can be silently truncated at checkout**
  - `backend/internal/handlers/user.go:129-143`
  - ListAddresses loops `for rows.Next()` building the address slice, `continue`s on per-row scan errors, then returns the slice with NO `rows.Err()` check. Because pgx surfaces a mid-iteration error by simply making Next() return false, a transient read fault truncates the list and the handler returns HTTP 200 with a partial set. Addresses are ordered `is_default DESC, label`, so 
  - _Fix:_ After the loop, before the `if addresses == nil` guard, add:      if err := rows.Err(); err != nil {         writeError(w, http.StatusInternalServerError, "failed to fetch addresses")         return     }
- **Courier 'new delivery available' broadcast silently truncated (missing rows.Err)**
  - `backend/internal/notify/notifier.go:63-85 (tokensForOnlineCouriers), 41-59 (tokensForUser)`
  - tokensForOnlineCouriers and tokensForUser drain pgx.Rows in a `for rows.Next() { ... }` loop and then `return devices` WITHOUT ever calling rows.Err(). A mid-iteration error (connection reset, server-side cancel, read timeout) makes rows.Next() return false exactly like a clean end-of-rows, so the loop ends early and the function returns a SILENTLY TRUNCATED device list with no
  - _Fix:_ After each loop, check the iterator error before returning, e.g. in tokensForOnlineCouriers: `if err := rows.Err(); err != nil { log.Printf("[notify] iterate online courier tokens: %v", err); return nil }` and the same in tokensForUser (`return devices, err`-style). Returning nil
- **Notifier.tokensForUser drops rows.Err() — order/chat pushes can be silently delivered to only some of a user's devices**
  - `backend/internal/notify/notifier.go:50-58`
  - tokensForUser drains device tokens with a `for rows.Next()` loop that `continue`s on scan error and returns the slice without calling `rows.Err()`. A mid-iteration read error truncates the device list with no signal (the function returns []device and never errors by design), so a user with multiple devices could miss an order-status or chat push on the device(s) whose token row
  - _Fix:_ After the loop add a rows.Err() check and log on failure (the function intentionally returns nil on error):      if err := rows.Err(); err != nil {         log.Printf("[notify] iterate tokens for user=%s app=%s: %v", userID, app, err)     }     return devices  (matching the exist
- **Dispatcher background sweeps drain pgx.Rows without checking rows.Err(), silently truncating refund/payout/dispatch batches**
  - `backend/internal/scheduler/dispatcher.go:397-405, 485-500, 656-665, 872-882`
  - Every `for rows.Next()` loop in the scheduler sweeps (sweepScheduled, sweepAutoDispatch, sweepStaleRejection, and the legacy sweepCourierPayouts claim loop) closes the rows but never calls rows.Err() after the loop. pgx makes Next() return false on a mid-iteration transport error exactly as it does at a clean end of rows, so a connection blip part-way through a result set silen
  - _Fix:_ After each `rows.Close()` (or before iterating the collected slice), add `if err := rows.Err(); err != nil { slog.Error("<sweep>: row iteration failed", slog.String("error", err.Error())); return }`. Apply to all four loops (sweepScheduled at 405, sweepAutoDispatch at 500, sweepS
- **Auto-assigned courier is promised delivery_fee+tip but paid delivery_fee+tip+2.5%-of-subtotal bump**
  - `backend/internal/scheduler/dispatcher.go:464 (payout select), 577 (CourierAutoAssigned); cf. courier_orders.go:366-367`
  - The auto-dispatch sweep computes the payout it shows the courier as `o.delivery_fee + COALESCE(o.courier_tip,0)` (dispatcher.go:464) and pushes that number to the assigned courier via CourierAutoAssigned (dispatcher.go:577). But the actual amount written to courier_payout and transferred at delivery time is `deliveryFee + tip + platformBump`, where `platformBump = subtotal * 25
  - _Fix:_ Make the notified payout match the real payout formula. In the sweepAutoDispatch SELECT (dispatcher.go:464), compute payout as `o.delivery_fee + COALESCE(o.courier_tip,0) + (o.subtotal * 25 / 1000)` (or factor the payout formula into one shared helper used by both DeliverOrder an
- **Custom-tip cap error message integer-truncates the cents, showing the wrong dollar limit**
  - `ios/consumer/KosherEatsConsumer/ViewModels/CheckoutViewModel.swift:139-143`
  - In updateCustomTip the cap is computed as min(maxTipCents, bundle.subtotal) in CENTS, but the user-facing error interpolates `cap / 100` with integer division: errorMessage = "Tip can't exceed $\(cap / 100)". For any subtotal that is not a whole dollar — e.g. a $10.50 cart (cap = 1050 cents) — the message reads 'Tip can't exceed $10' instead of '$10.50', understating the real l
  - _Fix:_ Render the cap with the shared cents->dollars formatter: errorMessage = "Tip can't exceed \(Money.dollars(cap))" so $10.50 shows correctly.
- **Apple Pay sheet shows a $0.00 "Delivery" line item on pickup orders that the on-screen totals hide**
  - `ios/consumer/KosherEatsConsumer/ViewModels/CheckoutViewModel.swift:408-445 (applePaySummaryItems), esp. 419-420`
  - applePaySummaryItems unconditionally appends a "Service fee" and "Delivery" row using bundle.serviceFee / bundle.deliveryFee, with no >0 guard. For a pickup order the backend zeros the delivery fee (and tip), so the native Apple Pay sheet renders a literal "Delivery $0.00" line. The on-screen TotalsCard in CheckoutView deliberately suppresses this row (CheckoutView.swift:617 `i
  - _Fix:_ Mirror the TotalsCard gating in applePaySummaryItems: only append the Delivery row when `bundle.deliveryFee > 0` (and consider the same for tip, which already is gated). The reconciliation delta row at lines 428-432 will still make the visible rows sum to bundle.total, so droppin
- **Custom-tip cap error message truncates cents to whole dollars**
  - `ios/consumer/KosherEatsConsumer/ViewModels/CheckoutViewModel.swift:139-143`
  - updateCustomTip computes cap = min(maxTipCents, subtotal) (cents) and, when the typed tip exceeds it, shows errorMessage = "Tip can't exceed $\(cap / 100)". cap/100 is integer division, so a subtotal of $10.50 (cap = 1050) renders as "Tip can't exceed $10" instead of $10.50. The user is told a lower ceiling than the real one (the value they're being blocked at), which is confus
  - _Fix:_ Format the cap with the existing Money helper instead of integer division: errorMessage = "Tip can't exceed \(Money.dollars(cap))". Money.dollars renders cents as "$10.50", matching the actual ceiling.
- **Order tracking opens a useless courier SSE stream for external (Uber Direct / DoorDash) deliveries**
  - `ios/consumer/KosherEatsConsumer/ViewModels/OrderTrackingViewModel.swift:29-35, 121-192`
  - OrderTrackingViewModel.start() unconditionally calls startLocationStream(), which opens GET /orders/:id/location/stream, for EVERY order — including external-delivery orders fulfilled by Uber Direct / DoorDash Drive (Order.isExternalDelivery == true). External orders have no platform courier and never produce a courier location ping; the backend StreamOrderLocation handler (bac
  - _Fix:_ In start(), gate the location stream on the order being a platform delivery. Because refresh() is awaited first, self.order is populated: replace the unconditional `startLocationStream()` with `if !(order?.isExternalDelivery ?? false) { startLocationStream() }`. (Polling alone al
- **Pickup orders show bogus '$0.00' Delivery Fee and Service Fee rows in order detail and confirmation**
  - `ios/consumer/KosherEatsConsumer/Views/Orders/OrderDetailView.swift:347-348 (also OrderConfirmationView.swift:266-267)`
  - The backend zeros both the delivery fee and the service fee for pickup orders (backend/internal/handlers/orders.go: deliveryFee=0 when fulfillment_type=='pickup', serviceFee:=0 unconditionally). OrderDetailView.priceBreakdown and OrderConfirmationView.priceCard both render the 'Delivery Fee' and 'Service Fee' rows UNCONDITIONALLY via SummaryRow/priceRow, so a pickup order's bre
  - _Fix:_ Wrap each fee row in a positivity guard, mirroring CheckoutView: `if order.deliveryFee > 0 { SummaryRow(label: "Delivery Fee", value: order.deliveryFeeFormatted) }` and `if order.serviceFee > 0 { SummaryRow(label: "Service Fee", value: order.serviceFeeFormatted) }`, in both Order
- **Tracking screen renders an all-empty 6-step progress timeline for cancelled/rejected orders**
  - `ios/consumer/KosherEatsConsumer/Views/Orders/OrderTrackingView.swift:248-273 (statusHeader) / 323-325 (progressBar) / 519-545 (DeliveryTimeline.node)`
  - statusHeader(for:) calls progressBar(for: order.status) UNCONDITIONALLY (line 265), which renders DeliveryTimeline(stepIndex: order.status.stepIndex). For .cancelled and .rejected, OrderStatus.stepIndex is -1 (Models.swift:130). In DeliveryTimeline.node, isDone = index < -1 (always false) and isActive = index == -1 (always false), so every node is an empty ring and every connec
  - _Fix:_ In statusHeader, gate the progress bar the same way OrderDetailView does: only render `progressBar(for: order.status)` when `order.status != .cancelled && order.status != .rejected` (i.e. when order.status.stepIndex >= 0). For terminal/negative-index states, omit the timeline (th
- **EarningsView dollar total and delivery count read from two independent history fetches**
  - `ios/courier/KosherEatsCourier/Views/Earnings/EarningsView.swift:32-41 (with DashboardViewModel.swift:189-204)`
  - On the courier Earnings screen the "Today's earnings" dollar figure and the "N deliveries today" count are sourced from two different network responses. `todayTotal` returns `dashVM.todayEarnings`, which DashboardViewModel.loadTodayEarnings() computes from its OWN `api.listHistory()` call. `todayDeliveryCount` instead filters EarningsView's separately-fetched `@State history` (
  - _Fix:_ Derive both the today total and the today count from a single source of truth. Compute todayTotal locally from the same `history` array the count uses: `private var todayTotal: Int { history.filter { isToday($0.deliveredAt) }.reduce(0){ $0 + $1.courierPayout } }`, factoring the t
- **Phone OTP auto-submit never fires for real (6-digit) codes — autofill silently does nothing**
  - `ios/seller/KosherEatsSeller/Views/Auth/PhoneOTPView.swift:62-66`
  - The code field's onChange auto-submits only when `code.count == maxCodeLength` where maxCodeLength = 10. The inline comment claims this fires on 'iOS autofill from the SMS', but Twilio Verify codes are 6 digits (the comment at the top of the file even says '6 digits by default'). When iOS autofills the actual code via .oneTimeCode, code.count becomes 6, which never equals 10, s
  - _Fix:_ Auto-submit when the entered code reaches a plausible complete length rather than the hard cap. Simplest correct behavior: trigger submit once `code.count >= minCodeLength` AND the field just received a paste/autofill, or pragmatically auto-submit when `code.count == 6` (the Twil
- **Pickup orders render $0.00 Delivery Fee and Service Fee rows in the seller payment breakdown**
  - `ios/seller/KosherEatsSeller/Views/Orders/SellerOrderDetailView.swift:345-378`
  - priceSection() unconditionally renders priceRow("Delivery Fee", value: order.deliveryFee) and priceRow("Service Fee", value: order.serviceFee). The backend (orders.go CreateOrder) forces deliveryFee = 0 for pickup orders (fulfillmentType == "pickup") and serviceFee is always 0 for every order, so a pickup ticket shows a meaningless 'Delivery Fee $0.00' line — directly contradic
  - _Fix:_ Guard the rows on a non-zero / non-pickup value, e.g. `if !order.isPickup && order.deliveryFee > 0 { priceRow("Delivery Fee", value: order.deliveryFee) }` and `if order.serviceFee > 0 { priceRow("Service Fee", value: order.serviceFee) }`. Subtotal, Tax, optional Savings, optional
- **Seller order detail shows raw ISO8601 timestamp as the delivery/pickup ETA**
  - `ios/seller/KosherEatsSeller/Views/Orders/SellerOrderDetailView.swift:280-289 (render at 285); model at ios/seller/KosherEatsSeller/Models/Models.swift:555,617`
  - The seller's order detail screen renders the ETA by interpolating the raw backend string: `if let eta = order.estDeliveryTime { Text("ETA: \(eta)") }`. `order.estDeliveryTime` is decoded as a plain `String?` (Models.swift:555 / decodeIfPresent at 617) and is never parsed into a `Date` or formatted. The backend emits `est_delivery_time` as a non-omitempty `time.Time` (backend/in
  - _Fix:_ Parse and format the timestamp before display instead of interpolating the raw string. Simplest: add a computed `estDeliveryTimeFormatted` on `Order` that reuses the existing ISO8601 fallback parsers (`iso8601Frac`/`iso8601Plain`) already defined for `createdAtDate`, returns a `.
- **Escalate-to-Uber button shown on already-dispatched accepted/preparing orders**
  - `ios/seller/KosherEatsSeller/Views/Orders/SellerOrderDetailView.swift:405-437 (specifically 419 and 436)`
  - In the .accepted and .preparing branches of actionButtons(_:), the "Dispatch to Uber" escalate button is rendered for ANY non-pickup order with the single guard `if !order.isPickup { escalateButton(order) }` — it does NOT check whether the order already has an external delivery in flight (`externalDeliveryId`) or a claimed courier. The backend EscalateToUber handler (backend/in
  - _Fix:_ Apply the same eligibility guard the .ready branch already uses. Replace the bare `if !order.isPickup { escalateButton(order) }` at lines 419 and 436 with `if !order.isPickup && order.courier == nil && (order.externalDeliveryId ?? "").isEmpty { escalateButton(order) }` (or factor
- **Pickup orders show bogus $0.00 Delivery Fee and Service Fee rows in seller order detail**
  - `ios/seller/KosherEatsSeller/Views/Orders/SellerOrderDetailView.swift:345-378`
  - priceSection unconditionally renders `priceRow("Delivery Fee", value: order.deliveryFee)` and `priceRow("Service Fee", value: order.serviceFee)`. For pickup orders the backend forces both to 0 (payments.go: `deliveryFee := 0; if !isPickup {...}` and `serviceFee := 0`; CreateOrder zeroes delivery fee for fulfillment_type=='pickup'), so the seller sees "Delivery Fee $0.00" and "S
  - _Fix:_ Gate the two rows on delivery: `if !order.isPickup { priceRow("Delivery Fee", value: order.deliveryFee); priceRow("Service Fee", value: order.serviceFee) }` (or, more robustly, only render each row when its value > 0), matching the consumer app's pickup handling.
- **Pickup orders render a 'Delivery' card with a blank address and map-pin in seller order detail**
  - `ios/seller/KosherEatsSeller/Views/Orders/SellerOrderDetailView.swift:266-296`
  - deliverySection is rendered for every order with a 'Delivery' header, a mappin.circle.fill icon, and `Text(order.deliveryAddress)`. The backend allows an empty delivery_address on pickup orders (orders.go only requires an address when fulfillment_type=='delivery'; pickup orders persist delivery_address as empty). So on a pickup order the seller sees a section titled 'Delivery' 
  - _Fix:_ For pickup orders, either hide deliverySection entirely (`if !order.isPickup { deliverySection(order) }`) or relabel it as a pickup card (header 'Pickup', show 'Customer picks up at counter' instead of an empty address). At minimum, guard the address row with `if !order.deliveryA
- **Orders-tab 'today' ticker uses device timezone while seller dashboard uses America/New_York**
  - `ios/seller/KosherEatsSeller/Views/Orders/SellerOrdersView.swift:32-46`
  - SellerOrdersView.todayOrders/todayRevenue compute the 'today' boundary with `Calendar.current` + `TimeZone.current` (device-local) and sum subtotals client-side. The seller dashboard stats the same seller sees on the Dashboard tab are computed by the backend using a fixed America/New_York boundary (seller.go GetDashboardStats: `(o.created_at AT TIME ZONE 'America/New_York')::da
  - _Fix:_ Pin the Orders-tab 'today' boundary to the same timezone the backend uses: build the Calendar with `TimeZone(identifier: "America/New_York")` (or, better, drive the ticker from the same `getDashboardStats` values the Dashboard uses) so the two surfaces always agree on what 'today
